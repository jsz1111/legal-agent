# 多模态功能使用说明

## 功能概述

多模态功能允许用户**上传图片**作为法律证据，系统自动提取图片中的关键信息（订单号、商品描述、价格、聊天内容等），并注入到对话流程中，无需手动输入。

## 核心特性

### 1. **上下文感知的智能分析**

系统根据当前对话上下文生成针对性的图片分析提示词：

- **消费纠纷场景**：重点提取订单信息、商品瑕疵、防伪码、退换货政策
- **劳动纠纷场景**：重点提取劳动合同条款、工资金额、工作时间、解除通知
- **合同纠纷场景**：重点提取合同条款、违约条款、双方信息、签订日期
- **交通事故场景**：重点提取事故认定书、责任划分、医疗费用

### 2. **追问感知的动态提示**

系统分析助手最近的追问内容，生成针对性提示：

```
助手：您有订单截图吗？
用户：[上传图片]
→ 系统自动识别这是订单截图，重点提取订单号、商品信息、价格
```

```
助手：请问有聊天记录吗？
用户：[上传图片]
→ 系统自动识别这是聊天记录，重点提取对话内容、商家回复
```

### 3. **无缝注入对话流**

图片分析结果自动转化为用户消息注入 GuideState，后续流程正常执行：
- `extract_issues`：识别新的法律问题
- `score`：重新评估证据完整度
- `retrieve`：可能触发重新检索
- `conclude`：更新维权方案

## 配置方法

### 1. 获取阿里云 API Key

访问 [阿里云百炼平台](https://bailian.console.aliyun.com/)，创建 API Key。

### 2. 配置 .env 文件

```env
# 启用多模态功能
ENABLE_MULTIMODAL=true

# 选择模型（推荐 qwen3.7-plus，性价比高）
VL_MODEL=qwen3.7-plus

# 填入 API Key
VL_API_KEY=sk-xxxxxxxxxxxxxxxxxx
```

### 3. 安装依赖

```bash
pip install dashscope
```

### 4. 重启服务

```bash
python -m uvicorn src.main:app --port 8001 --reload
```

## 使用方式

### 方式1：Gradio 测试台

1. 打开 http://localhost:7862
2. 左侧「图片证据上传」面板，点击上传图片
3. 点击「分析图片」按钮
4. 系统自动提取信息并更新对话

### 方式2：API 调用

```python
import requests

files = {'file': open('order_screenshot.jpg', 'rb')}
data = {
    'user_id': 'user_123',
    'session_id': 'session_456',
    'auto_inject': 'true'  # 自动注入对话流
}

response = requests.post(
    'http://localhost:8001/api/v1/chat/upload-image',
    files=files,
    data=data
)

result = response.json()
print(result['analysis'])  # 图片分析结果
print(result['assistant_reply'])  # 助手回复
```

## 典型场景示例

### 场景1：消费维权 - 假货投诉

```
用户：我在淘宝买了个包，收到后发现是假货
助手：请问您有什么证据吗？
用户：[上传订单截图]
系统提取：
  - 订单号：123456789
  - 商品：某品牌包
  - 价格：1999元
  - 商家：XX旗舰店
  - 下单时间：2024-01-15
助手：收到您的订单信息。请问包具体哪里有问题？
用户：[上传商品照片]
系统提取：
  - 图片类型：商品照片
  - 瑕疵描述：logo印刷模糊，缝线不齐，拉链卡顿
  - 法律相关性：可作为"以假充真"的直接证据
助手：【更新维权方案，包含订单信息和瑕疵证据】
```

### 场景2：劳动纠纷 - 拖欠工资

```
用户：公司拖欠我工资
助手：请问您有劳动合同吗？
用户：[上传劳动合同照片]
系统提取：
  - 合同类型：劳动合同
  - 公司名称：XX科技有限公司
  - 月薪：8000元
  - 签订日期：2023-06-01
助手：请问有工资条或银行流水吗？
用户：[上传工资条照片]
系统提取：
  - 工资条显示月薪8000元
  - 最后一次发放：2023-10月
  - 已拖欠3个月
助手：【更新维权方案，计算欠薪金额24000元】
```

## 成本估算

使用阿里云百炼平台 qwen-vl-v1 模型（最便宜）：
- 每张图片分析：约 0.005-0.01 元
- 每月上传100张图片：约 0.5-1 元

## 注意事项

1. **可选功能**：不启用多模态，系统仍可通过纯文字对话正常工作
2. **隐私保护**：上传的图片保存在本地 `uploads/` 目录，不会上传到第三方服务器
3. **图片质量**：建议上传清晰的截图，模糊照片可能影响识别准确率
4. **文件大小**：建议单张图片不超过 5MB

## 故障排查

### 问题1：提示"多模态功能未启用"

**原因**：.env 中未配置或配置错误

**解决**：
```env
ENABLE_MULTIMODAL=true
VL_API_KEY=sk-your-key-here  # 不要留空
```

### 问题2：提示"dashscope 库未安装"

**解决**：
```bash
pip install dashscope
```

### 问题3：图片分析返回错误

**可能原因**：
- API Key 无效或过期
- 图片格式不支持（仅支持 jpg/png）
- 网络连接问题

**解决**：
- 检查 API Key 是否正确
- 转换图片格式
- 检查网络连接

## 技术架构

```
用户上传图片
  ↓
保存到 uploads/{user_id}/{uuid}.jpg
  ↓
从 Redis 恢复 GuideState（获取上下文）
  ↓
调用 build_context_aware_question()
  - 根据 legal_domain 生成领域特定提示
  - 根据 confirmed_issues 生成问题相关提示
  - 根据 evidence_unavailable 生成缺失证据提示
  - 根据 recent_assistant_message 识别追问意图
  ↓
调用阿里云视觉模型分析图片
  ↓
构造 "【图片证据补充】\n{analysis}" 消息
  ↓
注入 _run_guide_turn() 或 Supervisor
  ↓
GuideState 正常处理（extract_issues → score → retrieve → conclude）
  ↓
返回助手回复 + 更新后的对话历史
```

## 未来扩展

- [ ] 支持多张图片批量上传
- [ ] 支持 PDF 文档解析
- [ ] 支持视频证据分析
- [ ] OCR 专项优化（合同、判决书）
- [ ] 图片证据归档管理
