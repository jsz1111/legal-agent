# 法律多智能体平台 · 项目说明

> 文档基于 2026-07-27 的代码实况（分支 `feature/multimodal`）逐文件核对生成，与 `README.md` 若有冲突，以本文件为准。
> 数据库当日未启动，所有数据量为文档记载值，已标注最后核查日期，使用前请重新核查。

## 一、项目定位

面向不懂法的普通市民（ToC）的法律 AI 智能体系统。用户用大白话描述纠纷，系统完成案情理解、法条与类案检索、胜算评估，输出可执行的维权行动方案，并可按需生成参考文书草稿。架构预留 ToB 扩展位。

项目采用 Supervisor + 领域 Worker + LangGraph 状态机的多智能体范式，面向法律问答、维权指引和法律统计场景组织工作流。

## 二、技术栈

| 类别 | 实际使用 |
|---|---|
| API | FastAPI + Uvicorn，SSE 流式；Gradio 调试台（7862） |
| Agent | LangChain / LangGraph（StateGraph + AsyncRedisSaver Checkpointer）、langchain-deepseek |
| LLM | DeepSeek `deepseek-v4-flash`（Supervisor 内为字面量，未读 settings）；可选多模态 VL 模型 |
| Embedding | 代码默认 `ollama` + `bge-large`，`.env` 实际为 `volcengine` + `doubao-embedding-vision-251215`，维度固定 1024 |
| 精排 | DashScope `qwen3-rerank` |
| 存储 | PostgreSQL 17 · Milvus 2.6 · Neo4j 5.20 · Redis Stack 7.4 · MinIO |
| 中文 NLP | jieba（BM25 分词）、pypinyin |
| 文档解析 | MinerU、python-docx、BeautifulSoup |
| 其他 | Alembic、pytest、TruLens、Docker Compose |

**端口注意**：`src/core/config.py` 里的默认端口是通用默认值（5432/6379/19530/7687/9000），`.env` 实际把全套存储改到独立端口 **5433 / 6380 / 19531 / 7688 / 9010**，与其他项目容器完全隔离。`DB_HOST` 必须写 `127.0.0.1`，写 `localhost` 时 asyncpg 会优先走 IPv6 导致连接失败。

## 三、系统架构

### 1. 双层路由

```
用户输入 → POST /api/v1/chat
              │
              ├─ Redis 存在 guide_active:{user_id}:{session_id} ─→ 直驱指引状态机（完全绕过 Supervisor）
              │
              └─ 否则 ─→ Supervisor Agent ─→ Agent-as-Tool 调度 Worker
```

`thread_id = f"{user_id}:{session_id}"`。GuideState 以 JSON 存 `guide_state:{thread_id}`，TTL 取 `REDIS_SESSION_TTL`（默认 3600s）。

**透传双保险**：Supervisor 容易把 Worker 回复改写成摘要，因此 `guide_agent` 首轮回复会额外写入 Redis 键 `guide_last_reply:{thread_id}`；路由层若发现该键存在，直接用原始回复覆盖 Supervisor 输出。Supervisor 系统提示词也把「不改写专项助手回复」列为最高优先级。

### 2. Worker 清单

`WORKER_TOOLS` 注册 5 个，加 `save_memory` / `search_memory` 共 7 个工具挂在 Supervisor 上。

| 工具 | 面向对象 | 实现状态 |
|---|---|---|
| `call_guide_agent` | 普通市民，具体纠纷 | ✅ 真实实现，唯一带 `ToolRuntime[UserContext]` 注入 user_id/session_id |
| `call_legal_qa_agent` | 通用法律知识问答 | ✅ 真实实现，挂 5 个 RAG 工具 |
| `call_professional_agent` | 法律从业者 | 🚧 占位，返回固定「功能正在建设中」，`get_*_agent()` 返回 None |
| `call_exam_agent` | 法学学生 | 🚧 占位，同上 |
| `call_operation_agent` | 运营 | 🚧 占位，有 agent 但 `tools=[]`，提示词要求回「功能开发中」 |

Supervisor 侧记忆：`AsyncRedisSaver` 做短期 checkpoint，`MilvusStore(dims=1024)` 做长期记忆，`SummarizationMiddleware` 在 4000 tokens / 6 条消息时触发摘要并保留 6 条。

### 3. 公民法律指引状态机（核心引擎）

`src/agents/legal_guide/graph.py`，**实际注册 14 个节点**：`dispatcher`、`load_context`、`check_urgency`、`extract_issues`、`clarify`、`score`、`retrieve`、`conclude`、`ask_details`、`ask_facts`、`ask_evidence`、`parse_details`、`save_record`、`generate_doc`。入口为 `dispatcher`。

```
dispatcher（仅 round+1 计数器）
   │ round==0 → load_context → check_urgency；否则直接 check_urgency
   ▼
check_urgency（每轮必过的高危熔断）
   ├─ phase==END 且命中文书请求 → generate_doc → END
   ├─ phase==END → END
   ├─ 有 pending_ask_details → parse_details
   └─ 否则 → extract_issues
   ▼
extract_issues（三层法律术语标准化）
   ├─ 信息严重不足 → clarify → END（最多 2 轮）
   └─ 否则 → score
   ▼
score（三维加权置信度，零 I/O 零 LLM）→ retrieve（无条件）
   ▼
retrieve（并行检索：法条双路 + 类案 + 图谱）→ conclude（无条件）
   ▼
conclude（五段式行动方案 + 胜算评估）
   ├─ HIGH 或 force_conclude ────────→ save_record → END
   ├─ MEDIUM 且 evidence_rounds<3 ──→ ask_evidence → END
   ├─ LOW 且 facts_rounds<3 ────────→ ask_facts → END
   └─ 否则 ─────────────────────────→ save_record → END
       ▲
       └── 下一轮用户回复 → parse_details → 回到 extract_issues 或 score（循环）
```

代码实况中三处与旧文档不一致，维护时需留意：

- `ask_details` 已是**死节点**（源码注释「保留兼容」），路由函数恒返 END
- `score` 和 `retrieve` 挂的是条件边，但两个路由函数各分支返回值相同，实际无分支
- `convergence.py` 里的 `check_convergence`（`max_rounds=4`）在 `graph.py` 被 import 但**全仓无调用**，是死代码；真正生效的是 `should_conclude`

### 4. 置信度与轮次控制

`confidence.py` 三维加权，纯规则、无 LLM、刻意前置于检索，保证任何档位都有法律依据可输出：

| 维度 | 权重 | 计分方式 |
|---|---|---|
| 证据完整度 | 0.40 | `len(evidence_confirmed) / evidence_total`，封顶后 ×0.40 |
| 事实清晰度 | 0.30 | domain_locked / region_known / time_known 各 +0.10 |
| 权责清晰度 | 0.30 | `confirmed_issues>=1` +0.20，`>=2` 再 +0.10 |

分档阈值来自配置：**HIGH ≥ 0.65，MEDIUM ≥ 0.50，其余 LOW**（`GUIDE_CONFIDENCE_HIGH` / `GUIDE_CONFIDENCE_MID`）。

生效的轮次上限：总轮次 `max_rounds=12`（`should_conclude` 调用处实传值）、澄清 2 轮（`GUIDE_MAX_CLARIFY_ROUNDS`）、追问 10 轮（`GUIDE_MAX_ASK_ROUNDS`）、`ask_facts` / `ask_evidence` 各硬编码 < 3 轮。`state.py` 注释里写的 5/5 是过时说明。

`should_conclude` 收敛条件：总轮次达 12 强制收敛；HIGH 且已确认 ≥1 项证据；MEDIUM 且证据比例 ≥ 0.4（分母 `max(len(evidence_tpl), 3)`）；LOW 一律继续追问。

### 5. 三层法律术语标准化

中文口语到法律术语的映射是本项目最核心的技术难点（"老板炒了我" → "违法解除劳动合同赔偿"）。`issue_normalizer.py`：

- **L1 LLM 提取** — 不用 `with_structured_output`（DeepSeek thinking mode 不支持 `tool_choice`），走 JSON 提示词，解析失败降级为空 issue + domain=other
- **L2 Neo4j 精确匹配** — `MATCH (c:LegalConcept) WHERE c.name IN $names`，逐字相等；另查 `Domain` 节点确认领域。图谱未构建时降级为全部未命中
- **L3 Milvus 语义兜底** — 集合 `legal_term_index`，阈值 **0.75**，`top_k=3`，COSINE / nprobe=16，同领域候选优先，否则取全局最高分；擦边分（margin 0.08）仅记日志不采纳

出口刻意维持**两个不合并的 issue 池**：`standard`（L2+L3 结果，可喂 BM25 / PG ilike / Dense）与 `colloquial`（只喂 Dense / HyDE）。原因是 BM25 对中文口语近乎零召回，口语词混入会污染精确匹配通道。

## 四、检索（RAG）流水线

### 1. 模块职责

| 模块 | 作用 | 数据源 | top_k / 精排 |
|---|---|---|---|
| `statute_rag.py` | 法条检索 → 精排 → PG 补法律标题 → 生成 → 幻觉校验 | Milvus `statute_index`；PG `laws` / `articles` | 30 → 8 |
| `case_rag.py` | 类案检索 → PG 补 title/gist → 质量过滤 → 生成 | Milvus `case_index`；PG `legal_cases` | 15 → 5 |
| `graph_rag.py` | 实体抽取 → NL2Cypher（重试 2 次）→ Neo4j → LLM 整合 | Neo4j，结果截断 20 条 | — |
| `nl2sql.py` | 渠道等结构化查询 | PostgreSQL | 强制 LIMIT 20 |
| `doc_rag.py` | 知识文档检索 + HyDE + 精排 | Milvus `knowledge_docs` | 20 → 5 |
| `hyde.py` | 假设文档生成，三模式 `statute` / `case` / `general` | — | 失败回退原句向量 |
| `reranker.py` | DashScope `qwen3-rerank`；文档数 ≤ top_k 时直接返回；SDK 缺失或异常回退向量序 | — | 默认 3 |
| `hallucination_check.py` | LLM 判断答案是否被证据支撑，阈值 0.7 | — | 异常时返回 grounded=True（失败即放过） |
| `audit.py` | `QueryAuditLog` + `Timer`，loguru `bind(audit=True)` | — | — |
| `runtime.py` | `lru_cache` 单例：LLM(temperature=0.3) + embedding + MilvusClient + Neo4j driver | — | — |

`tools.py` 对外暴露 5 个工具：`search_statute`、`search_similar_cases`、`search_legal_graph`、`search_channels`、`search_legal_docs`。除 `search_similar_cases` 外都先经 `_rewrite()` 术语改写。

### 2. 两层融合

- **Milvus 内混合检索**：dense（COSINE，nprobe=16）+ sparse BM25（IP）经 `hybrid_search` + `RRFRanker(k=60)`。BM25 模型从本地 `models/bm25_statute.json` / `models/bm25_case.json` 加载，异常时降级为纯 dense。
- **应用层双路 RRF**：`legal_guide/graph.py` 自实现 `_rrf_fuse(k=60, top_n=20)`，融合「领域过滤路」与「全库路」两条法条检索结果（两路均 `skip_rerank=True`），再统一精排 top 8。纯 RRF，无加权。双路设计的目的是防止领域分类错误直接导致零召回。

另有三处兜底：法条领域过滤会额外放行民法典（`law_id == "75"`）；Milvus 失败时走 PG `ilike` 兜底（limit 8）；类案零命中时由 LLM 生成裁判文书网检索建议，以 `fallback_guide` 返回。

**注意**：类案检索**不使用 HyDE**；`doc_rag` 没有 sparse 通道也没有 RRF。

## 五、数据层

### 1. PostgreSQL（7 张表，`src/modules/legal/model.py`）

`channels` · `laws` · `articles`（law_id FK）· `legal_cases` · `law_cases`（关联表）· `users` · `consultations`

数据量（最后核查 2026-07-25）：

| 表 | 行数 | 来源 |
|---|---|---|
| `laws` | 75 | 法律 53 + 行政法规 14 + 司法解释 5 + 其他 3 |
| `articles` | 7,105 | python-docx 解析 + 正则匹配「第 X 条」 |
| `legal_cases` | 231 | CAIL2019 刑事 + 劳动/消费 HTML + 预付卡 |
| `channels` | 103 | 结构化维权渠道（电话/网站/地址） |
| `law_cases` | 0 | 尚未建立关联 |

### 2. Milvus 集合

| 集合 | 用途 | 嵌入策略 |
|---|---|---|
| `statute_index` | 法条（约 7,077） | Dense 1024 + Sparse BM25 |
| `case_index` | 类案 | Dense 1024 + Sparse BM25 |
| `legal_term_index` | 法律术语（约 13,268） | Dense 1024 |
| `agent_long_term_memory` | 长期记忆 | Dense 1024，IVF_FLAT / COSINE / nlist=128 |
| `knowledge_docs` | 知识文档 | Dense 1024 |

`case_index` 的实际条数在历史文档中有 12 与 24 两个记载，属初始化阶段的临时状态而非架构限制，导入前请直连核查。

### 3. Neo4j 知识图谱（最后核查 2026-07-25）

节点：Law 75 · Article 7,105 · LegalCase 1,680 · Channel 103 · Domain 23 · LegalConcept 13,268
关系：HAS_ARTICLE 7,105 · APPLIES_TO 77 · HANDLES 103 · BELONGS_TO 12,706 · CITES 0（合计约 19,991）

### 4. 待导入数据包

两个已就绪但**尚未入库、未接入 Agent** 的数据包：

**`案例数据包/`** — 752 条民事裁判文书，从 16,585 条原始数据按每案由上限 50 条筛出，覆盖 123 种原始案由，提供 CSV / JSONL / SQLite 三份等价文件。要点：全为民事案件（刑事是另一批）；刻意**不做 Agent 领域映射**、保留原始案由，检索时不强制领域过滤，这是为绕开旧域码漏召回问题的有意设计，导入时不要擅自加 domain 字段；只向量化 `retrieval_text`（443–754 字）不向量化全文，一案一 Dense + 一 BM25，不做递归切分；JSONL/CSV 用中文字段名而 SQLite 用英文字段名，需字段映射层。**向量化前必须确认 embedding 配置**：数据包方案文档写的是本地 Ollama bge-large，与 `.env` 实际的 volcengine 不一致，换模型会与现有 collection 和长期记忆 Store 不兼容。

**`nl2sql数据包/`** — 中国法律年鉴 2019–2021 统计数据（统计年份 2015–2020）：193 个工作簿 / 193 datasets / 3,044 records / 11,612 facts / 23,573 cells，覆盖法院、检察、刑事、民事、行政复议、交通事故等 16 个主题。目标库 `legal_statistics_db`，schema `legal_statistics`，附 `load_postgres.py` 与 `validate_package.py`。优先级低于案例数据包。

## 六、API 接口

`src/api/routers/chat.py`，prefix `/api/v1/chat`：

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/v1/chat` | 非流式，请求 `{user_id, session_id, message}`，响应 `{reply, session_id, debug}` |
| POST | `/api/v1/chat/stream` | SSE，事件 `{"type": "token" \| "done" \| "error"}`；指引回复整体单块推送，不逐 token |
| POST | `/api/v1/chat/upload-image` | multipart 图片证据分析，未启用多模态时返回 `{"enabled": false}` |
| GET | `/health` · `/health/deps` · `/api/v1/legal/health` | 存活检查 / 五存储连接状态 / 法律模块状态 |

指引流程额外返回 `debug` 字段：`domain`、`confidence_tier`、`statute_hits`、`case_hits`、`graph_laws`、`graph_channels`。

**多模态图片分析**（`feature/multimodal` 分支）：文件落到本地磁盘 `uploads/{user_id}/{uuid}{ext}`（**未走 MinIO**），从 Redis 恢复 GuideState 生成上下文感知提示词，调 `analyze_image()`，默认 `auto_inject=True` 把「【图片证据补充】…」作为用户消息注入对话流。

## 七、文书生成

`is_doc_request()` 靠 9 个关键词 + ≤10 字的肯定短语集合判定，是 `generate_doc` 的唯一触发条件。`generate_legal_doc()` 按 domain 查 `DOC_TYPE_MAP` 得文书类型（默认「投诉信」），再查 `DOC_TEMPLATES` 注入格式框架，单次 LLM 调用返回 `(doc_type, doc_text)`。

`DOC_TYPE_MAP` 映射 **13 个领域**（劳动仲裁申请书、消费者投诉信、催告函、刑事报案材料、离婚协议书草稿、人身损害赔偿申请书、行政复议申请书、侵权警告函、医疗损害赔偿申请书、网络侵权/诈骗报案材料、环境污染举报信、仲裁申请书、投诉信），但 `DOC_TEMPLATES` 只为 **5 类**提供结构化模板（劳动仲裁申请书、行政复议申请书、消费者投诉信、人身损害赔偿申请书、医疗损害赔偿申请书），其余 8 类走无模板自由生成。

## 八、安全与质量机制

- **多轮高危熔断** — `check_urgency` 每轮执行（用户可能在对话中途才追加家暴、拘押等信息），命中人身安全威胁立即终止并推送 110 / 12348
- **法条幻觉校验** — 用检索到的法条原文对生成答案做 grounding check（阈值 0.7）
- **置信度分级输出** — HIGH / MEDIUM / LOW 控制结论确定性，各档有独立输出模板
- **不利事实跟踪** — `adverse_facts` 跨轮累积用户自身不利事实；`evidence_unavailable`（无法取得的证据）自动并入胜算评估的不利因素
- **NL2SQL 护栏** — 仅允许 SELECT，正则黑名单拦截 INSERT/UPDATE/DELETE/DROP/ALTER/CREATE/TRUNCATE/GRANT/REVOKE，自动补 LIMIT 20，10s 超时，重试 2 次
- **检索超时保护** — statute 8.0s / case 5.0s / graph 3.0s
- **渠道兜底** — `fmt_channels` 最多输出 6 条渠道，为空时提示 12348

**未覆盖项**：三个 chat 端点**均无鉴权**，`user_id` 由客户端自报；无速率限制；文件上传无类型/大小白名单；NL2Cypher 只靠提示词约束，无只读语法校验。生产部署前必须补齐。

## 九、目录结构

```
src/
├── main.py                     FastAPI 应用工厂 + 健康检查
├── agents/
│   ├── supervisor_agent.py     意图路由（7 工具）
│   ├── tools/                  worker_tools / store_tools / multimodal_tools
│   ├── workers/                guide · legal_qa（真实）+ professional · exam · operation（占位）
│   ├── legal_guide/            ★ 14 节点状态机：graph / state / prompts / issue_normalizer
│   │                             / confidence / convergence / neo4j_queries / db_queries
│   │                             / formatters / doc_generator
│   └── legal_knowledge/        RAG 工具箱：statute_rag / case_rag / graph_rag / nl2sql
│                                 / doc_rag / hyde / reranker / hallucination_check
│                                 / audit / runtime / tools
├── api/routers/                chat.py（双路由 + 图片上传）· legal.py
├── core/                       config / deps / logger / exceptions / base_*
├── infra/                      database · milvus_client · milvus_store · neo4j_client
│                                 · redis_cache · minio_client · embedding · volcengine_embeddings
├── middlewares/                logging
├── modules/legal/model.py      7 张 SQLAlchemy 表
└── utils/                      jwt_utils · password_utils（暂未接入 API）

scripts/                        22 个脚本：init_legal_postgres / init_legal_neo4j
                                / init_milvus_indexes / build_legal_concepts
                                / build_case_pilot_dataset / crawl_civil_cases
                                / repair_sparse_bm25 / update_milvus_incremental
                                / gradio_chat_demo 等
test/                           6 个 pytest 文件 + fixtures
案例数据包/ nl2sql数据包/        待导入数据包
乱文件/                          历史测试报告与方案文档（非代码）
models/                          bm25_statute.json · bm25_case.json
```

## 十、快速开始

```bash
# 1. 基础设施
docker compose up -d

# 2. 环境
conda create -n legal python=3.13 && conda activate legal
pip install -r requirements.txt

# 3. 配置：cp .env.example .env，填 DeepSeek / DashScope Key
#    DB_HOST 必须写 127.0.0.1

# 4. 建表 + 入库
alembic upgrade head
python scripts/init_legal_postgres.py
python scripts/init_legal_neo4j.py
python scripts/init_milvus_indexes.py

# 5. 启动
uvicorn src.main:app --port 8001 --reload
python scripts/gradio_chat_demo.py     # 调试台 http://localhost:7862
```

测试：`pytest test/ -v`（单元测试）；根目录 `_test_*.py` 为历史场景脚本（状态追踪、综合场景、端到端、P0 回归）。

## 十一、已知问题

代码核对中发现的待修项，按影响排序：

1. **`config.get_llm()` 返回字符串** `"deepseek-v4-flash"` 而非模型对象，调用方需自行构造
2. **Supervisor 硬编码模型名**，未读 `settings`，改配置不生效
3. **`VL_MODEL` 在 `config.py` 定义两次**（`qwen-vl-max` / `qwen-vl`），pydantic 取后者生效
4. **Embedding provider 三处不一致**：代码默认 ollama、`.env` 实为 volcengine、`supervisor_agent.py` 注释写 DashScope。volcengine 分支 `dimensions=1024` 硬编码且逐条串行请求，无批量
5. **`check_convergence` 是死代码**（import 未调用），`ask_details` 是死节点
6. **`state.py` 轮次注释过时**（写 5/5，实际 3/3）
7. **`SummarizationMiddleware` 注释与代码不符**（注释「4 条」，代码 6 条）
8. **`case_index` 数据量过小**，类案检索目前只能算演示功能，752 条数据包待导入
9. **无鉴权 / 无速率限制 / 上传无校验**
10. **图片落本地磁盘未走 MinIO**，多实例部署会丢文件
11. **`law_cases` 关联表为空**，法律-案例关联未建立
12. **`hallucination_check` 异常时放过**（返回 grounded=True），失败静默

## 十二、后续优先项

- [ ] 案例数据包导入（752 条民事案例 → PostgreSQL + Milvus），优先级高于统计包
- [ ] 三库数据一致性对齐（PostgreSQL / Milvus / Neo4j 的 case 数量差异）
- [ ] nl2sql 统计数据包导入 + 接入 Agent
- [ ] 接口鉴权、速率限制、上传安全
- [ ] 清理已知问题清单中的配置不一致与死代码
- [ ] CI/CD + 自动化测试
