# 法律多智能体平台项目说明书

> 最后更新：2026-07-30。本文以当前代码、九节点图结构和纯文本验收结果为准；不再保留早期 13/14 节点、固定档位分流和旧端口陈述。

## 1. 项目定位

本项目是一个面向普通用户的法律咨询与维权行动指引系统。用户无需掌握法律术语，可以直接描述劳动、消费、合同、住房、治安、人身损害、婚姻家庭、行政救济、知识产权等问题。

系统的目标不是替用户作出确定的司法结论，而是完成以下工作：

1. 理解用户已经说明的案件事实，避免重复盘问。
2. 判断是否存在当前人身危险或紧迫时效风险。
3. 将口语描述映射为适合检索的法律问题。
4. 从权威法律数据、类案和渠道库中召回材料。
5. 只追问会实质改变法律判断的一个关键点。
6. 形成可执行的维权路径、风险提示和行动清单。
7. 按需生成可编辑参考文书并返回官方空白模板。

项目适合作为法律 RAG、多智能体路由、LangGraph 状态机、结构化记忆和文书生成的综合工程案例，但不应包装为可以替代律师或有权机关的自动裁判系统。

## 2. 当前能力范围

### 2.1 公民法律指引

适用于用户描述具体遭遇，例如欠薪、押金不退、预付卡停业、食品消费问题、借款未还、被人伤害等。

当前流程具备：

- 跨轮事实合并和更正。
- 动态事实与证据追问。
- 追问原因和权威来源展示。
- 当前安全状态检测。
- 法条、类案、图谱和渠道检索。
- 定性风险评估与行动清单。
- 用户随时收敛和后续文书生成。

### 2.2 法律知识问答

适用于“列举交通法规”“某条法律是什么意思”等不需要建立个案状态的问题。Supervisor 将其路由到法律知识 Worker，通过法条、类案、法律图谱、渠道和文档检索工具回答。

### 2.3 法律统计与可视化

法律统计问题通过独立 NL2SQL 流程处理。后端返回统计数据、SQL 元信息和图表描述，Gradio 前端使用 Plotly 展示折线图、柱状图或表格。

### 2.4 参考文书双交付

用户在方案后回复“生成文书”时：

- 系统依据已确认的事实和证据生成可编辑 DOCX。
- 缺失字段使用明确占位符，不虚构姓名、日期、金额或主体。
- 如果模板注册表存在匹配或相关的官方文本，同时返回官方空白 PDF。
- 前端分别展示“智能填写参考稿”和“官方空白模板”，并显示发布机关、文号、官方发布页和适用说明。

文书生成由 Guide 结束后的独立服务处理，不占用 LangGraph 业务节点。

## 3. 总体架构

```text
Browser / API Client
        |
        v
FastAPI /api/v1/chat
        |
        +-- 已有 GuideState --> 直接进入法律指引图
        |
        +-- 无 GuideState ----> Supervisor
                                  |
                                  +-- 具体纠纷 --> Guide Worker
                                  +-- 法律知识 --> Legal QA Worker
                                  +-- 法律统计 --> Statistics Worker
                                  +-- 其他意图 --> 对应工具或边界回复
```

双层路由的原因：

- 首轮需要 Supervisor 判断用户是在问知识、描述纠纷还是查询统计。
- 一旦进入具体纠纷，多轮消息必须稳定进入同一个状态机，避免 Supervisor 每轮重新分类或改写专项回复。
- Guide 原始回复通过 Redis 临时键透传到 API，减少 Supervisor 摘要造成的信息损失。

## 4. 九节点法律指引工作流

当前 `src/agents/legal_guide/graph.py` 注册且仅注册九个业务节点：

| 节点 | 职责 | 主要输出 |
|---|---|---|
| `prepare_turn` | 加载上下文、推进轮次、识别用户收敛意图 | round、wants_conclude |
| `check_urgency` | 每轮判断当前危险、时效和安全状态 | urgency、safety status |
| `extract_issues` | 提取法律问题和原子事实并标准化领域 | issues、case_facts |
| `clarify` | 案情完全无法归类时问一个基础问题 | clarification |
| `assess_retrieve` | 评分、知识检索和下一追问规划 | retrieval、followup_plan |
| `ask_followup` | 展示唯一问题、回答提示和追问依据 | pending question |
| `parse_details` | 解析回答并更新事实、证据、冲突状态 | fact/evidence updates |
| `conclude` | 生成、审校和净化行动方案 | final plan |
| `save_record` | 保存咨询结果 | persisted record |

主路径：

```text
prepare_turn -> check_urgency -> extract_issues
                                |-> clarify -> END
                                `-> assess_retrieve
                                    |-> ask_followup -> END
                                    `-> conclude -> save_record -> END
```

追问回答路径：

```text
prepare_turn -> check_urgency -> parse_details
                                |-> extract_issues
                                `-> assess_retrieve
```

节点数量由 `test_compiled_graph_has_exactly_nine_business_nodes` 固定验证。辅助函数可以重构，但业务节点数量不得在无设计评审的情况下增加。

## 5. 动态追问机制

### 5.1 设计目标

追问不是固定问卷。每轮规划器比较候选问题的信息增益和用户负担，只在答案会影响下列判断之一时继续：

- 责任主体。
- 请求类型或范围。
- 时效。
- 管辖。
- 程序路径。
- 关键证据缺口。
- 当前安全措施。

### 5.2 候选问题来源

`data/legal_guide/followup_catalog.json` 保存领域级事实和证据维度。每条规则包含：

- 稳定 ID。
- 决策槽位。
- 示例问题。
- 提问目的。
- 低负担回答提示。
- 优先级和匹配关键词。
- 权威来源元数据。

题库只提供决策维度，不直接把固定问题展示给用户。`followup_planner.py` 会把当前 `case_facts`、已知材料、已问决策键和本轮法条交给规划器，生成上下文化问题。

### 5.3 已知与缺失覆盖

每个候选维度会生成：

```json
{
  "known": ["用户已经说清的内容"],
  "missing": ["仍可能影响方案的维度"]
}
```

当 `missing` 为空时，该候选会被过滤。问题必须询问缺失内容，不能换一种说法重复询问已知事实。

### 5.4 收敛规则

- 一轮只问一个中心问题。
- 用户可随时回复“现在生成方案”。
- 连续低信息回答、反问或达到硬上限时自动收敛。
- 达到体验软上限后，提高信息增益门槛，不展示“继续补充/生成方案”二次菜单。
- 规划器没有高价值候选或输出未通过策略校验时，直接进入结论。
- JSON 中可选字段为 `null` 时进行结构化容错，不能因为一个可选字段导致整份计划丢失。

## 6. 安全状态机

`check_urgency` 每轮执行，并结合最近多轮消息判断当前状态：

| 状态 | 含义 | 行为 |
|---|---|---|
| `danger` | 危险正在发生或明确即将发生 | 立即返回 110/12348 等安全提示并结束当前轮 |
| `safe` | 用户明确已经安全或离开现场 | 继续正常案情追问 |
| `unknown` | 涉及人身安全，但当前状态未说明 | 下一问题固定为“您现在是否安全？” |
| `not_applicable` | 当前问题不涉及人身安全 | 不询问安全问题 |

“发生过伤害”不等于“当前有危险”，也不等于“当前安全”。安全是独立状态，不能从行为事实中猜测。

## 7. 结构化事实与证据

### 7.1 原子事实

`case_facts` 中每条记录包含：

- `key`：跨轮稳定语义键。
- `category`：actor、event、amount、time、location、claim、procedure 等。
- `statement`：不包含法律结论的规范陈述。
- `source_text`：当前用户消息中的逐字原文。
- `turn`：来源轮次。
- `status`：asserted、uncertain、denied、conflicted、superseded。
- `operation`：add、replace、deny。

只有能在当前用户原文中找到锚点的更新才会写入状态。模型总结不能单独作为事实来源。

### 7.2 更正与冲突

- 相同语义键、相同内容：更新来源，不重复添加。
- 相同语义键、明确更正：旧值标为 superseded，新值生效。
- 相同语义键、不同值但没有更正语义：双方标为 conflicted，要求用户确认。
- 纯流程指令，例如“现在生成方案”，只改变路由，不进入事实池或文书。

### 7.3 证据状态

`evidence_confirmed` 只表示用户称持有或已经上传，不代表法律真实性和证明力已经确认。

`evidence_assessments` 分层记录：

- availability：是否存在、是否上传、是否冲突。
- authenticity：真实性是否核验。
- relevance：与争议是否可能相关。
- legal_admissibility：能否被有权机关采纳。
- purpose：可能证明的事项。
- limitations：单项材料不能证明的内容。

只有用户明确使用“没有、未保存、找不到”等否定表达时，材料才进入 `evidence_unavailable`。

## 8. 短期与长期记忆

### 8.1 短期状态

Redis 保存：

- `guide_state:{user_id}:{session_id}`：完整 GuideState JSON。
- `guide_active:{user_id}:{session_id}`：活跃指引标记。
- Supervisor Checkpointer：通用对话历史。
- 文书文件与元数据：按 TTL 临时保存。

法律指引状态默认保留 24 小时，便于刷新页面或服务重启后继续生成文书。

### 8.2 长期记忆

Supervisor 使用向量 Store 保存可复用用户偏好或背景。历史案件不会自动进入当前案件推理；只有用户明确使用“之前、上次、我以前说过”等回忆表达时，相关长期记忆才会被读取，并提示以本轮说明为准。

## 9. 知识库与检索

### 9.1 PostgreSQL

存储法律、法条、案例、渠道、用户、咨询记录、权威来源与统计数据。结构化查询和 Milvus 元数据补全以 PostgreSQL 为准。

### 9.2 Milvus

主要集合：

- `statute_index`：法条 Dense + BM25。
- `case_index`：案例 Dense + BM25。
- `legal_term_index`：口语到法律概念的语义映射。
- `knowledge_docs`：法律知识文档。
- `agent_long_term_memory`：长期记忆。

文档不维护易过期的静态条数，实际规模以运行环境查询为准。

### 9.3 Neo4j

存储法律、法条、领域、法律概念、案例和渠道之间的图关系。图谱用于标准化、关联检索和前端调试展示，不替代法条原文。

### 9.4 法条召回

法条检索组合：

1. 领域过滤召回。
2. 全库召回。
3. PostgreSQL `ILIKE` 兜底。
4. 应用层 RRF 融合。
5. 统一精排。
6. 记录结构化 `retrieved_law_refs` 和文本 `law_context_str`。

结论中的法律名称、条号和原文必须出现在 `law_context_str`。确定性净化器会删除未召回的具体条文，并从真实检索结果重建“法律依据”栏目。

### 9.5 类案召回

类案通过 Milvus 与 PostgreSQL 联合查询，包含领域候选、全库候选、质量过滤和精排。未找到适配案例时，系统只返回公开平台搜索指引，不编造案例。

## 10. 权威追问依据

追问依据不是模型临时编写的“官方标准答案”。系统保存的是：

- 来源机关。
- 文件或示范文本标题。
- 官方网址。
- 权威级别。
- 适用领域。
- 从来源中整理出的事实或材料维度。
- 版本、哈希和审查状态。

运行时：

- 领域追问题库使用 JSON 读取，便于版本管理和测试。
- 权威来源注册表可同步到 PostgreSQL，支持查询和审计。
- Milvus 用于召回正文或知识文档，不适合单独承担规则版本控制。
- Neo4j 用于来源、法律、领域和概念之间的关系，不替代 PostgreSQL 元数据。

因此权威依据采用“Git/文件版本 + PostgreSQL 元数据 + Milvus 正文召回 + Neo4j 关系”的分层方案，而不是只选一个数据库。

## 11. 方案生成与审校

`conclude` 节点执行：

1. 组装结构化事实、证据状态、长期记忆、法条、类案和渠道。
2. 按置信档位生成行动方案。
3. 使用独立审校提示检查事实来源、法条来源和程序承诺。
4. 删除未召回法条、未核验证据断言和批量补充问卷。
5. 从结构化事实重建“理解您的情况”。
6. 补全维权路径、风险评估和行动清单等必要栏目。

LOW/MEDIUM/HIGH 表示信息充分程度，不代表案件胜率。系统禁止输出百分比胜率、稳赢或必胜承诺。

## 12. 文书与官方模板

文书请求在 API 层识别。Guide 已结束且已有法律问题时，调用 `generate_legal_document`：

- 使用 `draftable_facts`，不使用冲突、推测或纯流程指令。
- 生成 DOCX 二进制并存入 Redis 临时文件键。
- 返回 `generated_docx_url`。
- 根据争议领域和程序阶段选择官方模板。
- 返回 `official_blank_url`、发布机关、文号、日期、官方页面和匹配类型。
- 当只有相关模板而非同一程序文书时，前端显示明确提示。

官方空白模板本身不被系统改写；智能填写稿始终标明为系统参考稿。

## 13. 前端能力

Gradio 调试与展示界面位于 `scripts/gradio_chat_demo.py`，默认：

- 后端：http://127.0.0.1:8085
- 前端：http://127.0.0.1:7864

页面支持：

- 多轮法律对话。
- 领域、置信度、法条、类案和图谱调试信息。
- 法律统计图表。
- DOCX 与官方 PDF 下载。
- 官方模板库直接选择下载。
- 可选图片入口；默认关闭，不属于当前文本主流程验收。

## 14. API 契约

### 14.1 对话

`POST /api/v1/chat`

```json
{
  "user_id": "demo-user",
  "session_id": "demo-session",
  "message": "房东不退押金"
}
```

响应包含：

- `reply`：用户可见回复。
- `session_id`：会话 ID。
- `debug`：领域、置信度、法条、类案、图谱和渠道信息。
- `statistics`：可选统计产物。
- `document`：可选文书下载产物。

### 14.2 文书

- `GET /api/v1/chat/document-templates`
- `GET /api/v1/chat/document-templates/{template_id}/official`
- `GET /api/v1/chat/documents/{document_id}`

### 14.3 健康检查

- `GET /health`
- `GET /health/deps`
- `GET /api/v1/legal/health`

## 15. 配置与运行

### 15.1 基础设施端口

| 服务 | 宿主机端口 |
|---|---:|
| PostgreSQL | 5433 |
| Redis | 6380 |
| Milvus | 19531 |
| Neo4j HTTP / Bolt | 7475 / 7688 |
| MinIO API / Console | 9010 / 9011 |

### 15.2 应用端口

| 服务 | 默认端口 |
|---|---:|
| FastAPI | 8085 |
| Gradio | 7864 |

### 15.3 启动命令

```powershell
docker compose up -d
alembic upgrade head
D:\develop\Miniconda\envs\legal\python.exe -m uvicorn src.main:app --host 127.0.0.1 --port 8085
$env:LEGAL_AGENT_API_BASE='http://127.0.0.1:8085'
$env:LEGAL_AGENT_GRADIO_PORT='7864'
D:\develop\Miniconda\envs\legal\python.exe scripts/gradio_chat_demo.py
```

也可以运行 `start_dev.bat`。

## 16. 测试与验收

纯文本主测试：

```powershell
python -m pytest test --ignore=test/test_multimodal_pipeline.py -k "not multimodal" -q
```

2026-07-30 最近验收：

```text
209 passed
5 subtests passed
1 multimodal test deselected
```

重点覆盖：

- 九节点数量和状态路由。
- Supervisor 意图边界。
- 多轮安全检测。
- 动态上下文追问。
- 可选字段空值容错。
- 事实来源、冲突和流程指令隔离。
- 证据存在性与证明边界。
- 法条、类案、图谱、统计和渠道召回。
- 短期/长期记忆。
- DOCX 和官方模板下载。
- 前后端 API 契约。

## 17. 已知边界与后续优先级

### 当前边界

- 开发接口未实现生产级鉴权、限流、审计授权和租户隔离。
- 模型与多库检索延迟较高，普通追问常见 18-30 秒，完整方案约 48 秒。
- 外部模型、Milvus、Neo4j 或 PostgreSQL 不可用时会降级，回答完整度可能下降。
- 证据有效性只能给出结构化初评，最终由有权机关结合原件、形成过程和其他材料判断。
- 图片分析为可选实验入口，本轮未纳入验收结论。

### 优先级

1. 降低首轮 Supervisor、标准化、检索和动态规划的串行延迟。
2. 增加 API 鉴权、限流、文件安全和隐私审计。
3. 建立可重复的线上评估集和 CI。
4. 完善权威来源版本同步、失效检查和管理页面。
5. 扩充高质量类案并持续评估领域边界。

## 18. 仓库维护规则

- 根目录只保留当前入口文档和运行脚本。
- 历史测试日志、临时输出和一次性 `_test_*.py` 不进入 Git。
- `.codex-runtime`、`.pytest-tmp-*`、`uploads`、`logs` 和 `.env` 必须忽略。
- 文档中的节点、端口和测试数据必须与代码一致。
- 不在 README 中维护容易过时的数据库静态条数。
- 新场景问题优先修复本体、状态、来源和策略边界，禁止无限增加场景关键词补丁。
