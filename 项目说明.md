# 法律多智能体平台项目说明书

> 文档版本：V2.0
>
> 更新日期：2026-07-30
>
> 适用分支：`feature/multimodal`
>
> 项目性质：面向普通用户的法律咨询、维权指引、法律知识检索、法律统计分析与参考文书生成平台
>
> 说明：本文以当前仓库代码、配置、数据文件及测试用例为准。系统输出仅供法律信息与办事路径参考，不构成律师代理意见、司法裁判或行政决定。

---

## 1. 项目概述

### 1.1 建设背景

普通用户在遇到欠薪、消费纠纷、租房押金、合同违约、交通事故、人身伤害、婚姻家庭、行政救济和知识产权等问题时，通常面临四类困难：

1. 不知道自己的口语描述对应什么法律问题；
2. 不清楚哪些事实和证据真正影响处理路径；
3. 难以从大量法条、案例和办事信息中找到可用内容；
4. 即使知道可能适用的法律，也不清楚下一步去哪里、准备什么、如何形成材料。

本项目把多智能体路由、LangGraph 状态机、法律 RAG、结构化事实记忆、法律统计 NL2SQL、图片证据理解和 DOCX 文书生成组合成一套可运行的法律服务原型。系统先判断用户是在描述具体纠纷、询问法律知识，还是查询法律统计；随后调用相应的专项流程，最终给出来源化、分层次、可执行的回复。

### 1.2 建设目标

项目重点解决以下问题：

- 让用户可以直接使用自然语言描述纠纷，不要求预先掌握法律术语；
- 在多轮对话中持续保存事实、证据、冲突、更正和待确认事项；
- 每轮只追问一个会实质影响责任、时效、管辖、程序或证据判断的关键问题；
- 联合法条、类案、知识图谱、权威来源和办事渠道生成行动方案；
- 对具体法条、证据效力和案件结果保持审慎，避免虚构和过度承诺；
- 支持法律统计自然语言查询及图表展示；
- 在事实已经收敛后生成可编辑参考文书，并提供对应的官方空白模板；
- 为后续鉴权、审计、评估、知识更新和生产部署保留清晰的工程边界。

### 1.3 非目标与责任边界

系统不承担以下职责：

- 不替代执业律师的正式法律意见和代理服务；
- 不替代法院、仲裁机构、行政机关或公安机关作出认定；
- 不承诺胜诉、立案、受理、赔偿金额或办理时限；
- 不把用户上传或口述的材料直接认定为真实、合法、有效或可采；
- 不把相似案例机械等同于当前案件；
- 不在没有检索依据时编造法条、案例、机关或办理渠道。

涉及正在发生的人身危险、暴力、诈骗控制、重大财产转移或紧迫期限时，系统优先给出安全和时效提示，用户应及时联系有权机关或专业人员。

---

## 2. 用户对象与典型场景

### 2.1 目标用户

- 遇到具体纠纷、需要初步梳理和行动指引的普通公民；
- 需要查询法条、制度、流程、案例或办事渠道的用户；
- 需要从法律统计数据中查询数量、趋势、比例和年度变化的研究或业务人员；
- 需要生成投诉、仲裁、起诉等参考材料初稿的用户；
- 需要验证多智能体、法律 RAG、GraphRAG、NL2SQL 和结构化记忆方案的研发人员。

### 2.2 典型输入

- “公司拖欠我三个月工资，也没有签劳动合同，应该怎么办？”
- “房东以墙面有污渍为由不退押金。”
- “预付卡门店关门了，余额还能追回吗？”
- “发生交通事故后对方不配合理赔，需要准备什么？”
- “《民法典》关于格式条款是怎么规定的？”
- “查询某类案件近五年的数量变化并画图。”
- “根据刚才的信息生成一份起诉状。”
- 上传合同、聊天记录、订单、付款凭证或通知文书图片并请求提取关键信息。

### 2.3 当前法律领域

动态追问目录目前覆盖 13 个领域：

1. 劳动与社会保障；
2. 消费与市场监管；
3. 合同、财产与住房；
4. 刑事与治安；
5. 婚姻家庭与弱势群体保护；
6. 交通事故与人身损害；
7. 医疗、教育与税务；
8. 行政救济；
9. 知识产权；
10. 环境污染；
11. 网络、数据与诈骗；
12. 调解、公证与仲裁；
13. 其他或尚未稳定归类的问题。

领域标签是检索和追问的工程分类，不等于最终案由或有权机关的法律定性。

---

## 3. 核心功能

### 3.1 具体纠纷与维权指引

具体事件由 Guide Worker 和九节点 GuideGraph 处理，主要能力包括：

- 从口语中提取法律问题、时间、金额、主体、行为、请求和程序状态；
- 将口语问题标准化为法律检索术语；
- 合并跨轮事实并处理更正、否认和冲突；
- 区分已确认材料、缺失材料和未经核验材料；
- 每轮检查当前危险与紧迫期限；
- 动态决定继续追问还是按现有信息生成方案；
- 检索法条、类案、图谱关系和办事渠道；
- 输出案情理解、风险说明、法律依据、证据建议、办理路径和行动清单；
- 在终态继续支持生成或重新生成参考文书。

### 3.2 法律知识问答

法条含义、法律概念、制度流程、案例检索、办事渠道和专业知识文档等问题由 Legal QA Worker 处理。该 Worker 可以调用：

- 法条检索；
- 类案检索；
- 法律知识图谱查询；
- 办事渠道查询；
- 知识文档 RAG；
- 法律统计 ChatBI/NL2SQL。

专项 Worker 的原始回复通过 Redis 临时键回传给 API，避免 Supervisor 对来源化回复再次摘要或改写。

### 3.3 法律统计与 ChatBI

法律统计使用独立数据库和只读查询链路。流程为：

1. 识别统计意图和指标；
2. 根据数据库 Schema 生成 SQL；
3. 校验表名、字段、只读边界和查询规模；
4. 执行查询并整理结构化数据；
5. 返回文字结论、SQL 元信息和图表建议；
6. Gradio 根据结果展示折线图、柱状图或表格；
7. 同一统计会话的追问复用上一条 SQL 上下文，避免重新误路由。

统计查询使用独立的 `legal_statistics_db` 和只读用户，业务库与统计库逻辑隔离。

### 3.4 多模态图片证据理解

启用 `ENABLE_MULTIMODAL=true` 且配置视觉模型密钥后，可上传图片进行分析。当前实现支持：

- 校验真实图片格式、文件大小和像素数量；
- 计算原图 SHA-256 指纹；
- 根据当前法律领域、争议焦点、已知证据和最近追问生成上下文相关分析问题；
- 提取图片可见文字、主体、时间、金额和可能证明事项；
- 明确标记视觉模型结果需要与原图核对；
- 防止图片中的命令性文字被当作系统指令；
- 有活跃 Guide 会话时，将分析结果作为证据补充注入；
- 没有案情上下文时只返回识别结果，并提示用户先描述纠纷；
- 默认分析后删除服务端临时图片。

图片识别结果属于“未经人工核验的证据线索”，不等于真实性、完整性和证明力已经确认。

### 3.5 参考文书与官方模板

Guide 已结束且已识别具体法律问题时，用户可以提出“生成文书”“生成起诉状”等请求。系统会：

- 从 `draftable_facts` 中提取可安全写入文书的事实；
- 排除冲突、否认、推测、模型补全和纯流程指令；
- 对缺失的姓名、地址、金额、日期、主体信息使用明确占位符；
- 生成可编辑 DOCX 智能填写参考稿；
- 将文件按随机文书 ID 暂存 Redis，并设置 24 小时 TTL；
- 根据领域、关键词和程序阶段选择官方空白 PDF；
- 返回发布机关、文号、发布日期、官方页面、文件哈希和匹配说明。

当前注册了 8 类最高人民法院等单位发布的官方示范文本：

- 离婚纠纷民事起诉状；
- 买卖合同纠纷民事起诉状；
- 房屋买卖合同纠纷民事起诉状；
- 民间借贷纠纷民事起诉状；
- 房屋租赁合同纠纷民事起诉状；
- 劳动争议纠纷民事起诉状；
- 机动车交通事故责任纠纷民事起诉状；
- 侵害著作权及邻接权纠纷民事起诉状。

智能填写稿与官方空白模板是两个独立产物。若仅存在相关领域模板但程序阶段不同，前端必须显示“相关模板”提示，不能将其宣称为当前案件的完全匹配文书。

---

## 4. 总体架构

```text
用户 / Gradio / API Client
          |
          v
FastAPI  /api/v1/chat
          |
          +-- 已存在 GuideState ----------------------+
          |                                           |
          |                                           v
          |                                  九节点 GuideGraph
          |
          +-- 统计会话追问 --> Statistics ChatBI / NL2SQL
          |
          `-- 新会话 --> Supervisor Agent
                           |
                           +-- 具体纠纷 --> Guide Worker
                           +-- 法律知识 --> Legal QA Worker
                           +-- 专业/考试/运营等保留能力 --> 对应 Worker
                           `-- 长期记忆 --> Milvus Store

GuideGraph / Legal QA
    |
    +-- PostgreSQL：结构化法律、案例、渠道、用户、咨询、权威来源
    +-- Milvus：法条、类案、术语、文档、权威依据、长期记忆向量索引
    +-- Neo4j：法律、法条、概念、领域、案例、渠道关系
    +-- Redis：会话状态、检查点、临时回复、统计上下文、文书文件
    `-- MinIO：知识文档对象存储
```

### 4.1 双层路由设计

首轮由 Supervisor 判断用户意图；一旦进入具体纠纷，后续消息优先根据 Redis 中的 `GuideState` 直接进入 GuideGraph，不再让 Supervisor 每轮重新分类。这样可以：

- 保持案件事实、证据和追问连续；
- 避免多轮中途切换 Worker；
- 防止 Supervisor 改写专项输出；
- 使终态文书请求仍可使用已确认事实和检索结果；
- 将法律统计追问固定在同一 SQL 上下文中。

### 4.2 请求生命周期

非流式对话的主要生命周期如下：

1. API 接收 `user_id`、`session_id` 和 `message`；
2. Redis 检查是否存在 Guide 活跃标记或结构化状态；
3. 有 Guide 状态时恢复 `GuideState`，执行一轮图流程并重新保存；
4. 没有 Guide 状态时，先检查是否为统计追问；
5. 普通新会话进入 Supervisor；
6. Supervisor 调用专项 Worker；
7. API 优先读取专项 Worker 写入的原始回复与调试信息；
8. 返回 `reply`、`debug`、`statistics` 或 `document`；
9. 临时回复读取后删除，持久状态按 TTL 保留。

---

## 5. 多智能体角色

### 5.1 Supervisor Agent

Supervisor 是统一意图入口，负责：

- 判断具体纠纷、法律知识、统计及其他意图；
- 调用对应 Worker；
- 管理通用对话 Checkpointer；
- 按用户 ID 检索或保存长期偏好和背景；
- 对专项 Worker 输出执行原样透传。

Supervisor 不负责具体纠纷的多轮事实状态，不应自行重写 Guide 或 Legal QA 的最终答复。

### 5.2 Guide Worker

Guide Worker 负责创建依赖、启动 GuideGraph，并将原始回复和调试信息写入 Redis。API 在后续轮次直接调用 GuideGraph，以结构化状态作为当前案件的事实来源。

### 5.3 Legal QA Worker

Legal QA Worker 面向知识型问题，使用法条、案例、图谱、渠道、文档和统计工具。它适合一次性或知识上下文型问答，不负责具体案件的完整状态机。

### 5.4 其他保留 Worker

代码中还保留 Professional、Exam 和 Operation Worker 的入口，用于专业服务、考试和运营等扩展意图。当前项目核心验收集中在 Guide、Legal QA 和 Statistics 流程。

---

## 6. 九节点法律指引状态机

`src/agents/legal_guide/graph.py` 当前注册且仅注册 9 个业务节点：

| 节点 | 核心职责 | 主要状态输出 |
|---|---|---|
| `prepare_turn` | 加载本轮消息、推进轮次、识别收敛和文书意图 | `round`、`wants_conclude` |
| `check_urgency` | 每轮检测当前危险、时效和安全状态 | `urgency_level`、`current_safety_status` |
| `extract_issues` | 提取原子事实、法律问题并执行术语标准化 | `case_facts`、`confirmed_issues`、`legal_domain` |
| `clarify` | 案情无法归类时提出一个基础澄清问题 | 澄清回复 |
| `assess_retrieve` | 计算充分度、并行检索、规划下一追问 | 检索结果、`confidence_tier`、`followup_plan` |
| `ask_followup` | 展示唯一关键问题、回答提示和权威依据 | 待回答问题 |
| `parse_details` | 解析用户答复，更新事实、证据、更正和冲突 | 事实与证据状态 |
| `conclude` | 生成、审校并确定性净化行动方案 | 最终方案 |
| `save_record` | 保存咨询记录 | 持久化结果 |

主路径：

```text
prepare_turn -> check_urgency -> extract_issues
                                |-> clarify -> END
                                `-> assess_retrieve
                                    |-> ask_followup -> END
                                    `-> conclude -> save_record -> END
```

追问回答路径：

```text
prepare_turn -> check_urgency -> parse_details
                                |-> extract_issues
                                `-> assess_retrieve
```

九节点数量由测试固定约束。文书生成位于 Guide 终态后的 API 服务层，不占用业务节点。

---

## 7. 动态追问与收敛策略

### 7.1 追问原则

追问不是固定问卷。规划器只保留可能改变下列判断的问题：

- 责任主体；
- 请求类型和范围；
- 诉讼时效、仲裁时效或办理期限；
- 管辖和受理机关；
- 调解、投诉、仲裁、复议或诉讼路径；
- 关键证据缺口；
- 当前人身安全措施。

每轮只问一个中心问题，并提供低负担回答提示。

### 7.2 追问来源

`data/legal_guide/followup_catalog.json` 保存领域级事实和证据维度。规则包含稳定 ID、决策键、问题模板、提问目的、优先级、关键词和权威来源元数据。

规划器结合以下内容生成上下文化问题：

- 已确认和未确认事实；
- 已有与缺失证据；
- 已问过的决策键；
- 当前检索到的法条；
- 用户地区、程序阶段和风险状态；
- 用户连续低信息答复或反问情况。

### 7.3 防重复与信息增益

每个候选维度都会形成 `known` 和 `missing`。`missing` 为空时不再追问；同一稳定规则 ID 或决策键已经问过时也会过滤。系统不能把用户已经清楚说明的事实换一种说法重复询问。

### 7.4 收敛条件

- 用户明确回复“现在生成方案”“不要再问”等；
- 没有通过策略校验的高价值候选；
- 达到体验软上限后，新问题的信息增益不足；
- 连续低信息回答达到上限；
- 连续反问达到上限；
- 达到事实、证据、追问或总轮次硬上限；
- 检索已完成且现有信息足以形成分层方案。

默认配置：

| 配置项 | 默认值 | 含义 |
|---|---:|---|
| `GUIDE_MAX_CLARIFY_ROUNDS` | 2 | 最多澄清轮数 |
| `GUIDE_MAX_ASK_ROUNDS` | 6 | 事实与证据追问总上限 |
| `GUIDE_SOFT_ASK_ROUNDS` | 3 | 用户体验软上限 |
| `GUIDE_MAX_FACT_ROUNDS` | 3 | 关键事实追问上限 |
| `GUIDE_MAX_EVIDENCE_ROUNDS` | 3 | 证据追问上限 |
| `GUIDE_MAX_LOW_INFO_ANSWERS` | 2 | 连续低信息回答上限 |
| `GUIDE_MAX_COUNTER_QUESTIONS` | 3 | 连续反问上限 |
| `GUIDE_MAX_TOTAL_ROUNDS` | 12 | 用户消息总轮次硬上限 |

---

## 8. 安全与紧迫性机制

`check_urgency` 每轮执行，而不是只在首轮执行。

| 安全状态 | 含义 | 系统行为 |
|---|---|---|
| `danger` | 危险正在发生或明确即将发生 | 优先返回报警、求助和离开危险环境提示 |
| `safe` | 用户明确表示已经安全或已离开现场 | 继续正常事实与证据流程 |
| `unknown` | 涉及安全风险，但当前状态没有说明 | 下一问题优先确认“您现在是否安全” |
| `not_applicable` | 当前问题不涉及人身安全 | 不额外询问安全状态 |

系统区分“过去发生过伤害”“当前仍有危险”和“用户明确已经安全”。历史伤害本身不能自动推断当前危险，也不能自动推断当前安全。

紧迫性还包括可能影响权利的时效、仲裁期限、复议期限、诉讼期限、证据灭失和财产转移风险。系统只能提示核实，不应在事实不足时给出确定期限结论。

---

## 9. 结构化事实、证据与冲突管理

### 9.1 原子事实

`case_facts` 中的事实记录包含：

- `key`：跨轮稳定的语义键；
- `category`：主体、行为、金额、时间、地点、请求、程序等；
- `statement`：不含法律结论的规范陈述；
- `source_text`：当前用户消息中的原始锚点；
- `turn`：来源轮次；
- `status`：`asserted`、`uncertain`、`denied`、`conflicted` 或 `superseded`；
- `operation`：`add`、`replace` 或 `deny`。

只有可以在当前用户原文中找到锚点的事实更新才允许写入。模型的归纳、检索内容和历史猜测不能单独作为事实来源。

### 9.2 更正与冲突

- 相同语义键、相同内容：更新来源，不重复添加；
- 用户明确更正：旧值标记为 `superseded`，新值成为有效事实；
- 同一语义键出现不同值且无明确更正：双方标记为 `conflicted`；
- 用户明确否认：写入否认状态，不再作为肯定事实；
- “现在生成方案”“继续”等流程指令不进入事实池或文书。

### 9.3 可写入文书的事实

`draftable_facts` 仅包含用户清楚陈述、无冲突且可追溯到原文的事实。以下内容不得进入：

- 用户没有确认的模型推测；
- 检索到的其他案件事实；
- 冲突或被替代事实；
- 单纯疑问和假设；
- 视觉模型识别但尚未由用户核对的内容；
- 系统流程指令。

### 9.4 证据分层

`evidence_confirmed` 只表示用户称持有、已经上传或明确提到，不代表证据已经核验。

`evidence_assessments` 分层记录：

- `availability`：是否存在、是否上传、是否冲突；
- `authenticity`：真实性是否核验；
- `relevance`：与争议是否可能相关；
- `legal_admissibility`：是否可能被有权机关采纳；
- `purpose`：可能证明的事项；
- `limitations`：不能单独证明的事项。

只有用户明确表示“没有、没保存、找不到”等，材料才进入 `evidence_unavailable`。图片或转述中出现但未直接核验的材料进入 `evidence_unverified`。

---

## 10. 知识库与检索体系

### 10.1 PostgreSQL

核心表包括：

- `laws`：法律法规元数据；
- `articles`：法条正文；
- `legal_cases`：案例；
- `law_cases`：法律与案例关联；
- `channels`：投诉、仲裁、诉讼和公共法律服务渠道；
- `authority_sources`：权威来源注册；
- `authority_versions`：权威来源版本；
- `followup_rule_citations`：追问规则与来源关联；
- `users`：用户；
- `consultations`：咨询记录。

统计数据使用独立数据库及专用只读用户。

### 10.2 Milvus

当前代码使用的主要集合：

| 集合 | 用途 |
|---|---|
| `statute_index` | 法条 Dense 与 BM25 混合检索 |
| `case_index` | 类案语义与稀疏检索 |
| `legal_term_index` | 口语到法律术语的语义映射 |
| `knowledge_docs` | 法律知识文档 RAG |
| `authority_basis_index` | 权威依据召回 |
| `agent_long_term_memory` | 用户长期记忆 |

### 10.3 Neo4j

Neo4j 保存法律、法条、领域、法律概念、案例和渠道之间的关系，用于：

- 术语和领域关联；
- 相关法律与法条发现；
- 类案和渠道关系补充；
- 调试面板的图谱结果展示。

图谱关系是检索辅助，不替代法律原文和结构化来源。

### 10.4 Redis

Redis 承担：

- `guide_state:{user_id}:{session_id}`：完整 GuideState；
- `guide_active:{user_id}:{session_id}`：Guide 活跃标记；
- Supervisor 的 LangGraph Checkpointer；
- 专项 Worker 原始回复与调试信息；
- 统计查询上下文与本轮图表产物；
- 短期 DOCX 文件和元数据；
- 当前用户消息等临时协调数据。

Guide 状态和文书默认保留 24 小时；普通 Redis 会话默认 TTL 为 1 小时。

### 10.5 MinIO

MinIO 用作知识文档对象存储。应用启动时尝试确保目标 Bucket 存在；MinIO 不可用时，其他可独立运行的功能仍可继续，但文件和文档能力会受限。

---

## 11. RAG 与来源控制

### 11.1 法条检索

法条检索组合以下通道：

1. 按法律领域过滤召回；
2. 全库 Dense 召回；
3. BM25 稀疏召回；
4. PostgreSQL `ILIKE` 字面检索兜底；
5. RRF 融合；
6. 统一精排；
7. PostgreSQL 补充法律名称、条号和正文。

系统同时保存结构化 `retrieved_law_refs` 和文本化 `law_context_str`。最终答复中的具体法律名称、条号和法条内容必须能在检索上下文中找到。确定性净化器会删除未召回的具体法条，并从真实结果重建“法律依据”部分。

### 11.2 类案检索

类案通过 Milvus 与 PostgreSQL 联合查询，执行领域候选、全库候选、质量过滤、去重和精排。相似性只代表参考价值，不能直接推导当前案件结果。

若没有找到适配案例，系统返回公开平台和检索词建议，不编造案例名称、案号或裁判结果。

### 11.3 法律术语标准化

标准化采用多层策略：

- 规则和领域词典；
- 法律术语映射；
- `legal_term_index` 语义兜底。

`confirmed_issues` 仅保存可以进入字面检索通道的标准术语；无法稳定标准化的口语保存在 `unmatched_issues`，只进入 Dense 或 HyDE 通道，防止无效口语污染 BM25 和数据库字面查询。

### 11.4 权威追问依据

追问依据使用“文件版本 + PostgreSQL 元数据 + Milvus 正文召回 + Neo4j 关系”的分层方案。每个来源可记录：

- 发布机关；
- 文件或示范文本标题；
- 官方网址；
- 权威级别；
- 适用领域；
- 版本、哈希和审查状态；
- 可支持的事实或材料维度。

系统引用来源是为了说明为什么该问题值得确认，不把来源整理出的维度包装成对当前案件的事实认定。

---

## 12. 方案生成、置信度与审校

### 12.1 方案生成

`conclude` 节点组装：

- 当前有效事实；
- 证据存在性和核验边界；
- 法条和类案检索结果；
- 图谱和渠道结果；
- 地区与程序信息；
- 不利事实和风险；
- 用户明确要求。

输出通常包含：

- 对用户情况的理解；
- 当前可能涉及的法律关系；
- 法律依据及适用边界；
- 有利与不利因素；
- 建议补充的证据；
- 分步骤维权路径；
- 时效、管辖和程序提醒；
- 可立即执行的行动清单；
- 必要的免责声明。

### 12.2 置信度

`LOW`、`MEDIUM`、`HIGH` 表示当前信息和检索材料的充分程度，不表示胜率。默认阈值：

- `HIGH`：不低于 0.65；
- `MEDIUM`：不低于 0.50；
- 其余为 `LOW`。

系统禁止把置信度转换为百分比胜诉概率，也禁止使用“稳赢”“必胜”“一定受理”等承诺性表述。

### 12.3 独立审校与确定性净化

初稿生成后执行审校，重点检查：

- 事实是否来自用户；
- 是否误用冲突、否认或被替代事实；
- 法条是否真实召回；
- 是否把证据存在误写为证据有效；
- 是否承诺机关决定或案件结果；
- 是否一次性补充大量问卷；
- 是否缺少必要路径和风险栏目。

最后再使用确定性规则删除未召回法条、未经核验的证据断言和不当承诺，并用结构化事实重建案情摘要。

---

## 13. API 说明

### 13.1 普通对话

`POST /api/v1/chat`

请求：

```json
{
  "user_id": "demo-user",
  "session_id": "demo-session",
  "message": "房东不退押金，我应该怎么办？"
}
```

响应：

```json
{
  "reply": "用户可见回复",
  "session_id": "demo-session",
  "debug": {
    "domain": "contracts_property_housing",
    "confidence_tier": "GATHERING",
    "statute_hits": "",
    "case_hits": "",
    "graph_laws": [],
    "graph_channels": [],
    "fallback_guide": null
  },
  "statistics": null,
  "document": null
}
```

### 13.2 SSE 流式对话

`POST /api/v1/chat/stream`

事件类型：

```text
data: {"type":"token","content":"..."}
data: {"type":"done","session_id":"...","debug":{},"document":null}
data: {"type":"error","message":"..."}
```

GuideGraph 当前以整段回复推送；Supervisor 支持消息流。客户端应以 `done` 事件作为本轮完成标志。

### 13.3 图片上传

`POST /api/v1/chat/upload-image`

表单字段：

- `file`：图片文件；
- `user_id`：用户 ID；
- `session_id`：会话 ID；
- `question`：可选自定义分析问题；
- `auto_inject`：是否注入活跃 Guide 会话，默认 `true`。

未启用多模态时接口返回 `enabled: false`，不会静默调用视觉模型。

### 13.4 文书与模板

- `GET /api/v1/chat/document-templates`：列出官方模板；
- `GET /api/v1/chat/document-templates/{template_id}/official`：下载官方空白 PDF；
- `GET /api/v1/chat/documents/{document_id}`：下载短期 DOCX。

文书 ID 必须为 32 位十六进制字符串；过期或不存在时返回 404。

### 13.5 健康检查

- `GET /health`：应用存活；
- `GET /health/deps`：PostgreSQL、Redis、MinIO、Milvus、Neo4j 依赖检查；
- `GET /api/v1/legal/health`：法律模块健康检查；
- `GET /docs`：FastAPI OpenAPI 文档。

`/health/deps` 在任一依赖不可用时返回 `degraded`，便于定位基础设施问题。

---

## 14. 工程目录

```text
legal-agent/
├─ src/
│  ├─ main.py                         # FastAPI 应用、生命周期与健康检查
│  ├─ api/routers/                    # 对话、流式、图片、文书和法律路由
│  ├─ agents/
│  │  ├─ supervisor_agent.py          # 统一路由与长期记忆
│  │  ├─ workers/                     # Guide、Legal QA 等专项 Worker
│  │  ├─ tools/                       # Worker、记忆和多模态工具
│  │  ├─ legal_guide/                 # 九节点法律指引状态机
│  │  └─ legal_knowledge/             # 法条、类案、图谱、文档与 NL2SQL
│  ├─ infra/                          # PostgreSQL、Redis、Milvus、Neo4j、MinIO
│  ├─ modules/legal/                  # 法律业务数据模型
│  ├─ core/                           # 配置、日志、依赖和异常处理
│  └─ middlewares/                    # 请求日志中间件
├─ data/                              # 评估数据、追问目录、权威来源和原始资料
├─ database/                          # 案例库与法律统计数据库脚本
├─ resources/legal_document_templates/# 官方模板清单、源 PDF 与空白 PDF
├─ scripts/                           # 初始化、导入、校验、评估与 Gradio
├─ alembic/                           # PostgreSQL 迁移
├─ test/                              # 自动化测试
├─ nl2sql数据包/                      # 法律统计数据、Schema 和导入工具
├─ 案例数据包/                        # 案例试点数据与建设报告
├─ docker-compose.yml                 # 基础设施编排
├─ .env.example                       # 环境变量示例
├─ start_dev.bat                      # Windows 开发启动脚本
├─ README.md                          # 快速使用入口
└─ 项目说明.md                        # 本说明书
```

---

## 15. 环境与配置

### 15.1 推荐环境

- Windows 10/11 或可运行 Docker Compose 的 Linux；
- Python 3.11 兼容环境；
- Docker 与 Docker Compose；
- 可用的 OpenAI 兼容聊天模型服务；
- 可选的 DashScope 视觉模型；
- 具备运行 PostgreSQL、Redis、Milvus、Neo4j 和 MinIO 的内存与磁盘空间。

### 15.2 基础设施端口

| 服务 | 容器端口 | 默认宿主机端口 |
|---|---:|---:|
| PostgreSQL | 5432 | 5433 |
| Redis | 6379 | 6380 |
| Redis Insight | 8001 | 8002 |
| Milvus | 19530 | 19531 |
| Milvus Health | 9091 | 9092 |
| Neo4j HTTP | 7474 | 7475 |
| Neo4j Bolt | 7687 | 7688 |
| MinIO API | 9000 | 9010 |
| MinIO Console | 9001 | 9011 |

应用端口：

| 应用 | 默认端口 |
|---|---:|
| FastAPI | 8085 |
| Gradio | 7864 |

### 15.3 关键环境变量

| 类别 | 变量 |
|---|---|
| 应用 | `APP_NAME`、`APP_ENV`、`APP_DEBUG` |
| PostgreSQL | `DB_HOST`、`DB_PORT`、`DB_USER`、`DB_PASSWORD`、`DB_NAME` |
| 统计库 | `LEGAL_STATISTICS_DB_NAME`、`LEGAL_STATISTICS_DB_USER`、`LEGAL_STATISTICS_DB_PASSWORD` |
| Redis | `REDIS_HOST`、`REDIS_PORT`、`REDIS_PASSWORD`、`REDIS_DB` |
| Milvus | `MILVUS_HOST`、`MILVUS_PORT` |
| Neo4j | `NEO4J_URI`、`NEO4J_USER`、`NEO4J_PASSWORD` |
| MinIO | `MINIO_ENDPOINT`、`MINIO_ACCESS_KEY`、`MINIO_SECRET_KEY`、`MINIO_BUCKET` |
| 聊天模型 | `BASE_URL_CHAT`、`CHAT_MODEL`、`DEEPSEEK_API_KEY`、`DEEPSEEK_MODEL` |
| Embedding | `EMBEDDING_PROVIDER`、`EMBEDDING_MODEL`、`OLLAMA_BASE_URL` |
| 多模态 | `ENABLE_MULTIMODAL`、`VL_MODEL`、`VL_API_KEY`、图片安全参数 |
| MinerU | `MINERU_API_URL`、`MINERU_BACKEND`、`MINERU_TIMEOUT` |

`.env` 包含密钥和本机配置，已列入忽略规则，不应提交到 Git。

---

## 16. 安装、初始化与启动

### 16.1 准备配置

```powershell
Copy-Item .env.example .env
```

编辑 `.env`，至少确认数据库、Redis、Milvus、Neo4j、MinIO 和聊天模型配置。宿主机直接运行后端时，应使用 Compose 暴露的宿主机端口。

### 16.2 安装依赖

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip
pip install -r requirements.txt
```

### 16.3 启动基础设施

```powershell
docker compose up -d
docker compose ps
```

### 16.4 初始化数据库

```powershell
alembic upgrade head
python scripts/init_legal_postgres.py
python scripts/init_legal_neo4j.py
python scripts/init_milvus_indexes.py
```

法律统计数据和案例试点数据按需执行：

```powershell
python scripts/load_legal_statistics_postgres.py
python scripts/import_case_pilot.py
```

具体导入脚本可能依赖已经准备好的数据包，执行前应阅读脚本参数和对应目录 README，避免重复导入。

### 16.5 启动后端

```powershell
python -m uvicorn src.main:app --host 127.0.0.1 --port 8085 --reload
```

### 16.6 启动 Gradio

```powershell
$env:LEGAL_AGENT_API_BASE='http://127.0.0.1:8085'
$env:LEGAL_AGENT_GRADIO_PORT='7864'
python scripts/gradio_chat_demo.py
```

Windows 开发环境也可以运行：

```powershell
.\start_dev.bat
```

访问地址：

- Gradio：<http://127.0.0.1:7864/>
- OpenAPI：<http://127.0.0.1:8085/docs>
- 健康检查：<http://127.0.0.1:8085/health>
- 依赖检查：<http://127.0.0.1:8085/health/deps>

---

## 17. 降级与异常处理

应用启动时会检查 MinIO、Milvus 和 Neo4j，但单个外部依赖异常不会直接阻止 FastAPI 启动。运行中按能力降级：

- PostgreSQL 不可用：结构化法律、案例、渠道和咨询记录能力受限；
- Redis 不可用：多轮 Guide 状态、Checkpointer、统计上下文和文书下载无法稳定工作；
- Milvus 不可用：向量法条、类案、术语、文档和长期记忆召回受限；
- Neo4j 不可用：图谱关联和图谱调试结果缺失；
- MinIO 不可用：知识文档对象存储相关功能不可用；
- 聊天模型不可用：意图路由、抽取、规划和生成能力不可用；
- 视觉模型未配置：文本主流程正常，图片接口返回未启用；
- 法条或类案未召回：系统应给出明确降级说明，不得补写虚构内容。

生产环境应设置 `APP_DEBUG=false`，避免将内部异常细节返回给用户。

---

## 18. 测试与验收

### 18.1 测试规模

当前 `test/` 目录可收集 218 个测试用例。测试覆盖：

- 九节点图结构与条件路由；
- Supervisor 意图边界和专项回复透传；
- 多轮安全检测与紧迫性；
- 动态上下文追问、信息增益和收敛；
- 事实来源、更正、否认、冲突和流程指令隔离；
- 证据存在性、真实性和证明边界；
- 法条来源约束和幻觉净化；
- 类案检索与空结果兜底；
- 权威来源注册与追问引用；
- 法律统计 NL2SQL、ChatBI 和追问；
- 短期状态与长期记忆；
- 文书生成、官方模板和下载；
- 图片校验、上下文注入和隐私清理；
- API 契约与前端体验场景。

### 18.2 常用测试命令

纯文本主流程：

```powershell
python -m pytest test --ignore=test/test_multimodal_pipeline.py -k "not multimodal" -q
```

完整测试：

```powershell
python -m pytest test -q
```

单独测试多模态：

```powershell
python -m pytest test/test_multimodal_pipeline.py -q
```

测试收集：

```powershell
python -m pytest --collect-only -q test
```

依赖真实数据库、向量库或模型的测试应在完整环境中执行；单元测试通过不等于线上知识数据已完成初始化。

### 18.3 验收建议

每次修改至少检查：

1. Git 工作区中是否混入 `.env`、上传文件、日志或临时测试目录；
2. 九节点数量和状态兼容性是否保持；
3. 既有事实是否被重复询问；
4. 用户更正是否替代旧事实；
5. 具体法条能否追溯到真实召回；
6. 未核验证据是否被错误写成有效证据；
7. 用户要求收敛时能否立即生成方案；
8. Guide 终态能否继续生成 DOCX；
9. 官方模板是否显示正确匹配类型；
10. 依赖异常时是否给出可理解的降级提示。

---

## 19. 安全、隐私与合规

当前仓库是开发原型，已实现部分基础防护：

- `.env`、上传目录、日志和临时运行目录不进入 Git；
- 图片使用哈希化用户目录和随机文件 ID；
- 校验图片真实格式、大小和像素；
- 默认不保留图片临时副本；
- 文书下载使用随机 ID、私有缓存策略和 TTL；
- 图片 OCR/视觉文本中的指令不作为系统命令；
- 生成文书不虚构个人信息；
- 统计数据库使用只读用户；
- 具体法条与来源执行检索约束。

但生产部署前仍必须补充：

- 用户鉴权、租户隔离和资源级授权；
- API 限流、配额和防滥用；
- 文件恶意内容检测；
- 传输与存储加密；
- 密钥托管与定期轮换；
- 敏感信息脱敏和最小化留存；
- 操作审计、访问审计和模型调用审计；
- 数据删除、导出和用户授权机制；
- 数据库最小权限；
- 供应商模型的数据处理协议和跨境合规评估；
- 法律内容版本更新、失效标记和人工审核流程。

---

## 20. 已知限制

- 当前 FastAPI 接口未实现生产级身份认证、限流和租户隔离；
- 多模型与多数据库串行调用可能产生较高延迟；
- 法律数据规模和质量取决于实际导入环境，仓库文件不代表线上集合已经完整建立；
- 类案相似度不能代替法律要件比对；
- 法律统计只适用于已导入并符合 Schema 的数据；
- 图片理解可能受清晰度、遮挡、压缩和视觉模型误识别影响；
- DOCX 是参考初稿，仍需用户或专业人员核对事实、请求、主体和管辖；
- 官方空白模板仅覆盖当前注册的 8 类民事起诉状；
- CORS 白名单、调试模式和默认账号密码均为开发配置，生产环境必须调整；
- 外部法律、机构页面和办理规则会变化，权威来源需要持续更新；
- `requirements.txt` 目前包含较多固定依赖，后续应拆分运行、开发和评估依赖。

---

## 21. 后续建设建议

### P0：生产安全

1. 增加 OAuth2/JWT 鉴权和租户隔离；
2. 增加限流、上传安全扫描和敏感数据治理；
3. 关闭生产调试信息，改造默认口令和密钥管理；
4. 建立用户授权、留存期限和数据删除机制。

### P1：可靠性与质量

1. 建立 CI，自动运行单元、集成和契约测试；
2. 建立法条、案例、渠道和模板的版本同步与失效检查；
3. 为所有检索结论保存可审计的来源快照；
4. 扩充真实、多领域、多轮评估集；
5. 对幻觉率、重复追问率、错误收敛率和渠道有效率进行量化评估。

### P2：性能与体验

1. 并行化标准化、法条、类案、图谱和渠道查询；
2. 对低风险意图使用更轻量模型；
3. 增加流式节点进度和可取消请求；
4. 优化首轮路由和统计追问延迟；
5. 增加会话恢复、历史案件管理和用户可控记忆。

### P3：业务扩展

1. 扩充行政复议、仲裁、投诉和答辩类文书；
2. 建立不同地区的办理渠道和材料差异库；
3. 增加 PDF、DOCX 和更多证据文件解析；
4. 增加律师人工复核与转介机制；
5. 建立权威来源管理后台和知识更新工作流。

---

## 22. 仓库维护规范

- 根目录仅保留当前入口文档、配置和启动脚本；
- `.env`、密钥、个人数据、上传文件、日志和临时结果禁止提交；
- 不提交 `.pytest-tmp-*`、`.codex-runtime`、缓存和一次性调试输出；
- 文档中的节点、端口、接口和测试数量必须与代码一致；
- 不在说明书中维护容易过期的线上数据库静态条数；
- 新场景优先修复数据模型、状态策略、来源约束和通用规则，避免无限增加关键词补丁；
- 修改 GuideState 时要考虑 Redis 中旧状态的反序列化兼容；
- 修改官方模板时同步更新清单、哈希、来源信息和下载测试；
- 修改 API 返回结构时同步更新 Gradio 和契约测试；
- 推送前运行相关测试并检查 `git diff`；
- 提交信息应说明变更类型和影响范围。

---

## 23. 结语

本项目的核心价值不是用大模型直接“判断输赢”，而是把法律咨询拆解为可审计的工程过程：意图分流、事实确认、安全检查、证据分层、权威检索、动态追问、风险审校、行动指引和文书交付。

在当前阶段，它适合作为法律多智能体与 RAG 系统的研发、教学和验证平台。若用于真实公众服务，应在现有来源约束和安全机制之上，继续完成身份权限、隐私合规、知识更新、人工复核、服务监控和生产级可靠性建设。
