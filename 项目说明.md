# 法律多智能体平台 项目说明

> 本项目以「天宫医疗智能体」为工程底座，沿用其架构与技术栈，将多智能体范式迁移至**法律领域**。  
> 目标用户：不懂法的普通市民（ToC）；架构预留 ToB 扩展接口。定位：从描述情况到拿到可执行行动方案，并可按需生成参考文书草稿。

## 一、项目概述

采用「总助手 / 专项助手」（Supervisor / Worker）架构，Supervisor 识别用户意图并路由到对应专项 Agent：

| Agent | 面向对象 | 职责 | 状态 |
|---|---|---|---|
| **公民法律指引** (`guide_agent`) | 普通市民 | 描述具体纠纷/事件 → 法律依据 + 证据清单 + 维权路径比较 + 可操作渠道 | 调试中 |
| **法律知识问答** (`legal_qa_agent`) | 通用 | 法律概念/法条含义/制度性知识问答 | 已完成 |
| **专业法律助手** (`professional_agent`) | 法律从业者 | 裁决预测、案件分析、文书摘要 | 占位 |
| **法考助手** (`exam_agent`) | 法学学生 | 法考真题练习、知识点讲解 | 占位 |

### 典型场景（公民法律指引）

> "公司已经3个月没给我发工资了，我该怎么办？"

输出结构：
1. **理解情况**（一句共情）
2. **法律依据**（《劳动合同法》第85条等，锚定真实条文）
3. **类似案例**（类案参考+裁判要旨）
4. **维权路径比较**（调解[免费]/仲裁[免费]/起诉[收费] 三选对比）
5. **行动清单**（证据清单 + 具体步骤 + 渠道联系方式）

## 二、技术栈

- **Web / API**：FastAPI + Uvicorn，SSE 流式；Gradio 调试台（port 7862）
- **Agent 框架**：LangChain、LangGraph（Redis Checkpointer）、LangChain-DeepSeek
- **大模型**：DeepSeek (`deepseek-chat`)、DashScope (`qwen3.7-text-embedding` 1024维)
- **存储**：PostgreSQL 17(5433) · Neo4j 5.20(7688) · Milvus 2.6(19531) · Redis Stack 7.4(6380) · MinIO(9010)
- **迁移/评估/解析**：Alembic · TruLens · MinerU · python-docx · BeautifulSoup

## 三、系统架构

### 1. 双层路由

- **Supervisor**：`create_agent` + DeepSeek，只做意图识别与路由。Worker 以 Agent-as-Tool 暴露：
  `call_guide_agent` / `call_legal_qa_agent` / `call_professional_agent` / `call_exam_agent`
  + 记忆工具 `save_memory` / `search_memory`
- **指引状态机**：`guide_agent` 激活后（Redis `guide_active` 标志），`/api/v1/chat` 绕过 Supervisor 直驱 LangGraph，保证多轮追问连贯。

### 2. 公民法律指引状态机（`agents/legal_guide/graph.py`）

**最新改动（2026-07-26）**：
- ✅ **追问逻辑恢复**：MEDIUM/LOW档按置信度分级追问（MEDIUM追问证据，LOW追问事实细节），最多各3轮
- ✅ **检索结果传递优化**：`ask_facts`/`ask_evidence` 现在都接收 `law_context_str` 前400字，生成法条要件相关的追问（替代通用模板问题）
- ✅ **不利因素增强**：`evidence_unavailable`（缺失证据）自动合并进胜算评估的不利因素，格式："缺少「xxx」，对方可能质疑举证能力"
- ✅ **Supervisor透传修复**：`guide_agent` 首轮回复通过 Redis `guide_last_reply` 直传路由层，避免被 Supervisor 改写
- ✅ **文书生成bug修复**：phase=END后用户回复「生成文书」直接路由到generate_doc，不再二次触发conclude节点

13节点，带循环的状态机（对标医疗问诊流程）：

```
dispatcher → load_context → check_urgency
                ↓（每轮必过高危检测）
          extract_issues（三层标准化）
          ↓    ↓    
    clarify  score（打分前置）
    (2轮)     ↓           
           retrieve（所有档位都检索）
             ↓              
          conclude（按档位调整语气 + 胜算评估）
             ↓
      route_after_conclude（按置信度分流）
       ↙        ↓         ↘
  ask_facts  save_record  ask_evidence
  (LOW档3轮)  (HIGH档)    (MEDIUM档3轮)
      ↓                       ↓
  parse_details ←─────────────┘
      ↓
  extract_issues（重新标准化+打分+检索）
      ↓
    ...（循环直到轮次用尽或置信度提升到HIGH）
      ↓
  save_record → END
      ↓（用户回复「生成文书」）
  generate_doc → END
```

**关键机制：**
1. **分级追问（route_after_conclude）**：
   - HIGH档或超轮次上限 → `save_record` 直接结束
   - MEDIUM档（证据有缺口）→ `ask_evidence` 追问证据材料（最多3轮）
   - LOW档（事实不清）→ `ask_facts` 追问法律细节（最多3轮）
   
2. **法条驱动追问**：`ask_facts`/`ask_evidence` 接收 `law_context_str[:400]`，结合已检索法条的要件生成针对性问题（如"劳动合同法85条规定1年仲裁时效，需确认距离发生多久"）

3. **证据缺口纳入胜算**：`node_conclude` 的 `adverse_facts_section` 自动包含 `evidence_unavailable` 项，格式化为"缺少「工资条」，对方可能质疑举证能力"

| 节点 | 职责 | 法律特色 |
|---|---|---|
| `check_urgency` | 三级紧急分级（每轮） | CRITICAL(人身安全熔断) / TIME(时效紧迫) / NORMAL |
| `extract_issues` | 三层法律问题标准化 + 首轮证据提取 | LLM提取→Neo4j `LegalConcept`精确匹配→`legal_term_index`语义术语替换；首轮关键词匹配提取证据 |
| `clarify` | 澄清模糊案情（定性） | 上限2轮，仍模糊则用原话兜底检索（配合LOW档保守措辞） |
| `score` | 置信度打分（前置、零I/O） | 三维度：证据完整0.4+事实清晰0.3+权责清晰0.3 → HIGH(≥0.70)/MID(≥0.60)/LOW(<0.60) |
| `retrieve` | 并行RAG检索 | statute_rag（Dense+BM25混合+RRF融合+重排） + case_rag + Neo4j图查询；HIGH档额外做自省可降档；Milvus异常时PG兜底 |
| `conclude` | 五段式行动方案（按档位调语气）+ 双向胜算评估 | HIGH：完整方案+可直接参考；MEDIUM：完整方案+列出缺失信息；LOW：法律依据+初步方向+关键缺失清单+强烈建议补充；输出前置置信档位标签；附维权胜算三步分析（有利因素→不利因素[含证据缺口]+举证责任→综合胜算） |
| `ask_facts` | 追问法律细节（LOW档触发） | 从 `DOMAIN_DETAIL_TEMPLATES` 获取领域问题清单，结合 `law_context_str[:400]` 生成法条要件相关追问，最多3轮 |
| `ask_evidence` | 追问证据材料（MEDIUM档触发） | 从 `EVIDENCE_TEMPLATES` 获取领域证据清单，结合 `law_context_str[:400]` 生成证据相关追问，最多3轮 |
| `parse_details` | 解析用户追问回答 | 抽取新问题/证据/缺失证据/不利事实/地区/时间，写入 `GuideState` 各字段，返回 `extract_issues` 重新打分检索 |
| `generate_doc` | 参考文书生成 | 根据 legal_domain 选文书类型，用 GuideState 数据一次 LLM call 生成草稿 |
| `save_record` | 保存咨询记录到PG | 记录domain/issues/confidence/laws/cases到 guide_records 表 |

### 3. 法律RAG工具箱（`agents/legal_knowledge/`）

`legal_qa_agent` 和 `guide_agent` 共用，5个工具：

| 工具 | 数据源 | 说明 |
|---|---|---|
| `search_statute` | Milvus `statute_index` + PostgreSQL兜底 | Dense+BM25(jieba中文分词)混合检索 + RRF融合 + 重排；Milvus异常/返回空时降级PG ILIKE检索（仅用标准术语） |
| `search_similar_cases` | Milvus `case_index` | 类案相似检索，PG回查详情+裁判要旨 |
| `search_legal_graph` | Neo4j | NL2Cypher，Domain→Law+Channel关系 |
| `search_channels` | PostgreSQL | NL2SQL，按domain+region查渠道 |
| `search_legal_docs` | Milvus `knowledge_docs` | 律师上传文书RAG |

### 4. 记忆体系

- **短期**：LangGraph `AsyncRedisSaver`，键 `user_id:session_id`；`SummarizationMiddleware` 压缩（4000 tokens 或 6条消息触发，保留最近6条）
- **长期**：`MilvusStore` + DashScope `qwen3.7-text-embedding`（1024维），命名空间 `("users", user_id, "memories")`

### 5. API 调试信息

`POST /api/v1/chat` 在法律指引流程中额外返回 `debug` 字段：

```json
{
  "reply": "...",
  "session_id": "...",
  "debug": {
    "domain": "劳动",
    "confidence_tier": "HIGH",
    "statute_hits": "...",
    "case_hits": "...",
    "graph_laws": [...],
    "graph_channels": [...]
  }
}
```

Gradio 测试台底部「检索详情」面板实时展示以上内容。

## 四、目录结构

```
src/
├── main.py                          # FastAPI工厂
├── agents/
│   ├── supervisor_agent.py          # 法律路由提示（已更新）
│   ├── tools/
│   │   ├── worker_tools.py          # call_guide_agent/call_legal_qa_agent
│   │   └── store_tools.py           # save_memory/search_memory（长期记忆工具）
│   ├── workers/
│   │   ├── legal_qa_agent.py        # 完成
│   │   ├── guide_agent.py           # 完成（首轮+Redis状态持久化）
│   │   ├── professional_agent.py    # 占位
│   │   └── exam_agent.py            # 占位
│   ├── legal_guide/                 # 完成（11节点状态机 + 文书生成）
│   │   ├── state.py                 # GuideState/GuidePhase（含 doc_draft 字段）
│   │   ├── prompts.py               # 9提示词+3模板字典（含 DOC_GEN_PROMPT / DOC_TYPE_MAP）
│   │   ├── issue_normalizer.py      # 三层标准化（含JSON降级解析）
│   │   ├── neo4j_queries.py         # domain→laws+channels
│   │   ├── convergence.py           # check_convergence（milvus_hit优先收敛）
│   │   ├── db_queries.py            # load_user_context+save_record
│   │   └── graph.py                 # 10节点状态机 + run_guide接口
│   └── legal_knowledge/             # 完成
│       ├── runtime.py               # get_shared_legal_runtime（统一embedding入口）
│       ├── tools.py / statute_rag.py / case_rag.py
│       └── graph_rag.py / nl2sql.py / prompts.py
├── modules/legal/model.py           # ORM模型（Law/Article/LegalCase/Channel）
├── api/routers/
│   ├── chat.py                      # guide_active双路由 + DebugInfo返回
│   └── legal.py
└── core/ infra/ middlewares/

scripts/
├── init_legal_postgres.py           # 已执行
├── init_legal_neo4j.py              # 已执行（limit=200条案例）
├── init_milvus_indexes.py           # 已执行（法条全文embedding，案例facts+gist embedding）
├── build_legal_concepts.py          # 构建法律术语知识图谱（LLM提取→Neo4j LegalConcept→Milvus legal_term_index）
├── gradio_chat_demo.py              # 对话测试台（port 7862）含检索详情面板
└── start_dev.bat                    # 一键启动后端+测试台
```

## 五、数据现状

### PostgreSQL（法律库，端口5433）

| 表 | 数量 | 来源 |
|---|---|---|
| `laws` | 75部 | docx（法律53+行政法规14+司法解释5+其他3）|
| `articles` | 7,105条 | python-docx解析，最长968字 |
| `legal_cases` | 231个 | CAIL2019刑事(200) + 劳动/消费HTML(13) + 预付卡消费(12) + 混凝土供应(6) |
| `channels` | 103个 | 结构化contacts（phone/website/address）|

> 案例领域分布：contracts_property_housing(206) + labor_social_security(13) + consumer_market(12)

### Milvus（端口19531）

| 集合 | 数量 | embedding策略 | 备注 |
|---|---|---|---|
| `statute_index` | 7,105 | Dense(1024维) + Sparse(BM25,jieba分词) | 法条全文嵌入，RRF混合检索 |
| `case_index` | 24 | Dense(1024维) + Sparse(BM25) | facts+gist 完整拼接嵌入，每领域采样2条 |
| `legal_term_index` | 13,268 | Dense(1024维) | 法律标准术语向量，供第三层口语→术语映射 |

### Neo4j（端口7688，Browser: 7475）

节点：`Law`(75) · `Article`(7,105) · `LegalCase`(200) · `Channel`(103) · `Domain`(13) · `LegalConcept`(13,268)  
关系：`HAS_ARTICLE`(7,105) · `APPLIES_TO` · `HANDLES` · `BELONGS_TO`(12,706) · `CITES`(0，law_cases表为空)

### Embedding 模型

统一使用 `src.infra.embedding.get_embedding_model()`，由 `.env` 的 `EMBEDDING_PROVIDER` 控制：
- `dashscope`：`qwen3.7-text-embedding`，1024维（当前生产用）
- `ollama`：`bge-large`（本地备用）

## 六、开发进度

### 阶段一：底座配置 ✅

`core/config.py` · `main.py` · `modules/legal/model.py` · Alembic迁移 · `/health/deps` 五存储全绿

### 阶段二：数据入库 ✅

`init_legal_postgres.py` · `init_legal_neo4j.py` · `init_milvus_indexes.py` 全部执行完成

### 阶段三：RAG工具箱 + legal_qa_agent ✅

法条/类案/图谱/NL2SQL 五工具 + 幻觉检测 + qa_agent 集成完成

### 阶段四：guide_agent 状态机 ✅

**最新改进（2026-07-26）**：
- ✅ **追问逻辑恢复**：修复 `route_after_conclude` 实现分级追问（LOW档→ask_facts，MEDIUM档→ask_evidence），各最多3轮
- ✅ **检索信息传递**：`ask_facts`/`ask_evidence` 接收 `law_context_str[:400]`，生成法条要件相关追问（解决中间轮检索浪费问题）
- ✅ **不利因素增强**：`evidence_unavailable` 自动合并进 `adverse_facts_section`，格式："缺少「xxx」，对方可能质疑举证能力"
- ✅ **Supervisor透传**：通过 Redis `guide_last_reply` 键绕过 Supervisor 改写，确保 guide_agent 原始回复直达用户

**当前状态（追问恢复后的流程）**：
- 13节点状态机，澄清循环（2轮）+ 追问循环（LOW/MEDIUM各3轮）
- 提取到标准化问题后：`score → retrieve → conclude → route_after_conclude`
  - HIGH档或超轮次 → `save_record → END`
  - MEDIUM档 → `ask_evidence → parse_details → extract_issues → score → retrieve → conclude`（循环）
  - LOW档 → `ask_facts → parse_details → extract_issues → score → retrieve → conclude`（循环）
- 追问阶段用户每次补充信息都会触发**重新检索+重新打分**，置信度可能从LOW→MEDIUM→HIGH逐步提升

**置信度打分**：
- 三维度：证据完整0.4 + 事实清晰0.3 + 权责清晰0.3
- 阈值：HIGH≥0.70 / MID≥0.60 / LOW<0.60
- 打分前置、零I/O（不依赖检索结果）

**输出质量（按档位调整）**：
- **HIGH档**：完整的五段式方案 + 可直接参考（建议咨询律师确认细节）+ 完整胜算评估
- **MEDIUM档**：完整方案 + 明确列出缺失的关键信息 + 提示补充后更准确 + 胜算评估（注明需补充项）
- **LOW档**：法律依据 + 初步方向建议 + 关键缺失信息清单 + 强烈建议补充或拨打12348 + 胜算评估（信息不足处注明）
- **追问阶段（ask_facts/ask_evidence）**：短问题（200-600字），结合法条要件解释为何需要这些信息

**参考文书生成**：
- conclude完成后，用户回复「生成文书」触发 `node_generate_doc`
- 按领域自动选文书类型（劳动仲裁申请书/消费投诉信/行政复议申请书等）
- 复用 GuideState 已收集的案情数据，一次 LLM call 生成草稿

### 阶段五：三层标准化 + BM25 中文分词 ✅

**三层法律问题标准化：**
- **L1**：LLM 提取用户口语中的法律问题（粗标准化）
- **L2**：Neo4j `LegalConcept` 精确匹配（逐字相等）→ 命中即确认为标准术语
- **L3**：Milvus `legal_term_index` 语义映射（Top-3候选，同领域优先）→ 未命中词吸附到最近的标准术语

**口语→标准术语示例：**
- `"老板炒了我"` → `"违法解除劳动合同赔偿"`
- `"三个月没发工资"` → `"拖欠劳动报酬"`
- `"房东押金不退"` → `"押金返还纠纷"`

**BM25 中文分词修复：**
- 问题：BM25 默认按空格分词，中文整句作为一个词，查询返回空向量
- 修复：集成 jieba 分词器，词表从 15,985 个整句压缩为精简的词级词表
- 验证：所有标准术语均可正确编码（2-4个非零维度）

**检索提升机制：**
- **BM25 sparse**：只喂标准术语（字面精确匹配，如 `"拖欠劳动报酬"`）
- **Dense query**：标准术语 + 口语词 + 证据 + 领域标签（语义泛化）
- **RRF 融合**：`RRFRanker(k=60)` 混合 Dense + Sparse 排序
- **PG 兜底**：Milvus 异常时降级为 `ILIKE '%拖欠%' OR ILIKE '%劳动报酬%'`
- **民法典并列召回**：通过Neo4j `APPLIES_TO` 关系，特定领域（consumer_market/contracts_property_housing/labor_social_security）同时召回专门法和民法典

### 阶段六：测试与验证 ✅

**状态追踪测试（2026-07-26）**：
- 验证 4 轮对话状态积累：
  - 轮1（公司欠我工资）→ LOW(0.30)，触发 `ask_facts` 追问
  - 轮2（欠了3个月1万5）→ LOW(0.40)，继续追问
  - 轮3（在深圳有劳动合同没工资条）→ MEDIUM(0.60)，触发 `ask_evidence` 追问
  - 轮4（有银行流水记录）→ HIGH(0.70)，输出完整方案+胜算评估
- ✓ 状态字段正确积累（`evidence_confirmed`、`region`、`evidence_unavailable`）
- ✓ 检索每轮都执行（`law_context_str` 每轮非空）
- ✓ 最终回复包含维权胜算评估和法条引用
- ⚠ 追问阶段（轮1-3）回复不含胜算评估（设计行为：只有完整方案才含胜算）

**用户体验测试（2026-07-26 早期版本）**：4/5 通过
- ✓ 低智商用户（信息少）：3轮都检索，LOW档输出法律依据+初步方案
- ✓ 极低信息（第一轮）：澄清阶段，未过早检索
- ✓ 中等智商（逐步完善）：两轮都检索并输出
- ✓ 消费纠纷：成功召回民法典（9部法律包含民法典）
- ✗ 高智商用户：自省机制降档（预期的安全机制，非bug）

**测试报告**：`测试报告_用户体验全面测试_20260726.md` + `_test_state_trace.py`（状态追踪测试脚本）

### 阶段七：系统联调 ✅

已完成：
- Supervisor + Worker 双层路由打通
- guide_agent Redis 双路由（首轮走Supervisor→guide，续轮绕过Supervisor直驱）
- **Supervisor透传修复**：通过 Redis `guide_last_reply:{user_id}:{session_id}` 键，guide_agent 原始回复直达用户，避免被 Supervisor 改写
- API `DebugInfo` 调试字段（domain/confidence_tier/statute_hits/case_hits/graph_laws/graph_channels/fallback_guide）
- Gradio 测试台检索详情面板
- Embedding 模型统一（3处硬编码 → `get_embedding_model()`）
- **置信档位用户标签**：`node_conclude` 在回复前端预置一行可读标签（HIGH/MEDIUM/LOW 三级文案）
- **案例未命中 fallback**：`case_context_str` 为空且 `fallback_guide` 存在时，自动附上裁判文书网链接和检索提示
- **双向维权胜算评估**：`CONCLUDE_PROMPT` 新增三步分析（有利因素→不利因素+举证责任→综合胜算）
  - `GuideState.adverse_facts`：跨轮积累用户自身不利法律事实（由 `node_parse_details` 从 LLM 输出中提取）
  - `node_conclude` 构建 `adverse_facts_section`（不利事实列表 + **证据缺口自动合并**）和 `dialogue_snippet`（最近6条对话摘要）并注入 Prompt
- **追问法条感知优化**：`ask_facts`/`ask_evidence` 都接收 `law_context_str[:400]`，结合已检索法条要件生成针对性追问（替代通用模板问题，确保每轮追问都基于法律依据）

已修复的 Bug：
- `store_tools.py`：print 中的 emoji 导致 Windows GBK 终端 UnicodeEncodeError → 移除 print
- `DB_HOST=localhost` → `DB_HOST=127.0.0.1`（Python asyncpg 优先解析为 IPv6 `::1`，Docker 只监听 IPv4）
- `issue_normalizer.py`：`with_structured_output` 静默失败时增加 JSON 降级解析
- `graph.py` `node_extract_issues`：拼接最近3条人类消息，防止多轮澄清后上下文丢失
- `convergence.py`：`milvus_hit=True` 时直接收敛，不再要求 domain 非空
- `prompts.py`：域名映射表统一（13个规范域码 + 6个兼容别名），修复 EVIDENCE_TEMPLATES/DOMAIN_DETAIL_TEMPLATES 键不匹配问题
- `init_milvus_indexes.py`：BM25 训练改为全量语料（修复词表残缺），集成 jieba 中文分词器
- `state.py`：新增 `total_rounds`/`facts_rounds`/`evidence_rounds` 统一轮次计数
- `graph.py`：拆分 `ask_details` → `ask_facts`（追问细节）+ `ask_evidence`（追问证据），实现"追问细节→重新检索"循环
- `guide_agent.py`：`build_guide_deps()` 缺少 `db_session` → 导致 `_fetch_law_titles` 被跳过，法律名显示为"法律ID:XX" → 改用 `AsyncSessionLocal()` 创建 db_session 并传入
- `guide_agent.py` + `chat.py`：Supervisor 路径首轮指引结束时 `debug` 字段为空 → `guide_agent.py` 将调试信息写入 Redis `guide_last_debug:{user_id}:{session_id}`（TTL=120s），`chat.py` Supervisor 路径执行后读取该键并组装 `DebugInfo`
- `graph.py` `route_after_conclude`：恢复追问逻辑，LOW档→ask_facts（最多3轮），MEDIUM档→ask_evidence（最多3轮），HIGH档→save_record
- `confidence.py`：修复3处字符串引号嵌套语法错误（L115/L121/L134）
- `graph.py` `node_conclude`：`adverse_facts_section` 自动合并 `evidence_unavailable`，格式："缺少「xxx」，对方可能质疑举证能力"
- `graph.py` `node_ask_facts`：传入 `law_context_str[:400]` 到 prompt，生成法条要件相关追问
- `graph.py` `node_ask_evidence`：传入 `law_context_str[:400]` 到 prompt（与 ask_facts 同样处理），生成法条要件相关的证据追问
- `graph.py` `node_generate_doc`：phase=END 后生成文书不再改变 phase 状态，避免二次触发 conclude
- `start_dev.bat` + `gradio_chat_demo.py`：端口从8080改为8001（避免冲突）

### RAG 召回率优化 ✅

**问题**：用户口语（"被炒鱿鱼"/"老板不给钱"）与法条关键词（"解除劳动合同"/"拖欠劳动报酬"）语义gap，BM25完全失效。

**已完成**：
- `statute_rag.py` 新增 `search_statutes_pg_fallback()`：Milvus 异常或返回空时自动降级 PostgreSQL LIKE 检索（按领域+关键词）
- `graph.py` `node_retrieve`：Milvus 空结果时调用 PG 兜底，移除检索失败标记
- `statute_rag.py` filter 逻辑升级：有 domain 时并行召回领域法条 + 民法典（`domain == X || law_id == 75`），避免民法典被过滤漏掉
- `issue_normalizer.py` 三层标准化全面重写：
  - 第二层：`match_issues_in_neo4j` 查 `LegalConcept` 节点做精确匹配
  - 第三层：`semantic_fallback` 返回 `{原词: 标准术语}` 映射做真正的词替换（原逻辑只做可分类性检测，不做替换）
  - 降级：`legal_term_index` 未建时自动回退到 `statute_index`

**进行中**：
- `build_legal_concepts.py` 构建法律术语知识图谱（LLM从法条提取标准术语 → Neo4j `LegalConcept` + Milvus `legal_term_index`），脚本运行中

待完成：
- [ ] `build_legal_concepts.py` 运行完成后验证 Neo4j / Milvus 数据，测试口语→标准术语映射效果
- [ ] guide_agent 端到端完整测试（澄清→追问→检索→结论全流程）
- [ ] legal_qa_agent 超时优化（当前偶发60s超时）
- [ ] 案例数据扩充（劳动/消费/民事领域）

## 七、快速开始

### 前提：Docker 容器运行

```bash
docker compose up -d
# 验证：postgres/redis/milvus/neo4j/minio 全部 healthy
```

### 启动后端

```powershell
cd D:\learn\legal-agent
$env:PYTHONIOENCODING="utf-8"
D:/develop/Miniconda/envs/legal/python.exe -m uvicorn src.main:app --port 8001 --reload
```

### 启动测试台（另开终端）

```powershell
D:/develop/Miniconda/envs/legal/python.exe scripts/gradio_chat_demo.py
# 浏览器打开 http://localhost:7862
```

### 或使用一键脚本

```
双击 start_dev.bat
```

### 注意事项

- `.env` 中 `DB_HOST` 必须用 `127.0.0.1`（不能用 `localhost`，asyncpg 会优先走 IPv6）
- Windows 终端必须设置 `PYTHONIOENCODING=utf-8`，否则 loguru 日志中文不显示
- 端口 8080 被占用时已改为 8001（`start_dev.bat` 和 `gradio_chat_demo.py` 已同步更新）
- 多次重启未生效时，用 PowerShell 彻底清理旧进程：
  ```powershell
  Get-NetTCPConnection -LocalPort 8001 -State Listen | ForEach-Object { Stop-Process -Id $_.OwningProcess -Force }
  ```

## 八、安全与注意事项

- **幻觉防控**：法条与案号必须来源锚定，`statute_rag` 输出附带法律名+条号
- **NL2SQL护栏**：只允许SELECT，拦截DDL/DML，强制LIMIT 20，10s超时
- **紧急分流**：`check_urgency` 识别人身安全威胁 → 立即给出110/12348热线
- **时效提醒**：识别到时间信息时，RAG检索对应时效条文，输出中加警示
- **鉴权**：jwt_utils/password_utils脚手架已存在，正式部署前需接入chat接口
- **默认凭据**：docker-compose默认密码仅限本地，生产前必须更换
