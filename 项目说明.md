# 法律多智能体平台 项目说明

> 本项目以「天宫医疗智能体」为工程底座，沿用其架构与技术栈，将多智能体范式迁移至**法律领域**。  
> 目标用户：不懂法的普通市民。定位：从描述情况到拿到可执行行动方案。

## 一、项目概述

采用「总助手 / 专项助手」（Supervisor / Worker）架构，Supervisor 识别用户意图并路由到对应专项 Agent：

| Agent | 面向对象 | 职责 | 状态 |
|---|---|---|---|
| **公民法律指引** (`guide_agent`) | 普通市民 | 描述具体纠纷/事件 → 法律依据 + 证据清单 + 维权路径比较 + 可操作渠道 | 🔄 调试中 |
| **法律知识问答** (`legal_qa_agent`) | 通用 | 法律概念/法条含义/制度性知识问答 | ✅ 已完成 |
| **专业法律助手** (`professional_agent`) | 法律从业者 | 裁决预测、案件分析、文书摘要 | ⏳ 占位 |
| **法考助手** (`exam_agent`) | 法学学生 | 法考真题练习、知识点讲解 | ⏳ 占位 |

### 典型场景（公民法律指引）

> "公司已经3个月没给我发工资了，我该怎么办？"

输出结构：
1. **理解情况**（一句共情）
2. **法律依据**（《劳动合同法》第85条等，锚定真实条文）
3. **类似案例**（类案参考+裁判要旨）
4. **维权路径比较**（调解[免费]/仲裁[免费]/起诉[收费] 三选对比）
5. **行动清单**（证据清单 + 具体步骤 + 渠道联系方式）

## 二、技术栈

- **Web / API**：FastAPI + Uvicorn，SSE 流式；Gradio 调试台
- **Agent 框架**：LangChain 1.2、LangGraph 1.1（Redis Checkpointer）、LangChain-DeepSeek
- **大模型**：DeepSeek (`deepseek-chat`)、DashScope (`qwen-max` + `text-embedding-v3` 1024维)
- **存储**：PostgreSQL 17 · Neo4j 5.20 · Milvus 2.6 · Redis Stack 7.4 · MinIO · SQLite(TruLens)
- **迁移/评估/解析**：Alembic · TruLens · MinerU · python-docx · BeautifulSoup

## 三、系统架构

### 1. 双层路由

- **Supervisor**：`create_agent` + DeepSeek，只做意图识别与路由。Worker 以 Agent-as-Tool 暴露：
  `call_guide_agent` / `call_legal_qa_agent` / `call_professional_agent` / `call_exam_agent`
  + 记忆工具 `save_memory` / `search_memory`
- **指引状态机**：`guide_agent` 激活后（Redis `guide_active` 标志），`/api/v1/chat` 绕过 Supervisor 直驱 LangGraph，保证多轮追问连贯。

### 2. 公民法律指引状态机（`agents/legal_guide/graph.py`）

9节点，对标医疗问诊状态机：

```
dispatcher → load_context → check_urgency
                ↓
          extract_issues ←─── parse_details
          ↓           ↑
       clarify     issue_search → ask_details
                       ↓
                    conclude → save_record → END
```

| 节点 | 职责 | 法律特色 |
|---|---|---|
| `check_urgency` | 三级紧急分级 | CRITICAL(人身安全) / TIME(时效紧迫) / NORMAL |
| `extract_issues` | 三层法律问题标准化 | LLM→Neo4j领域确认→statute_index语义兜底 |
| `issue_search` | 并行RAG检索 | statute_rag + case_rag + graph查询 |
| `ask_details` | 领域专项追问 | domain模板（劳动/消费/合同/刑事各不同） |
| `conclude` | 五段式行动方案 | 情感开场+法条+类案+路径比较[免费标注]+证据清单+行动步骤 |

### 3. 法律RAG工具箱（`agents/legal_knowledge/`）

`legal_qa_agent` 和 `guide_agent` 共用，5个工具：

| 工具 | 数据源 | 说明 |
|---|---|---|
| `search_statute` | Milvus `statute_index` | HyDE + 重排，返回法条+条号 |
| `search_similar_cases` | Milvus `case_index` | 类案相似检索，PG回查详情 |
| `search_legal_graph` | Neo4j | NL2Cypher，Domain→Law+Channel关系 |
| `search_channels` | PostgreSQL | NL2SQL，按domain+region查渠道 |
| `search_legal_docs` | Milvus `knowledge_docs` | 律师上传文书RAG |

### 4. 记忆体系（零改动复用）

- **短期**：LangGraph `AsyncRedisSaver`，键 `user_id:session_id`；`SummarizationMiddleware` 压缩
- **长期**：`MilvusStore` + DashScope 向量（1024维），命名空间 `("users", user_id, "memories")`

## 四、目录结构（当前实际）

```
src/
├── main.py                          # FastAPI工厂（沿用）
├── agents/
│   ├── supervisor_agent.py          # ✅ 法律路由提示（已更新）
│   ├── tools/
│   │   ├── worker_tools.py          # ✅ call_guide_agent/call_legal_qa_agent（已完成）
│   │   └── store_tools.py           # 沿用
│   ├── workers/
│   │   ├── legal_qa_agent.py        # ✅ 完成
│   │   ├── guide_agent.py           # ✅ 完成（首轮+Redis状态持久化）
│   │   ├── professional_agent.py    # ✅ 占位完成
│   │   └── exam_agent.py            # ✅ 占位完成
│   ├── legal_guide/                 # ✅ 完成（9节点状态机）
│   │   ├── state.py                 # ✅ GuideState/GuidePhase
│   │   ├── prompts.py               # ✅ 6提示词+2模板字典
│   │   ├── issue_normalizer.py      # ✅ 三层标准化（含JSON降级解析）
│   │   ├── neo4j_queries.py         # ✅ domain→laws+channels
│   │   ├── convergence.py           # ✅ check_convergence（milvus_hit优先收敛）
│   │   ├── db_queries.py            # ✅ load_user_context+save_record
│   │   └── graph.py                 # ✅ 完成（9节点状态机 + run_guide接口）
│   └── legal_knowledge/             # ✅ 完成
│       ├── __init__.py / runtime.py / tools.py
│       ├── statute_rag.py / case_rag.py / graph_rag.py
│       └── nl2sql.py / prompts.py
├── modules/legal/model.py           # ✅ ORM模型（Law/Article/LegalCase/Channel）
├── core/ infra/ rag/ api/           # 沿用
└── middlewares/ utils/              # 沿用

scripts/
├── init_legal_postgres.py           # ✅ 已执行
├── init_legal_neo4j.py              # ✅ 已执行
├── init_milvus_indexes.py           # ✅ 已执行（statute_index+case_index）
└── gradio_chat_demo.py              # ✅ 对话测试台（port 7860）
```

## 五、数据现状

### PostgreSQL（法律库）

| 表 | 数量 | 来源 |
|---|---|---|
| `laws` | 74部 | batch-04 docx（法律53+行政法规14+司法解释5+其他2）|
| `articles` | 5,845条 | python-docx解析条文正则提取 |
| `legal_cases` | 1,655个 | CAIL2019刑事三元组(1555) + concrete劳动/消费HTML(100) |
| `channels` | 78个 | batch-06结构化contacts（phone 159条/website 76条）|

> 案例数据持续补充中，当前只覆盖刑事+劳动+消费三域，后续会扩展。

### Milvus

| 集合 | 数量 | 说明 |
|---|---|---|
| `statute_index` | 5,792 | articles全量嵌入，IVF_FLAT COSINE dim=1024 |
| `case_index` | 100 | legal_cases抽样（facts前512字符），待扩充 |
| `knowledge_docs` | 保留 | 律师上传文书RAG |

### Neo4j（法律知识图谱）

节点：`Law` · `Article` · `LegalCase` · `Channel` · `Domain`
关系：`HAS_ARTICLE` · `APPLIES_TO` · `HANDLES` · `CITES`

## 六、开发进度

### 阶段一：底座配置 ✅

`core/config.py` · `main.py` · `modules/legal/model.py` · Alembic迁移 · `/health/deps` 五存储全绿

### 阶段二：数据入库 ✅

`init_legal_postgres.py` · `init_legal_neo4j.py` · `init_milvus_indexes.py` 全部执行完成

### 阶段三：工作流 ✅

| 子阶段 | 内容 | 状态 |
|---|---|---|
| 3-A | `agents/legal_knowledge/` RAG工具箱（7文件） | ✅ 完成 |
| 3-B Workers | `legal_qa_agent.py` | ✅ 完成 |
| 3-B Workers | `professional_agent.py` · `exam_agent.py` 占位 | ✅ 完成 |
| 3-B Workers | `guide_agent.py`（首轮+Redis状态持久化） | ✅ 完成 |
| 3-C | `agents/legal_guide/` 9节点状态机 | ✅ 完成 |
| 3-D | `supervisor_agent.py` 法律路由提示 | ✅ 完成 |
| 3-D | `worker_tools.py` call_guide_agent/call_legal_qa_agent | ✅ 完成 |
| 测试台 | `scripts/gradio_chat_demo.py` 对话测试台 | ✅ 完成 |
| API | `api/routers/chat.py` guide_active双路由 | ✅ 完成 |

**当前阶段：系统联调测试**

已修复的关键 Bug：
- `issue_normalizer.py`：`with_structured_output` 静默失败时增加 JSON 降级解析
- `graph.py` `node_extract_issues`：拼接最近3条人类消息，防止多轮澄清后上下文丢失
- `convergence.py`：`milvus_hit=True` 时直接收敛，不再要求 domain 非空

## 七、快速开始

```sh
conda create -n legal python=3.13 && conda activate legal
pip install -r requirements.txt
cp .env.example .env        # 填写 DeepSeek/DashScope Key 及各DB连接
docker compose up -d
alembic upgrade head
python scripts/init_legal_postgres.py
python scripts/init_legal_neo4j.py
python scripts/init_milvus_indexes.py
uvicorn src.main:app --port 8080 --reload

# 启动对话测试台（另开终端）
python scripts/gradio_chat_demo.py  # http://localhost:7860
```

## 八、安全与注意事项

- **幻觉防控**：法条与案号必须来源锚定，`statute_rag` 输出附带法律名+条号
- **NL2SQL护栏**：只允许SELECT，拦截DDL/DML，强制LIMIT 20，10s超时
- **紧急分流**：`check_urgency` 识别人身安全威胁 → 立即给出110/12348热线
- **时效提醒**：识别到时间信息时，RAG检索对应时效条文，输出中加警示
- **鉴权**：jwt_utils/password_utils脚手架已存在，正式部署前需接入chat接口
- **默认凭据**：docker-compose默认密码仅限本地，生产前必须更换
