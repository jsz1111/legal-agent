# `assess_evidence` 证据评估节点说明书

> 文档状态：正式节点说明，已接入 GuideGraph、FastAPI DebugInfo、Gradio 兼容路径和电脑网页端证据评估面板
> 编写日期：2026-08-01  
> 所属工作流：维权助手 GuideGraph  
> 节点序号：节点六  
> 关联文档：`维权工作流优化说明书.md`、`plan_evidence节点说明书.md`、`知识库数据内容详细说明书.md`

## 1. 节点定位

`assess_evidence` 是维权助手目标八节点工作流中的第六个核心节点，负责评估用户实际提交的证据材料，并计算材料对案件证明目标的覆盖情况。

它位于：

```text
plan_evidence
    ↓
用户提交或补充证据
    ↓
assess_evidence
    ├── 需要核验材料属性 → 最多暂停追问一轮
    ├── 发现新事实或事实冲突 → update_facts → decide_facts
    └── 完成单份评估和整体覆盖 → generate_solution
```

一句话定义：

> `assess_evidence` 负责确认“用户实际提交了什么材料、材料能够初步支持哪些证明目标、存在哪些限制，以及当前案件的整体证据覆盖处于什么状态”。

当前实现边界：

| 已实现能力 | 当前行为 |
|---|---|
| 独立图节点 | `GuideGraph` 已暴露 `assess_evidence`；旧 `assess_retrieve` 仍保留为兼容桥 |
| 批次控制 | 上传或保存草稿只暂存；显式“完成本批次并评估”后才生成完整评估 |
| 版本校验 | 评估绑定案件代次、事实快照、证据计划和材料批次；过期材料进入待重新归类状态 |
| 单份材料评估 | 每份材料记录解析状态、可见内容、定位、来源形式、完整性、主体/时间可见性、可能用途和限制 |
| 证明目标覆盖 | 按节点五的 `proof_target_id` 计算 `covered`、`partially_covered`、`conflicted`、`not_submitted` 等状态 |
| 材料核验 | 最多暂停一轮属性核验；回答“不清楚”不会被系统强行推断 |
| 事实回流 | 材料出现可定位的新事实或冲突时，只生成待确认候选，不自动覆盖事实账本 |
| 增量评估 | 复用未变化材料并生成 `evidence_review_version`、变化摘要和评估指纹 |
| 前端/Gradio | 两条入口共用同一节点和结构化结果；网页端展示材料状态、覆盖状态和补强方向 |

原始文件是否长期保留仍由上传接口和项目存储策略决定。节点六不会把解析摘要当作原件，也不会在未确认时宣称原件已保存。

节点不负责：

- 决定案件需要哪些法律关系；
- 替法院、仲裁机构或行政机关认定证据效力；
- 认定材料绝对真实、合法、可采或具有最终证明力；
- 根据上传材料直接修改案件事实；
- 生成最终维权方案。

节点负责：

- 保存和识别本批次材料；
- 提取材料中可以定位的直接观察；
- 调用证据规则和法律知识库；
- 将材料映射到待证事实；
- 评估单份材料的可能用途和限制；
- 计算整体证明目标覆盖；
- 识别冲突、新事实和证据缺口；
- 决定是否需要一轮材料核验追问；
- 输出可供方案节点使用的结构化报告。

节点六只评估实际收到或能够读取的材料。用户仅说“我有聊天记录”时，只更新材料名称和声称持有状态，不得生成已提交或已评估结论。

对用户可见的名称建议使用：

> 证据材料初步评估

不要使用：

> 证据有效性最终判定

## 2. 核心原则

### 2.1 保留原件，文字只是中间表示

图片、PDF、DOCX、音频和视频必须保留原始文件。解析后的文字、OCR、表格和视觉观察只能作为可检索、可比对、可引用的中间表示。

```text
原始材料
+ 文件元数据
+ 解析文本
+ 页面或时间定位
+ 视觉观察
= 可审计材料记录
```

不能将原始证据替换成一段无定位的模型摘要。

### 2.2 先确定证明目标，再评估材料

系统不应向知识库直接提问：

```text
这张截图是不是有效证据？
```

应当先确定：

```text
本案需要证明什么？
这份材料可能证明其中哪一项？
相关法律或程序规则要求检查哪些属性？
```

然后按待证事实和证明目标调用知识库。

### 2.3 区分材料观察和法律判断

材料观察可以由解析器和模型完成：

```text
图片显示“成交金额：800.00元”。
```

法律判断必须有事实、规则和来源依据：

```text
该内容可以初步支持付款金额，但不能单独确认收款人身份。
```

### 2.4 评估结论必须可回链

每个重要结论必须至少绑定一种依据：

- 材料原文和定位；
- 用户已确认事实；
- 其他材料的交叉印证；
- 结构化证据规则；
- 精确定位的法律或程序依据。

没有依据的结论只能输出为未知、待核验或条件式判断。

### 2.5 单份材料和整体覆盖必须分开

节点六必须先形成单份材料报告，再按 `proof_target_id` 汇总整体覆盖：

```text
材料 A 的观察和限制
+ 材料 B 的观察和限制
+ 与确认事实及其他材料的交叉比对
= 某个证明目标的整体覆盖状态
```

一份材料部分支持某个事实，不代表案件整体证据充分；多份内容重复的截图也不能按数量直接累加证明强度。

### 2.6 增量评估，不重复处理全部材料

后续补证、替换材料或事实变化时，只重新处理：

```text
新增或替换的材料
+ 受事实变化影响的材料关联
+ 受影响的证明目标
```

未变化的材料观察、来源定位和有效依据可以复用，但必须检查它们绑定的 `fact_snapshot_version`、`evidence_plan_version` 和来源版本是否仍然有效。

### 2.7 不因缺少材料阻断条件式方案

用户可以只提交部分材料、暂不提交或明确无法提供。节点六应保留相应缺口并输出条件式覆盖，不得循环要求用户补齐所有材料，也不得把 `not_submitted` 自动改成 `explicitly_absent`。

## 3. 节点触发条件

### 3.1 正常触发

以下情况触发 `assess_evidence`：

- 用户在证据中心完成本批次提交；
- 用户提交一批图片、PDF、DOCX、音频或视频；
- 用户补充此前缺失的材料；
- 用户替换某份材料；
- 用户明确修改材料状态；
- 事实变化导致已有材料需要重新映射；
- 方案生成后用户补交关键证据。

### 3.2 不触发完整评估

以下情况不立即触发完整证据评估：

- 用户只提到“我有聊天记录”，但没有提交材料；
- 用户在事实收集阶段只提供证据名称；
- 用户上传空文件或上传失败；
- 用户没有点击“完成本批次并评估”；
- 用户询问如何准备证据但没有新增材料。

这些情况只更新证据库存或生成证据需求。

### 3.3 早期上传

事实收敛前可以接收用户提前上传的材料，但先保存为：

```text
received_pending_target
```

待证明目标建立后再进行完整评估。

## 4. 节点输入

### 4.1 案件状态

```text
case_id
case_generation
workflow_stage
legal_domain
region
procedure_type
fact_snapshot_version
fact_snapshot_hash
fact_blackboard_version
legal_model_version
case_facts
fact_records
confirmed_issues
claims
time_info
```

### 4.2 证明目标

```text
proof_targets
formal_evidence_requirements
evidence_plan_version
dependent_fact_keys
priority
```

旧状态中的 `required_evidence` 只能作为迁移兼容字段。进入目标节点六前，应转换为具有稳定 `requirement_id` 和 `proof_target_id` 的 `formal_evidence_requirements`。

每个证明目标至少包含：

```text
proof_target_id
target_description
proof_purpose
dependent_fact_keys
recommended_material_types
alternative_material_types
importance
procedure_type
authority_refs
```

### 4.3 历史证据状态

```text
evidence_items
evidence_assessments
evidence_links
evidence_coverage
evidence_unavailable
evidence_unverified
previous_evidence_review_version
previous_evidence_coverage_version
previous_assessment_change_summary
```

### 4.4 本批次材料

```text
evidence_batch_id
evidence_batch_version
materials[]
submission_completed
submitted_by
submitted_at
base_case_generation
base_fact_snapshot_version
base_evidence_plan_version
verification_round_count
idempotency_key
```

每份材料至少包含：

```text
material_id
requirement_id
proof_target_id
file_name
file_type
file_path
sha256
source_form
original_object_ref
original_available
acquisition_method
parser_status
parsed_text
page_count_or_duration
metadata
user_note
submission_status
```

## 5. 节点内部流程

### 5.1 接收和幂等检查

首先检查：

- `case_id` 是否存在；
- `base_case_generation` 是否仍然有效；
- `evidence_batch_id` 是否已处理；
- 文件 SHA-256 是否已经存在；
- 同一请求是否重复提交；
- 当前用户是否有案件访问权限。

重复提交不应产生新的材料记录或重复评估。

### 5.2 保存原始材料

保存：

- 原始文件；
- 文件指纹；
- 上传时间；
- 来源表单；
- 解析状态；
- 文件大小；
- MIME 类型；
- 页数或时长。

保存失败时，节点返回 `received_but_unstored`，不能标记为已评估。

### 5.3 解析材料

按材料类型选择解析器：

| 材料 | 解析方式 |
|---|---|
| 图片 | OCR + 视觉模型 + 图像尺寸和区域定位 |
| PDF | 文本解析 + 页码 + 表格 + 页面渲染抽样 |
| DOCX | 段落、表格、页眉页脚和文件元数据 |
| TXT/CSV | 结构化文本和编码检查 |
| 音频 | 转写 + 时间戳 + 声音内容摘要 |
| 视频 | 关键帧 + 音频转写 + 时间轴 |

解析输出必须保留原文定位，例如：

```text
PDF 第3页第2段
图片第1张左下区域
音频 00:03:18-00:03:42
聊天记录 2026-07-18 14:32
```

### 5.4 提取材料观察

模型只允许提取材料中实际可以观察到的内容：

```json
{
  "material_id": "payment-001",
  "locator": "图片第1张中部",
  "observed_text": "成交金额：800.00元",
  "fields": {
    "amount": "800.00元"
  },
  "source_form": "screenshot",
  "completeness": "partial",
  "identity_visibility": "unclear",
  "time_visibility": "clear",
  "source_anchor": true
}
```

没有原文定位时，属性必须为 `unknown`。

模型不得仅依据“这是付款截图”推断：

- 付款已经成功；
- 收款人就是对方；
- 截图没有被修改；
- 对方一定承担责任；
- 法院一定采纳。

### 5.5 确定受影响的证明目标

优先使用以下顺序映射：

1. 材料已经关联的 `proof_target_id`；
2. 用户选择的证据清单项目；
3. 稳定的证据需求编号；
4. 领域证据目录匹配；
5. 通用 `proof_roles` 映射；
6. 无法归类时进入“待归类材料”。

新材料只影响关联的证明目标，不重新计算整个知识库。

### 5.6 校验证据计划和事实版本

正式评估前检查：

```text
base_fact_snapshot_version == current_fact_snapshot_version
base_evidence_plan_version == current_evidence_plan_version
requirement_id 仍处于 active 或可评估状态
proof_target_id 仍然存在
```

如果事实快照或证据计划已经过期：

- 不用旧证明目标覆盖新计划；
- 已上传文件继续安全保存；
- 材料标记为 `received_pending_remap`；
- 回到最新证据计划重新归类；
- 旧评估保留历史，不物理删除。

### 5.7 调用证据规则和权威依据

围绕受影响的 `proof_target_id` 调用第六节知识库流程，生成：

```text
assessment_dimensions
required_material_attributes
alternative_materials
authority_refs
basis_limitations
retrieval_trace_id
```

检索失败不影响保存和观察材料，但不得输出确定性的法律效力判断。

### 5.8 生成单份材料评估

对每份成功接收的材料分别生成：

```text
直接可见内容
来源和原始载体状态
完整性和可识别性
关联证明目标
可能支持的范围
不能单独支持的事项
与事实及其他材料的冲突
法律或程序风险提示
补强方向
依据和限制
```

解析失败的材料也必须生成记录，但状态只能是 `received_but_unparsed` 或 `unknown`。

### 5.9 规划最多一轮核验

只有材料属性会明显改变关联或覆盖结论时才生成核验问题，例如：

- 是否保留原始设备、原始文件或完整聊天记录；
- 文件来自平台导出、截图还是复制件；
- 收款账号与交易对方如何对应；
- 截图是否缺少主体、时间或上下文；
- 不同材料的金额、日期或账号为何不一致。

核验必须满足：

```text
verification_round_count < 1
question relates to submitted material
answer can materially change assessment
question is not an ordinary case-fact question
```

用户不回答或明确不知道时，未知状态直接进入条件式覆盖，不再重复核验。

### 5.10 汇总覆盖并写入新版本

单份评估和核验完成后：

```text
按 proof_target_id 汇总材料
→ 识别重复、互补和冲突
→ 计算整体覆盖状态
→ 生成 evidence_review_version
→ 生成 assessment_change_summary
→ 持久化后进入下一路由
```

只有实际发生材料、关联、依据或覆盖变化时才增加评估版本。

## 6. 知识库调用规范

### 6.1 调用目标

知识库调用不是为了判断材料真假，而是为了获得：

- 该证明目标需要证明哪些要素；
- 该领域建议检查哪些材料属性；
- 哪些材料可以作为替代或补强；
- 适用的法律和程序依据；
- 该依据是否现行、有效和已审核。

### 6.2 调用顺序

```text
proof_target_id
        ↓
结构化证据规则
        ↓
权威来源登记和版本
        ↓
精确法条检索
        ↓
官方办事或平台规则
        ↓
类案或专业文档补充
```

### 6.3 结构化证据规则

首先查询 `followup_catalog` 或未来的 `evidence_basis_rules`：

```text
domain
proof_target_id
procedure_type
fact_keys
recommended_materials
alternative_materials
assessment_dimensions
authority_refs
```

当前项目已有的 `evidence_rules.py` 可以提供部分领域证据清单和官方示范文本来源。

### 6.4 权威依据检索

调用 `authority_basis_index` 时必须携带：

```text
legal_domain
procedure_type
proof_target_id
target_description
region
event_date
```

召回结果必须回链 PostgreSQL 的：

- `authority_sources`；
- `authority_versions`；
- `followup_rule_citations`。

只有以下结果可以作为用户可见的法律依据：

```text
source.status = active
version.review_status = approved
version.effective_from <= event_date
version.effective_to 为空或晚于 event_date
mapping_status = pinpointed 或 source_located
```

`needs_pinpoint` 和 `system_only` 只能作为内部候选或缺口提示。

### 6.5 法条检索

通过 `statute_index` 检索：

```text
证明目标
+ 已确认事实
+ 用户诉求
+ 程序类型
+ 地区
+ 事件时间
```

检索结果必须包含：

```text
law_id
law_title
article_no
article_text
domain
retrieval_score
source_version
```

向量相似度只能用于召回，不能单独作为引用依据。

### 6.6 官方模板和办事规则

当证明目标与官方示范文本或官方办理要求相关时，补充检索：

- 文书中的事实栏目；
- 材料栏目；
- 办理机构；
- 地域范围；
- 受理期限；
- 官方提交渠道。

示范文本只能说明材料准备方向，不得写成“法院固定要求”。

### 6.7 类案检索

类案仅在以下情况调用：

- 用户请求类似案例；
- 需要补充争议处理背景；
- 需要说明可能的事实差异；
- 法律依据已经确定，类案只作为解释材料。

类案不能作为证据评估的主要法源。

### 6.8 MCP 调用

MCP 是可选的外部来源或专业工具连接，不替代本地知识库。

适合调用 MCP 的情况：

- 本地库没有最新官方平台规则；
- 需要查询实时官方页面；
- 需要外部文档解析或 OCR；
- 需要获得经授权的官方数据库内容。

MCP 返回内容必须经过：

```text
来源确认
→ 版本确认
→ 适用范围确认
→ 原文定位
→ 本地引用快照
```

MCP 未返回可定位原文时，不得作为正式法律依据展示。

### 6.9 检索输入和隐私边界

知识库检索输入应使用结构化、最小化信息：

```json
{
  "legal_domain": "consumer_market",
  "procedure_type": "platform_dispute_or_civil_claim",
  "proof_target_id": "proof.transaction.payment",
  "material_type": "payment_screenshot",
  "assessment_gaps": [
    "payee_identity_unclear",
    "original_carrier_unknown"
  ],
  "region": "已确认地区或未知",
  "event_date": "已确认日期或未知"
}
```

不得把不必要的姓名、身份证号、手机号、账号、住址或整份材料原文发送给外部 MCP。外部检索只接收完成任务所需的脱敏条件；原始文件和完整解析文本保留在案件材料存储中。

## 7. 单份材料评估

每份材料应分别评估以下维度：

| 维度 | 评估问题 |
|---|---|
| 可识别性 | 主体、时间、金额、内容是否看得清 |
| 来源形式 | 原件、原始电子文件、平台导出、截图还是复制件 |
| 完整性 | 是否缺页、裁剪、截断或缺少上下文 |
| 主体对应 | 材料中的人、账号、平台是否能对应案件主体 |
| 时间对应 | 材料时间能否对应事件时间线 |
| 内容对应 | 材料内容是否与待证事实有关 |
| 取得方式 | 取得或导出方式是否已知 |
| 原始载体 | 是否仍保留原始设备、原始文件或原始账号 |
| 一致性 | 是否与其他材料和事实列表一致 |
| 支持范围 | 能证明什么，不能单独证明什么 |
| 法律风险 | 是否存在隐私、非法取得或程序使用风险 |

用户侧结论使用：

- 支持较强；
- 部分支持；
- 待核验；
- 存在冲突；
- 暂不支持当前目标。

不使用“有效/无效”的二元判断。

单份材料建议使用以下评估状态：

```text
supports
partially_supports
needs_verification
conflicted
not_relevant_to_current_target
received_but_unparsed
unknown
```

真实性、合法性、可采性和最终证明力必须作为独立维度保存：

```text
authenticity_status = unknown | user_stated_original | cross_checked | disputed
legality_risk = none_identified | needs_review | material_risk | unknown
admissibility_note = not_determined
probative_scope = 当前材料可以初步支持的具体范围
```

`user_stated_original` 只表示用户声称保留原件，不能升级为系统已核验真实。

## 8. 整体证据覆盖

单份材料评估完成后，针对每个 `proof_target_id` 计算整体覆盖。

### 8.1 覆盖状态

```text
covered
partially_covered
conflicted
not_submitted
explicitly_absent
third_party_available
not_applicable
unresolved
```

### 8.2 覆盖计算原则

不能按材料数量简单评分：

```text
3张相似截图 ≠ 3个独立证明
1份完整平台导出 ≠ 自动证明所有争议事实
用户称持有 ≠ 已提交可评估材料
已提交 ≠ 真实性已确认
```

覆盖结果必须说明：

- 哪些待证事实已有材料支持；
- 哪些只被部分支持；
- 哪些材料之间存在冲突；
- 哪些材料尚未提交；
- 哪些材料可向第三方调取；
- 哪个缺口最可能改变当前判断。

## 9. 节点输出结构

建议输出如下：

```json
{
  "case_id": "case-001",
  "case_generation": 3,
  "fact_snapshot_version": 2,
  "legal_model_version": 2,
  "evidence_plan_version": 3,
  "evidence_review_version": "evidence-review:case-001:3",
  "evidence_batch_id": "batch-20260731-001",
  "reviewed_material_ids": [],
  "reused_material_assessment_ids": [],
  "items": [],
  "observations": [],
  "proof_targets": [],
  "links": [],
  "coverage": [],
  "basis_refs": [],
  "basis_limitations": [],
  "new_fact_candidates": [],
  "content_conflicts": [],
  "quality_gaps": [],
  "unclassified_materials": [],
  "pending_verification": [],
  "verification_round_count": 0,
  "assessment_change_summary": {
    "added_material_ids": [],
    "updated_material_ids": [],
    "changed_target_ids": []
  },
  "assessment_status": "completed",
  "next_route": "generate_solution",
  "disclaimer": "本评估仅用于梳理材料用途和补强方向，不认定真实性、合法性、可采性或最终证明力。"
}
```

### 9.1 `EvidenceItem`

```text
id
name
material_id
availability
source_form
parser_status
completeness
identity_visibility
time_visibility
acquisition_method
case_specificity
authenticity_status
inspection_basis
source_excerpt
source_locator
content_conflicts
limitations
```

### 9.2 `EvidenceLink`

```text
evidence_id
target_id
direction
relevance
proof_scope
basis
basis_refs
limitations
```

### 9.3 `EvidenceCoverage`

```text
target_id
label
purpose
status
supporting_evidence_ids
authority_refs
quality_gaps
limitations
next_action
```

### 9.4 `basis_refs`

```json
[
  {
    "basis_type": "material_observation",
    "material_id": "payment-001",
    "locator": "图片第1张中部",
    "excerpt": "成交金额：800.00元"
  },
  {
    "basis_type": "confirmed_fact",
    "fact_key": "transaction.amount",
    "fact_version": 2
  },
  {
    "basis_type": "authority_rule",
    "authority_source_id": "consumer-rights-current",
    "authority_version_id": "consumer-rights-current:2026",
    "article_no": "待精确定位",
    "mapping_status": "needs_pinpoint"
  }
]
```

## 10. 路由规则

### 10.1 评估完成

```text
assess_evidence
→ evidence_coverage 完成
→ 没有新事实
→ 没有高价值冲突
→ generate_solution
```

### 10.2 需要材料属性核验

```text
assess_evidence
→ pending_verification 非空
→ interrupt 等待用户回答
→ prepare_case 区分材料属性和案件事实
→ 纯材料属性回到 assess_evidence
→ 新案件事实进入 update_facts
```

上传后的核验最多进行一轮。用户不回答时，保持未知状态继续生成条件式方案。

### 10.3 发现新事实

```text
assess_evidence
→ new_fact_candidates 非空
→ update_facts
→ decide_facts
```

材料内容不能自动覆盖已有事实。新事实必须进入事实列表并等待用户确认。

### 10.4 发现事实或材料冲突

```text
assess_evidence
→ content_conflicts 非空
→ 标记冲突
→ 生成核对问题
→ 用户确认或保留 conflicted
```

系统不得自行选择其中一份材料为真。

如果冲突只涉及材料质量或来源属性，可以进入一次材料核验；如果冲突涉及金额、日期、主体、行为或案件经过，必须生成带材料定位的事实候选并回到 `update_facts`、`decide_facts`。

### 10.5 用户没有提交材料

```text
用户未提交
→ 保留 not_submitted
→ 允许按当前信息生成条件式方案
```

未提交不能自动转换为明确没有。

### 10.6 后续补交或替换材料

```text
方案生成后用户补交或替换材料
→ prepare_case 识别 evidence_submission
→ assess_evidence 只评估变化材料
→ 重新计算受影响的 proof_target_id
→ 生成新 evidence_review_version
→ generate_solution 生成受影响方案的新版本
```

未受影响的材料、证明目标和依据不得重复解析或重建。

### 10.7 评估失败或只有部分材料

```text
部分材料成功、部分失败
→ 保存全部接收记录
→ 对成功部分形成评估
→ 失败部分保留缺口和重试状态
→ 允许进入条件式方案
```

不能因一份文件失败而丢弃整个批次，也不能把半成品评估标记为完整完成。

## 11. 与其他节点的边界

| 节点 | 负责内容 | 不负责内容 |
|---|---|---|
| `prepare_case` | 恢复案件、识别本次提交类型 | 不评估材料 |
| `guard_case` | 安全、时效、证据灭失检查 | 不判断证据效力 |
| `update_facts` | 提取和修正事实 | 不决定材料是否足够 |
| `decide_facts` | 判断事实充分度、生成事实问题 | 不评估上传材料 |
| `plan_evidence` | 建立证明目标和证据清单 | 不判断实际材料质量 |
| `assess_evidence` | 评估材料和证据覆盖 | 不生成最终行动方案 |
| `generate_solution` | 生成方案和定性可能性 | 不重新解析每份材料 |
| `audit_and_save` | 输出审校、版本和任务保存 | 不替代证据评估 |

## 12. 版本、幂等和长期案件

### 12.1 版本绑定

每次评估必须绑定：

```text
case_generation
fact_snapshot_version
legal_model_version
evidence_plan_version
evidence_batch_id
evidence_batch_version
evidence_review_version
```

这样可以明确某份评估基于哪一版事实、法律模型、证据清单和材料批次。任一上游版本变化时，只将受影响的评估标记为 `stale`，禁止覆盖或删除旧版本。

### 12.2 幂等键

建议使用：

```text
case_id
+ evidence_batch_id
+ evidence_plan_version
+ material_sha256_set
+ verification_round_count
```

相同幂等键重复执行时返回已有评估版本，不重新解析文件、重复调用模型或创建重复材料记录。

### 12.3 长期案件和持续补证

案件、证据清单和证据评估长期保存，直到用户手动删除案件。用户可以跨日、跨周或在方案生成后继续补交材料：

```text
旧评估版本保留
→ 新材料形成新批次
→ 只重评变化材料和受影响证明目标
→ 形成新评估版本和变化摘要
→ 必要时更新方案版本
```

普通缓存 TTL 不得删除案件材料、评估历史、待核验状态或材料与证明目标之间的关联。

### 12.4 原始材料和删除

原始材料、解析产物、评估和版本记录必须具有统一案件归属。用户手动删除案件时，按照项目的数据删除策略统一清理：

- 原始附件和派生解析文件；
- 材料指纹与数据库记录；
- 单份材料评估；
- 整体覆盖报告；
- 待核验问题；
- 方案中的材料引用；
- 与该案件关联的长期记忆。

删除前应进行案件权限、目标范围和关联数量检查，避免跨案件误删。

## 13. 当前代码映射与重构要求

当前代码已经具备并已接入节点六的内部函数：

| 当前实现 | 作用 |
|---|---|
| `src/agents/legal_guide/attachment_parser.py::extract_document_bytes` | 解析部分文档附件 |
| `src/agents/legal_guide/evidence_analysis.py::split_uploaded_evidence_blocks` | 分离用户叙述和上传材料块 |
| `src/agents/legal_guide/evidence_analysis.py::inspect_uploaded_evidence_blocks` | 检查上传材料的基础属性 |
| `src/agents/legal_guide/evidence_analysis.py::normalize_evidence_observations` | 校验原文锚点和允许字段 |
| `src/agents/legal_guide/evidence_analysis.py::merge_evidence_observations` | 合并材料观察 |
| `src/agents/legal_guide/evidence_analysis.py::evaluate_evidence` | 生成当前证明目标和覆盖报告 |
| `src/agents/legal_guide/evidence_analysis.py::evaluate_state_evidence` | 根据当前状态计算覆盖 |
| `src/agents/legal_guide/evidence_analysis.py::format_evidence_coverage` | 生成用户可读覆盖说明 |
| `src/agents/legal_guide/assess_evidence.py::run_assess_evidence` | 节点六入口，负责批次、版本、评估报告和路由结果 |
| `src/agents/legal_guide/assess_evidence.py::parse_material_batch` | 解析本轮附件和旧 Gradio 内嵌材料块 |
| `src/agents/legal_guide/assess_evidence.py::assess_material_item` | 生成单份材料评估 |
| `src/agents/legal_guide/assess_evidence.py::calculate_target_coverage` | 按正式证明目标汇总覆盖状态 |
| `src/agents/legal_guide/assess_evidence.py::plan_evidence_verification` | 生成最多一轮材料属性核验 |
| `src/agents/legal_guide/graph.py::node_assess_evidence` | GuideGraph 正式节点入口 |
| `src/agents/legal_guide/graph.py::node_assess_retrieve` | 旧流程兼容桥，不作为新流程节点六的实现 |
| `src/agents/legal_guide/graph.py::node_parse_details` | 旧流程兼容逻辑，新增材料评估由正式节点六承担 |

历史上这些逻辑分布在 `parse_details`、证据追问和方案生成流程中；目前正式路径已形成独立的 `assess_evidence` 图节点，旧逻辑仅用于兼容已有状态和 Gradio 会话。

当前仍需后续完善：

- 上传接口的原始文件长期存储策略仍由项目存储层决定，节点六只记录是否可访问，不能替代原件保管；
- 证据规则检索目前优先复用节点五的 `plan_basis_refs`，更细粒度的法条/MCP 依据回链可继续补强；
- 正式 `generate_solution` 和 `audit_and_save` 已接入；证据评估快照随每个正式方案版本长期保存；
- 原始附件长期对象存储和材料删除审计仍需由生产存储层继续完善。

当前迁移和兼容边界：

```text
旧 node_parse_details 中的案件事实
→ update_facts

旧 node_parse_details 中的材料解析和观察
→ assess_evidence 内部解析模块

assess_evidence
→ 负责完整证据评估、证据依据引用和覆盖计算

generate_solution
→ 读取评估报告和法律检索结果，生成结构化方案草稿

audit_and_save
→ 审校证据边界，发布并保存正式方案版本

conclude / save_record
→ 只保留旧会话兼容能力
```

目标八节点图中不再保留 `parse_details` 核心节点。文件解析、观察提取、单份评估和覆盖计算是 `assess_evidence` 的内部辅助模块；需要用户确认的新案件事实才回流 `update_facts`。

建议新增内部函数：

```python
retrieve_evidence_basis()
build_assessment_basis_refs()
assess_material_item()
calculate_target_coverage()
plan_evidence_verification()
validate_evidence_plan_version()
parse_material_batch()
map_materials_to_proof_targets()
detect_material_fact_candidates()
build_assessment_change_summary()
checkpoint_evidence_review()
```

建议扩展状态字段：

```text
evidence_observations
evidence_basis_refs
formal_evidence_requirements
evidence_review_version
evidence_review_status
evidence_reviewed_at
evidence_batch_id
evidence_batch_version
pending_evidence_verification
verification_round_count
new_fact_candidates_from_evidence
evidence_basis_missing
assessment_change_summary
unclassified_materials
```

## 14. API 与前端要求

### 14.1 前端提交

前端提交材料时传递：

```text
case_id
evidence_batch_id
evidence_batch_version
base_case_generation
base_fact_snapshot_version
base_evidence_plan_version
requirement_id
proof_target_id
file
source_form
original_available
acquisition_method
user_note
submission_status
idempotency_key
```

“完成本批次并评估”是明确的批次提交动作。用户仅选择文件、保存草稿或继续上传时，不得反复触发完整节点六。

### 14.2 前端展示

每项材料展示：

- 材料名称；
- 解析状态；
- 材料中直接可见的内容；
- 关联的待证事实；
- 当前评估等级；
- 评估依据；
- 不能单独证明的内容；
- 补强建议；
- 是否需要用户核验。

整体区域还应展示：

- 当前 `evidence_plan_version` 和 `evidence_review_version`；
- 本批新增、变化和失败材料；
- 每个证明目标的覆盖状态；
- 当前最重要的证据缺口；
- 本次评估相对上一版的变化；
- “补交证据”“替换材料”“完成本批次并重新评估”操作。

材料状态和评估结果应使用稳定布局，长文本用 Markdown 分层展示。不得用单一百分比分数代替用途、限制、冲突和覆盖说明。

不得向用户展示：

- 内部 Agent 名称；
- Milvus 分数；
- Redis 键；
- 未审核来源的内部调试信息；
- 模型不确定的原始推理过程。

### 14.3 Gradio

Gradio 使用与网页端相同的 `assess_evidence` 后端节点：

- 支持批量文件；
- 显示材料观察和覆盖结果；
- 支持最多一轮核验；
- 支持补交材料；
- 使用相同批次提交动作和版本校验；
- 展示相同的来源和免责声明。

不能为 Gradio 单独实现一套绕过知识库和证据状态的评估逻辑。

## 15. 异常和降级

| 异常 | 处理 |
|---|---|
| 证据计划版本过期 | 保存材料并标记 `received_pending_remap`，基于最新计划重新归类 |
| 原始文件保存失败 | 不进入评估，标记 `received_but_unstored` 并允许安全重试 |
| 文件无法解析 | 保留文件和指纹，标记 `received_but_unparsed` |
| OCR 不清晰 | 保留原图，相关字段为 `unknown` |
| 部分文件解析失败 | 对成功材料继续评估，失败材料单独保留缺口 |
| 模型超时 | 使用确定性材料库存继续，禁止补造属性 |
| 知识库检索失败 | 输出材料观察和条件式缺口，不编造法律依据 |
| 权威依据待定位 | 显示“来源存在但条文定位未完成” |
| 多材料冲突 | 标记 `conflicted`，生成核对问题 |
| 用户不回答核验问题 | 保持未知，继续条件式方案 |
| 重复上传 | 依据 SHA-256 和幂等键复用结果 |
| 版本冲突 | 拒绝覆盖旧状态，要求基于最新案件版本重试 |
| 评估写入失败 | 不发布半成品覆盖，保留批次和可重试检查点 |
| 外部 MCP 不可用 | 使用本地知识库或降级为材料整理 |

## 16. 闲鱼付款案例示例

用户提交：

```text
付款截图：显示800元和付款日期
聊天截图：显示卖家承诺发货
平台页面：显示卖家已将用户拉黑
```

节点执行：

```text
1. 保存三份原始材料并生成文件指纹
2. 提取每份材料的文字和页面定位
3. 关联 transaction.payment
4. 关联 transaction.relationship
5. 关联 delivery.obligation
6. 关联 non_performance
7. 关联 communication.demand
8. 关联 platform_complaint 或 platform_action
9. 调用消费/合同领域证据规则
10. 检索付款、交易关系和电子记录相关依据
11. 比较金额、日期、账号和主体是否一致
12. 生成单份材料评估
13. 生成整体覆盖报告
14. 对收款人身份不清晰时生成一次核验追问
```

用户最终看到的重点应是：

```text
付款金额已有初步材料支持；
收款人与卖家身份对应仍需订单或完整账单补强；
未发货事实需要物流、订单状态或完整聊天记录支持；
截图本身不能单独证明对方构成欺诈或必然承担责任。
```

## 17. 测试要求

### 17.1 单元测试

至少覆盖：

1. 只有证据名称、没有实际材料时不触发完整评估；
2. 提前上传材料能够保存为 `received_pending_target`；
3. 事实快照或证据计划过期时材料不丢失且进入重新归类；
4. 同一 `evidence_batch_id` 重复提交保持幂等；
5. 相同文件指纹不会创建重复材料；
6. 图片、PDF、DOCX、音频和视频使用正确解析器；
7. 解析文字均保留页码、区域或时间定位；
8. 没有来源定位的观察不会成为评估依据；
9. 材料名称不会被误当作材料内容；
10. 用户声称保留原件不会被标记为系统已核验真实；
11. 未解析材料保留原始文件并输出未知状态；
12. 材料优先关联用户选择的 `requirement_id` 和 `proof_target_id`；
13. 未列入清单的材料进入待归类区；
14. 单份材料评估和整体覆盖分别保存；
15. 多份重复截图不会按数量累加覆盖；
16. 相互补充的材料能够共同提高相关证明目标覆盖；
17. 材料冲突不会被系统静默选择；
18. 评估结果明确说明可能用途、限制和补强方向；
19. 检索结果可以回链来源、版本和精确定位；
20. `needs_pinpoint` 不作为确定性用户依据；
21. 知识库失败时不编造法条或证据规则；
22. 类案不会替代现行法律和程序依据；
23. 外部 MCP 不可用时可以降级到本地知识库；
24. 不必要的敏感原文不会发送给外部 MCP；
25. 材料属性核验最多生成一轮；
26. 用户不回答核验问题时保留未知并继续条件式方案；
27. 核验回答包含新案件事实时正确回流节点三；
28. 材料暴露新事实时不会直接修改事实黑板；
29. 只有部分材料成功解析时可以形成部分评估；
30. 未提交材料不会自动转换为明确没有；
31. 方案后补交材料只重评变化材料和受影响证明目标；
32. 未变化材料的观察和依据可以安全复用；
33. 评估变化会生成新 `evidence_review_version`；
34. 没有实际变化时不会创建空评估版本；
35. 旧评估版本不会被物理覆盖；
36. 删除其他案件不会影响当前案件材料；
37. 网页端和 Gradio 得到相同评估状态、覆盖和下一路由。

### 17.2 集成测试

```text
plan_evidence
→ 用户完成证据批次
→ assess_evidence
→ generate_solution
```

验证正式证据清单、材料关联、单份报告和整体覆盖。

```text
assess_evidence
→ awaiting_evidence_verification
→ 用户回答
→ prepare_case
→ assess_evidence
```

验证材料属性核验最多一轮。

```text
assess_evidence
→ new_fact_candidates
→ update_facts
→ decide_facts
→ plan_evidence
→ assess_evidence
```

验证材料暴露新事实后的确认、计划更新和重新映射。

```text
generate_solution
→ 用户后续补证
→ assess_evidence
→ generate_solution 新版本
```

验证长期案件中的局部重评和方案更新。

## 18. 验收标准

### 18.1 节点行为

- `assess_evidence` 可以独立运行和重试；
- 只评估实际接收的材料，不把证据名称或声称持有当作已提交；
- 每次评估绑定事实快照、法律模型、证据计划和材料批次版本；
- 证据计划过期时材料不会丢失或错误关联；
- 用户未提交材料时不会误判为明确没有；
- 同一材料重复提交不会产生重复评估；
- 只重新评估新增材料和受影响的证明目标；
- 每份原始材料、解析内容和观察均有稳定定位；
- 单份材料评估和整体证明目标覆盖严格分离；
- 证据暴露新事实时能回流事实节点；
- 证据冲突不会被系统静默选择；
- 上传后核验最多一轮；
- 用户长期补证能够形成新版本并保留历史；
- 部分失败不会导致成功材料或整个批次丢失。

### 18.2 知识库调用

- 先查询证明目标，再调用法律依据；
- 规则、法条和官方来源可以回链；
- 未审核或未精确定位的来源不会作为确定法律依据；
- 法条检索失败时不编造引用；
- 类案不会替代法律原文；
- MCP 结果必须保存来源和定位后才能引用。

### 18.3 评估输出

- 每份材料有直接观察、可能用途和限制；
- 每个证明目标有覆盖状态；
- 每个重要判断至少有一个 `basis_ref`；
- 输出清晰区分材料观察、事实依据、规则依据和法律依据；
- 输出明确区分可能用途、不能单独证明的事项、冲突和补强方向；
- 覆盖状态不使用简单材料数量或单一百分比分数计算；
- 每次变化均有 `assessment_change_summary`；
- 不输出最终证据效力、胜诉保证或法院必然采纳结论；
- 网页端和 Gradio 输出一致。

## 19. 最小实现顺序

### 第一阶段：独立节点和版本边界

1. 将材料解析和 `evaluate_evidence` 封装为独立 `assess_evidence` 图节点；
2. 接入 `formal_evidence_requirements`、`proof_targets` 和 `evidence_plan_version`；
3. 建立材料批次、文件指纹、幂等和上游版本验证；
4. 保留原始材料、解析产物、定位和材料观察；
5. 增加评估检查点和独立重试。

### 第二阶段：单份材料和整体覆盖

1. 为每份材料建立独立评估记录；
2. 将材料映射到证明目标和正式证据需求；
3. 分离单份材料状态与整体覆盖状态；
4. 增加重复、互补和冲突材料处理；
5. 增加待归类和无法解析材料状态。

### 第三阶段：知识库和核验

1. 按证明目标检索证据规则、法条和权威依据；
2. 为 `EvidenceLink` 和 `EvidenceCoverage` 增加 `basis_refs`；
3. 增加版本、精确定位和法律审核门槛；
4. 增加最多一轮材料属性核验暂停；
5. 增加知识库和外部 MCP 降级。

### 第四阶段：回流、长期补证和联调

1. 材料暴露新事实时回流节点三、节点四；
2. 后续补证只重评变化材料和受影响证明目标；
3. 保存 `evidence_review_version` 和变化摘要；
4. 将结构化评估报告交给 `generate_solution`；
5. 接入电脑网页端证据中心；
6. 让 Gradio 使用相同节点、批次、版本和核验状态。

## 20. 最终节点定义

目标工作流中的节点六固定为：

```text
assess_evidence
```

节点内部处理链为：

```text
验证证据计划和材料批次
→ 保存原始材料
→ 解析并提取可定位观察
→ 映射证明目标
→ 调用证据规则和权威依据
→ 生成单份材料评估
→ 必要时进行一次材料属性核验
→ 计算整体证明目标覆盖
→ 识别缺口、冲突和新事实
→ 生成评估版本和变化摘要
```

正常结果只有三类：

```text
评估完成
→ generate_solution

需要材料属性核验
→ 暂停一次，回答后回到 assess_evidence

发现新的案件事实或重大事实冲突
→ update_facts
→ decide_facts
```

节点六回答：

> 用户实际提交的材料能够初步支持哪些证明目标、还存在哪些限制和缺口，以及当前整体证据覆盖处于什么状态。

节点七再回答：

> 在当前事实、法律依据和证据覆盖条件下，用户下一步应该采取哪些行动。
