"""agents/legal_knowledge/ 共享工具提示词常量（从 agents/knowledge/prompts.py 迁移）。"""

# ── HyDE ────────────────────────────────────────────────────────────────────────
HYDE_PROMPT = """你是知识专家。请根据以下问题，写一段假设性的回答（约100-200字），作为检索的参考文本。
不需要完全准确，目的是生成一段与正确答案语义相近的文本，用于提升向量检索的召回率。

问题：{question}

直接输出假设性回答，不要加前缀或解释。"""

LEGAL_STATUTE_HYDE_PROMPT = """你是中国法律专家。根据以下法律纠纷，生成一段最可能适用的**法律条文原文**（假设文档）。

目的：生成与真实法律条文语义相近的文本，提升法条向量检索的召回率。
格式要求：严格模仿真实法律条文格式，例如：
  《中华人民共和国劳动合同法》第八十五条：用人单位有下列情形之一的……

法律纠纷：{question}

只输出一段假设法条原文，不加任何解释或前缀。"""

LEGAL_CASE_HYDE_PROMPT = """你是中国法律专家。根据以下法律纠纷，生成一段最可能相似的**案件事实陈述**（假设文档）。

目的：生成与真实案件事实语义相近的文本，提升案例向量检索的召回率。
格式要求：模仿裁判文书事实部分，简洁陈述时间、当事人、行为、损害结果。

法律纠纷：{question}

只输出案件事实描述，不加解释。"""

# ── 文档 RAG ────────────────────────────────────────────────────────────────────
DOC_QA_PROMPT = """你是法律知识问答助手，请根据以下检索到的文档片段回答用户问题。

用户角色：{role}
用户问题：{question}

检索到的文档片段：
{context}

要求：
1. 只根据上述文档内容回答，不要编造信息
2. 回答中必须内联标注引用来源
3. 如果文档内容不足以回答问题，明确告知"当前知识库中未找到相关信息"

直接输出回答内容。"""

# ── 幻觉检测 ────────────────────────────────────────────────────────────────────
HALLUCINATION_CHECK_PROMPT = """判断以下回答是否完全基于提供的检索结果，有无编造信息。

用户问题：{question}

检索结果摘要：
{evidence}

系统回答：
{answer}

逐条检查回答中的事实性陈述，判断每条是否有检索结果支撑。

以 JSON 格式输出：
{{"is_grounded": true/false, "unsupported_claims": ["无依据的陈述1"], "confidence": 0.0-1.0}}

只输出 JSON，不要解释。"""
