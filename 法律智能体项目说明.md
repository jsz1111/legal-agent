# 法律多智能体平台（Legal Multi-Agent）项目说明书

> 本项目以「天宫医疗智能体」为工程底座与脚手架，沿用其架构与技术栈，将医疗领域的多智能体范式迁移到**法律领域**。总助手（Supervisor）根据用户意图路由到不同专项法律 Agent。

## 一、项目概述

法律多智能体平台面向不同用户群体，提供分层的法律服务。采用「总助手 / 专项助手」（Supervisor / Worker）架构，由 Supervisor 识别用户意图与身份，路由到边界清晰的专项 Agent。

规划四个 Agent，**本期开发 Agent 1、Agent 2**，Agent 3、Agent 4 在 `workers/` 目录下预留占位：

| Agent | 面向对象 | 职责 | 本期状态 |
| --- | --- | --- | --- |
| **公民法律助手** | 普通群众 | 解决生活中的法律问题、案件、纠纷：给出**怎么做**、**法律依据**、**相似案例**、**维权渠道** | ✅ 开发 |
| **法律知识问答** | 通用 | 法律概念、法条含义、法律制度的知识性问答 | ✅ 开发 |
| **专业法律助手** | 法律从业者 | 裁决预测、案件分析、法律文档摘要生成 | ⏳ 预留占位 |
| **法考助手** | 法学学生 | 法律考试辅导、真题练习、知识点讲解 | ⏳ 预留占位 |

### 典型场景（公民法律助手）

> 某群众在理发店被骗了很多钱，想知道怎么办。

Agent 应输出：
1. **行动建议**：如何报警/向消协投诉/协商/起诉的具体步骤
2. **法律依据**：引用《消费者权益保护法》《民法典》相关条款（合同欺诈、价格欺诈等），并**锚定真实条文**，不臆造
3. **相似案例**：检索类案，说明类似纠纷是如何解决的
4. **维权渠道**：12315 消费者热线、当地市场监管局、消协、人民调解委员会、法院立案窗口等，附官网/邮箱/电话

## 二、技术栈（沿用医疗底座）

- **Web / API**：FastAPI + Uvicorn，SSE 流式对话；Gradio 调试台
- **Agent 框架**：LangChain 1.2、LangGraph 1.1（Redis Checkpointer）、LangChain-DeepSeek/OpenAI、MCP
- **大模型**：DeepSeek（`deepseek-chat`，主对话/推理）、DashScope/Qwen（`qwen-max`、`text-embedding-v3` 向量）
- **存储 / 数据库**：
  - PostgreSQL 17（asyncpg + SQLAlchemy 2.0 异步）— 结构化法律数据
  - Neo4j 5.20（+APOC）— 法律知识图谱
  - Milvus 2.6 — 向量库（法条 / 案例 / 长期记忆）
  - Redis Stack 7.4 — 短期记忆检查点 + 咨询状态
  - MinIO — 上传法律文书对象存储
  - SQLite — TruLens 评估 / 本地缓存
- **迁移**：Alembic ｜ **评估**：TruLens（RAG 三元组）｜ **文档解析**：MinerU、LlamaIndex

## 三、系统架构

### 1. 双层路由（架构核心，沿用医疗）

- **Supervisor（总助手）**：LangChain `create_agent` + `deepseek-chat`，只做意图/身份识别与路由，不直接回答法律问题。各 Worker 以「Agent-as-Tool」暴露为工具：
  - `call_citizen_agent`（公民法律助手）
  - `call_legal_qa_agent`（法律知识问答）
  - `call_professional_agent`（专业法律助手，预留）
  - `call_exam_agent`（法考助手，预留）
  - 记忆工具：`save_memory` / `search_memory`
- **咨询状态机**：公民法律助手激活后（Redis `consult_active` 标志），`/api/v1/chat` **绕过 Supervisor**，直接驱动咨询 LangGraph，保证多轮追问连贯。

### 2. 意图边界（关键设计——避免 Agent 越界）

| Agent | 触发（IN） | 排除（OUT，路由给谁） |
| --- | --- | --- |
| **公民法律助手** | 用户遇到**具体**法律问题/纠纷/案件，需要行动方案与维权指引（"我被骗了怎么办"） | 纯概念性提问 → 法律知识问答；专业分析需求 → 专业助手 |
| **法律知识问答** | **抽象**知识性提问（"什么是善意取得""合同法第几条规定…"），无个人处境 | 涉及个人具体处境求助 → 公民法律助手 |
| **专业法律助手** | 法律从业者的裁决预测、案件深度分析、文书摘要 | 普通群众求助 → 公民法律助手 |
| **法考助手** | 备考、真题、知识点辨析 | 真实法律求助 → 公民法律助手 |

Supervisor 系统提示需明确：区分「求助/知识/专业/备考」四类意图；含个人处境细节的一律走公民法律助手；纯知识问答走知识问答 Agent；识别用户身份（群众/从业者/学生）辅助路由。

### 3. 记忆体系（沿用医疗）

- **短期记忆**：LangGraph `AsyncRedisSaver`，键 `user_id:session_id`；`SummarizationMiddleware` 超阈值压缩历史。
- **长期记忆**：`MilvusStore` + DashScope 向量（1024 维），命名空间 `("users", user_id, "memories")`。

### 4. 公民法律助手工作流（LangGraph StateGraph，对标问诊状态机）

对标医疗问诊的 9 节点，法律版节点设计：

1. `load_context`：加载用户历史咨询（首轮）
2. `check_scope`：判断是否属法律范畴 + **紧急/安全检测**（涉及人身安全、正在发生的犯罪 → 提示拨打 110/报警）
3. `extract_elements`：抽取案件要素（主体、行为、时间、金额、证据、诉求）——三层归一化（LLM 抽取 → Neo4j 案由匹配 → Milvus 语义兜底）
4. `clarify`：要素不全时追问（"是否签订合同""是否有转账记录/聊天证据"）
5. `classify_domain`：法律领域分类（消费维权/合同/劳动/婚姻家庭/侵权…），映射到相关法条与案由
6. `retrieve`：并行检索——法条（doc RAG）+ 相似案例（case RAG）+ 图谱关系（GraphRAG）
7. `generate_solution`：生成结构化方案（行动步骤 + 法律依据 + 类案 + 维权渠道），经**幻觉检查**锚定真实条文/案号
8. `conclude`：给出可执行结论与渠道清单（`RemedyChannels`）
9. `save_record`：咨询记录落库 PostgreSQL

`dispatcher` 按 `round` 路由：首轮走全流程；后续轮走 `parse_answer`（有待答追问）或 `extract_elements`。状态序列化到 Redis（`consult_state:{thread}`，TTL 1h）。

### 5. 法律 RAG 工具箱（对标医疗知识工具箱）

各 Agent 共享 `build_legal_tools`（`KnowledgeDeps` → `LegalDeps`），五类工具：

- `search_statutes`：法条向量 RAG（查询改写 + 可选 HyDE + 重排），命中《民法典》《消费者权益保护法》等条文
- `search_legal_graph`：GraphRAG（实体抽取 → NL2Cypher → LLM 综合），检索"法律—法条—案由—案例—机构"关系
- `search_cases`：类案检索（裁判文书向量库），返回相似案件及裁判要点
- `search_legal_multi`：法条 + 案例(+图谱) 并行融合 + 幻觉检查
- `search_legal_sql`：NL2SQL（**安全护栏**：仅 SELECT、拦截 DDL/DML 与敏感字段如当事人身份证/手机号、强制 LIMIT、超时）
- `find_remedy_channels`：按领域/地区返回维权渠道（热线/官网/邮箱/机构）

工具授权差异（对标医疗）：
- **公民法律助手**：法条 + 案例 + 图谱 + 融合 + 渠道（不含原始 SQL）
- **法律知识问答**：法条 + 图谱 + 融合（聚焦知识，不含个人数据 SQL）
- **专业法律助手**（预留）：全部工具 + 文书摘要
- **法考助手**（预留）：法条 + 图谱 + 题库工具

> 沿用医疗的**两套 RAG**思路：`src/agents/legal_knowledge/*`（生产）与 `src/rag/*`（可配置引擎，供实验与 TruLens 评估）。

## 四、目录结构（对标医疗，标注改造点）

```
legal-agent/
├── src/
│   ├── main.py                      # 沿用：FastAPI 应用工厂 + 健康检查
│   ├── agents/
│   │   ├── supervisor_agent.py      # 改造：法律版路由提示 + 四 Agent 意图边界
│   │   ├── tools/
│   │   │   ├── worker_tools.py       # 改造：call_citizen/legal_qa/professional/exam
│   │   │   └── store_tools.py        # 沿用：save_memory / search_memory
│   │   ├── workers/                 # 四个 Agent（本期 2 个，2 个占位）
│   │   │   ├── citizen_agent.py      # 新建：公民法律助手（对标 inquiry_agent）
│   │   │   ├── legal_qa_agent.py     # 新建：法律知识问答（对标 knowledge_agent）
│   │   │   ├── professional_agent.py # 占位：专业法律助手（对标 report/operation）
│   │   │   └── exam_agent.py         # 占位：法考助手
│   │   ├── consult/                 # 新建：公民咨询 LangGraph 状态机（对标 inquiry/）
│   │   │   ├── graph.py / state.py
│   │   │   ├── element_normalizer.py # 对标 symptom_normalizer
│   │   │   ├── confidence.py         # 领域分类置信度/收敛
│   │   │   └── neo4j_queries.py
│   │   └── legal_knowledge/         # 新建：法律 RAG 工具箱（对标 knowledge/）
│   │       ├── tools.py
│   │       ├── statute_rag.py / case_rag.py / graph_rag.py
│   │       ├── nl2sql.py / fusion.py
│   │       └── remedy_channels.py    # 新增：维权渠道工具
│   ├── core/                        # 沿用：config、logger、异常、base_model
│   ├── infra/                       # 沿用：PG / Milvus / Neo4j / MinIO / Redis 客户端
│   ├── rag/                         # 沿用：独立 RAG 引擎 + 评估
│   ├── api/routers/                 # 沿用+改造：chat.py、knowledge.py（→ legal.py）
│   └── modules/legal/model.py       # 新建：法律 ORM 模型（对标 medical/model.py）
├── scripts/                         # 改造：法律数据初始化 + RAG 实验 + Gradio 演示
├── data/raw/                        # 新建：法条/案例/渠道种子数据
├── data/eval/                       # 新建：法律 QA 评估集
├── alembic/                         # 沿用：数据库迁移
└── docker-compose.yml               # 沿用：基础设施栈
```

**可直接沿用（无需改造）**：`core/`（config/logger/异常/base_model/repository）、`infra/`（五大存储客户端）、`middlewares/`、`utils/`、`main.py` 主体、`docker-compose.yml`、Alembic 框架、`rag/` 引擎与评估、记忆体系（Redis 检查点 + Milvus 长期记忆）、Supervisor 的 Agent-as-Tool 骨架、Gradio 调试台框架。

**需要改造/新建**：Supervisor 路由提示、worker_tools 工具封装、四个 worker、咨询状态机（consult/）、法律 RAG 工具箱、法律 ORM 模型、数据初始化脚本、种子数据与评估集。

## 五、数据层设计（法条 + 案例 + 渠道）

### PostgreSQL 表（对标 medical 模型）

| 表 | 对应医疗表 | 说明 |
| --- | --- | --- |
| `laws` 法律法规 | diseases | 法律/法规/司法解释（名称、效力级别、发布/生效日期、制定机关） |
| `articles` 法条 | symptoms | 具体条文（所属法律、条号、正文），`law_articles` 关联 |
| `causes` 案由 | departments | 法律领域/案由分类（消费维权、合同、劳动…） |
| `cases` 案例 | drugs | 裁判文书（案号、法院、审级、案由、裁判要点、判决结果） |
| `case_details` | drug_details | 案例全文（1:1 拆分长文本） |
| `article_cases` | disease_drugs | 法条—案例引用关联 |
| `channels` 维权渠道 | —（新增） | 机构名、类型、覆盖领域、地区、官网、邮箱、电话 |
| `users` 用户 | patients | 用户（身份类型：群众/从业者/学生；敏感字段脱敏） |
| `consultations` 咨询记录 | consultations | 咨询会话与结论落库 |

### Milvus 向量集合

- `statute_index`：法条向量（条文语义检索）
- `case_index`：裁判文书向量（类案检索）
- `legal_docs`：法律文档 RAG 语料
- `agent_long_term_memory`：长期记忆

### Neo4j 图谱

- 节点：`Law` 法律、`Article` 法条、`Cause` 案由、`Case` 案例、`Channel` 机构/渠道、`Concept` 法律概念
- 关系：`HAS_ARTICLE`（法律→法条）、`APPLIES_TO`（法条→案由）、`CITED_BY`（法条→案例）、`BELONGS_TO`（案例→案由）、`REMEDY_VIA`（案由→渠道）、`RELATED_TO`（概念关联）

## 六、安全、合规与免责（法律领域强化）

- **免责声明**：所有输出须标注"本回答由 AI 生成，仅供参考，不构成正式法律意见，重大事项请咨询执业律师"。
- **幻觉防控（重中之重）**：法条与案号必须来源锚定，**严禁臆造**法律名称、条号、案号；`search_legal_multi` 对无来源支撑的结论追加警示。
- **紧急/安全分流**：涉及正在发生的犯罪、人身安全威胁 → 立即提示报警（110）。
- **NL2SQL 护栏**：仅 SELECT、拦截 DDL/DML、屏蔽当事人身份证/手机号等敏感字段、强制 LIMIT + 超时。
- **PII 脱敏**：案例与用户数据中的个人信息脱敏；专业助手输出提示注意隐私合规。
- **鉴权（部署前必补）**：医疗底座的鉴权脚手架（`jwt_utils`/`password_utils`/bcrypt）存在但未接入路由，法律项目正式部署前需接入 chat/legal 接口。
- **默认凭据**：`docker-compose` 默认账号密码仅限本地，生产前必须更换。

## 七、迁移改造清单（Roadmap）

**本期（Agent 1 + Agent 2）**：
1. 复用 `core/`、`infra/`、`main.py`、`docker-compose.yml`、Alembic、记忆体系
2. 改写 Supervisor 路由提示 + 四 Agent 意图边界；`worker_tools` 封装四个 `call_*_agent`
3. 新建 `workers/citizen_agent.py`、`workers/legal_qa_agent.py`；`professional_agent.py`、`exam_agent.py` 占位
4. 新建 `consult/` 咨询状态机（对标 inquiry）
5. 新建 `legal_knowledge/` RAG 工具箱（法条/案例/图谱/融合/渠道）
6. 新建 `modules/legal/model.py` + Alembic 迁移
7. 准备种子数据（法条/案例/渠道）与评估集，编写初始化脚本
8. 改造 Gradio 调试台为法律版

**后续（Agent 3 + Agent 4）**：
- 专业法律助手：文书摘要、案件分析、裁决预测（可接文书大模型）
- 法考助手：题库 ORM + 题库工具 + 知识点讲解

## 八、快速开始（沿用医疗）

```sh
# 1. 环境
conda create -n legal python=3.13
conda activate legal
pip install -r requirements.txt

# 2. 配置
cp .env.example .env   # 填写 DeepSeek / DashScope Key 及各数据库连接

# 3. 基础设施
docker compose up -d

# 4. 初始化数据（迁移在前）
alembic upgrade head
python scripts/init_postgres.py       # 法条 / 案例 / 渠道种子数据
python scripts/init_neo4j.py          # 法律知识图谱
python scripts/init_statute_index.py  # 法条向量索引

# 5. 启动
uvicorn src.main:app --port 8080 --reload
```
