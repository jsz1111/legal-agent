# 法律智能体项目说明书

## 1. 项目定位

本项目是面向普通市民和企业用户的法律信息与维权辅助系统。它把自然语言咨询分为两类：

- **具体纠纷与维权指引**：围绕一个案件收集事实、证据、时间、地区和诉求，给出有边界的行动方案。
- **法律知识问答**：查询法条、类案、知识图谱、办事渠道、专业文档和《中国法律年鉴》统计资料。

系统输出是信息和行动参考，不替代律师、法院、仲裁机构、行政机关或公安机关的正式判断。涉及现实危险时，安全分流优先于普通法律分析。

当前仓库同时保留两个客户端：

1. `scripts/gradio_chat_demo.py`：功能完整的 Gradio 测试台，作为人工验收和接口行为参考。
2. `frontend/`：新开发的桌面网页端，调用同一组 FastAPI 接口，不复制后端业务逻辑。

## 2. 当前目录

```text
legal-agent/
├─ src/
│  ├─ main.py                         # FastAPI 应用、生命周期、健康检查、CORS
│  ├─ api/routers/chat.py             # 对话、SSE、附件、文书、官方模板 API
│  ├─ api/routers/legal.py            # 法律模块健康占位 API
│  ├─ agents/supervisor_agent.py      # 总入口、意图路由、短期/长期记忆
│  ├─ agents/workers/guide_agent.py   # 维权 Worker 外层封装
│  ├─ agents/workers/legal_qa_agent.py# 法律问答 Worker
│  ├─ agents/legal_guide/              # GuideGraph、案件状态、追问、证据、文书
│  ├─ agents/legal_knowledge/          # 法条、类案、文档、图谱、统计 RAG
│  ├─ agents/tools/                    # Supervisor 工具、记忆、图片分析
│  ├─ infra/                           # PostgreSQL、Redis、Milvus、Neo4j、MinIO
│  └─ modules/legal/model.py            # 法律领域 SQLAlchemy 模型
├─ scripts/gradio_chat_demo.py         # 保留的 Gradio 功能验收客户端
├─ frontend/                            # 新桌面 React/Vite 前端
├─ data/                                # 追问题库、来源和法律统计数据
├─ resources/legal_document_templates/ # 官方空白文书及 manifest
├─ test/                                # 单元、工作流、接口回归测试
├─ start_dev.bat                        # 本地启动 FastAPI、Gradio、React
└─ 法律智能体项目说明书.md
```

## 3. 后端架构

### 3.1 FastAPI 入口

`src/main.py` 创建应用并注册：

- `LoggingMiddleware`：记录请求路径、状态码和耗时。
- `CORSMiddleware`：允许 `.env` 中 `FRONTEND_ORIGINS` 配置的来源。
- `/health`：应用进程健康检查，返回 `{"status":"ok"}`。
- `/health/deps`：检查 PostgreSQL、Redis、MinIO、Milvus、Neo4j，依赖异常时返回 `status=degraded` 与逐项错误。
- `/api/v1/chat`：聊天相关接口。
- `/api/v1/legal/health`：法律模块占位检查。

应用启动时尝试初始化 MinIO 桶、检查 Milvus 和 Neo4j；关闭时释放 Neo4j、Milvus 和 PostgreSQL 连接。

### 3.2 Supervisor 路由

`src/agents/supervisor_agent.py` 使用 LangChain Agent、Redis Checkpointer 和 Milvus Store：

- 具体纠纷、事件或维权诉求 -> `call_guide_agent`
- 法律概念、法条、流程和年鉴统计 -> `call_legal_qa_agent`
- 专业服务、法考和内部运营工具仍保留 Worker 入口，但当前属于建设中或内部能力
- `save_memory` / `search_memory` 用于长期记忆

路由层在每次请求中传入 `UserContext(user_id, session_id)`。`chat.py` 会优先从 Redis 读取 Worker 的公开回复，避免把 Supervisor 的工具轨迹、记忆保存结果或内部 JSON 泄露给用户。

## 4. 维权 GuideGraph

`src/agents/legal_guide/graph.py` 是案件中心的 LangGraph 状态机，状态模型为 `GuideState`，阶段为：

```text
CLARIFY -> ISSUE_SEARCH -> DETAIL_GATHER -> CONCLUDE -> END
```

实际节点和职责：

1. `prepare_turn`：加载历史上下文、推进轮次、识别“现在生成方案/继续补充”等控制意图。
2. `check_urgency`：每轮检查现实危险和时效风险；高危直接给出 110、12338、12348 等安全提示并暂停普通流程。
3. `extract_issues`：LLM 提取法律问题、原子事实、证据属性、地区和时间。
4. `clarify`：信息不足时一次只询问一个澄清问题。
5. `assess_retrieve`：构建证据覆盖报告、确定性置信档位和动态追问计划。
6. `ask_followup`：只展示一个高信息增益问题，并附回答提示与依据边界。
7. `parse_details`：解析回答、反问、否定、修正和附件观察，保留待答问题。
8. `conclude`：检索完成后生成理解、法律依据、类案、路径、胜算、行动清单和证据缺口。
9. `save_record`：向 PostgreSQL 保存可关联的咨询记录。

### 4.1 对话输出格式契约

- 法律知识问答使用标准 Markdown，先给 `## 核心结论`，再按问题需要组织适用规则、办理步骤、注意事项和依据来源。
- 案件追问由后端确定性模板输出“已记录 / 请确认 / 为什么要问”三个区块，一轮只问一个问题，并保留回答提示、追问依据和立即收敛入口。
- 最终方案保留稳定章节；并列条件、证据、路径、利弊和步骤必须使用列表。
- 标题单独成行且与正文之间保留空行，单个自然段原则上不超过四句话，不允许将多个独立要点压在一行。
- 前端与 Gradio 透传相同的 Markdown 文本；网页端使用 `react-markdown` 和 `remark-gfm` 渲染，不在浏览器端重写法律结论。

### 4.2 首轮材料包

前端和 Gradio 都可提交以下结构化字段，后端会以同一条普通聊天消息处理：

- 事情经过
- 对方及双方关系
- 时间、地点和金额
- 希望解决的结果
- 已经沟通或处理的情况

空字段不会被推测。用户也可以直接输入自然语言。

### 4.3 案件边界

已有案件状态继续存在时，后端会用语义分类判断新消息是：

- `continue`：继续、补充、纠正当前案件；
- `new`：另一组主体、事件或法律关系，创建隔离案件并归档旧状态；
- `uncertain`：先要求用户确认归属。

边界审计写入 `GuideState.case_boundary_audit`。案件状态默认在 Redis 长期保留，直到用户在前端手动删除；`GUIDE_SESSION_TTL=0` 表示不自动过期，部署方也可设置正整数秒数。生成文书与案件状态使用不同生命周期，DOCX 下载对象仍由 `GUIDE_DOCUMENT_TTL` 控制。

### 4.4 事实和证据边界

原子事实使用 `CaseFactUpdate`，必须包含当前用户消息中的 `source_text`。同一语义键支持 `add`、`replace`、`deny`，冲突记录不会被静默覆盖。

证据分为：

- 用户称持有；
- 系统实际收到的图片或文件副本；
- 明确缺失；
- 尚未核验的视觉或转述线索。

证据评估只描述可能证明目标、来源形式、完整性、主体/时间可见性和补强方向，不认定真实性、合法性、可采性或最终证明力。

### 4.5 置信度和收敛

置信度为确定性规则评分：

- 证据准备度 0.40；
- 事实清晰度 0.30；
- 权责清晰度 0.30。

阈值由配置决定，分为 `HIGH`、`MEDIUM`、`LOW`。所有档位都可以检索和输出，但档位越低，结论越保守。决策充分性会检查责任、请求范围、期限、管辖、程序、证据缺口和安全等维度。普通追问有软上限和硬上限；用户可用“继续补充”恢复同案追问，也可以用“现在生成方案”立即收敛。

## 5. 法律问答和检索

`src/agents/workers/legal_qa_agent.py` 可调用六类工具：

1. `search_statute`：Milvus `statute_index`，支持 Dense、BM25/RRF、HyDE、Reranker 和 PostgreSQL 字面兜底。
2. `search_similar_cases`：Milvus `case_index` + PostgreSQL 详情，按领域、案由和主题过滤；无命中时返回中国裁判文书网检索建议。
3. `search_legal_graph`：Neo4j 实体提取、NL2Cypher、失败重试和结果整合。
4. `search_channels`：PostgreSQL 确定性查询，按专属渠道、本地公共法律服务和政务兜底排序去重。
5. `search_legal_docs`：Milvus `knowledge_docs` 文档检索、HyDE 和精排。
6. `search_legal_statistics`：法律年鉴 ChatBI。

法条引用由 GuideGraph 的白名单校验限制在本轮检索结果；类案只能来自结构化检索结果，不能无命中编造。

## 6. 法律统计 ChatBI

统计查询独立使用 `LEGAL_STATISTICS_DATABASE_URL`：

```text
自然语言问题
 -> 目录候选检索
 -> LLM 生成 SQL
 -> SELECT / schema / 表名 / 单语句 / 行数白名单校验
 -> 只读事务 + 10 秒超时执行
 -> 确定性答案
 -> line/bar/pie/scatter/heatmap/table 图表推荐
 -> 来源、质量标记、SQL 和原始行返回
```

返回模型 `LegalStatisticsChatBIResult` 包含：

- `question`、`answer`、`summary`
- `sql`
- `columns`、`rows`
- `chart`：类型、坐标、系列和 ECharts 配置
- `sources`：数据集、机构、年份和质量标记

连续统计追问通过 Redis 的 `legal_statistics_context:{user_id}:{session_id}` 复用上一条已通过校验的 SQL 条件。

## 7. 附件和多模态

### 7.1 文档

`attachment_parser.extract_document_bytes` 支持 PDF、DOCX、TXT：

- 单文件最多 10 MB；
- PDF 最多读取前 30 页；
- 单次提取最多 8,000 字；
- 返回 SHA-256、预览、来源形式、扫描件提示和带指纹的证据块；
- 后端不保留上传文档二进制。

### 7.2 图片

`POST /api/v1/chat/upload-image` 依赖 `ENABLE_MULTIMODAL=true` 和 `VL_API_KEY`：

- 校验真实图片格式、大小和像素；
- 使用当前案件领域、事实、证据和最近助手消息生成上下文提示；
- 返回识别文本、图片元数据、SHA-256、是否结合上下文、是否自动注入；
- 默认处理后删除临时图片，除非 `MULTIMODAL_RETAIN_UPLOADS=true`；
- 单独图片没有案件上下文时只返回识别结果，不强行创建案件。

## 8. 文书和官方模板

`POST /api/v1/chat` 中发送“生成文书”等控制语句时，若当前案件已有可用事实，路由层直接进入独立文书服务，不把控制语句写入案情消息。

文书生成特点：

- 文书类型按领域默认，也支持明确指定劳动仲裁申请书、民事起诉状、投诉信、催告函等；
- 只使用 `draftable_facts`，冲突、推测和未核验视觉结果使用占位符；
- 经过 LLM 审校和确定性事实/法律兜底；
- 生成可编辑 DOCX，文书下载对象在 Redis 保存 24 小时；
- 可同时匹配精确或相关的官方空白 PDF 模板；
- 官方模板元数据包括发布机关、文号、日期、来源页面、页码和 SHA-256。

## 9. 实际 API 契约

API 基础地址默认 `http://127.0.0.1:8085`。

### 9.1 非流式聊天

`POST /api/v1/chat`

请求：

```json
{
  "user_id": "web-user-uuid",
  "session_id": "session-uuid",
  "message": "用户原始消息或首次案件材料包"
}
```

响应：

```json
{
  "reply": "仅供用户阅读的公开回复",
  "session_id": "session-uuid",
  "debug": {
    "case_id": "",
    "case_boundary_status": "",
    "domain": "consumer_market",
    "confidence_tier": "MEDIUM",
    "statute_hits": "",
    "case_hits": "",
    "graph_laws": [],
    "graph_channels": [],
    "fallback_guide": null
  },
  "statistics": null,
  "document": null
}
```

### 9.2 SSE 流式聊天

`POST /api/v1/chat/stream` 使用相同请求体，响应为 `text/event-stream`：

```text
data: {"type":"token","content":"..."}

data: {"type":"done","session_id":"...","debug":{},"statistics":{},"document":{}}

data: {"type":"error","message":"..."}
```

当前 GuideGraph 和 Supervisor 都以一条完整公开回复发送一个 `token`，再发送 `done`；前端必须按 SSE 事件解析，不能把原始 `data:` 行直接显示。

### 9.3 其他接口

- `GET /health`
- `GET /health/deps`
- `GET /api/v1/legal/health`
- `GET /api/v1/chat/document-templates`
- `GET /api/v1/chat/document-templates/{template_id}/official`
- `GET /api/v1/chat/documents/{document_id}`
- `DELETE /api/v1/chat/conversations/{session_id}`：请求体提供 `user_id`，手动删除案件状态、会话检查点、统计上下文、相关临时文书和案件长期记忆
- `POST /api/v1/chat/upload-document`：multipart `file`
- `POST /api/v1/chat/upload-image`：multipart `file`、`user_id`、`session_id`、可选 `question`、`auto_inject`

## 10. 数据存储

| 存储 | 当前用途 |
| --- | --- |
| PostgreSQL | 法律、法条、案例、渠道、权威来源、追问引用、用户和咨询记录 |
| Redis | Supervisor Checkpointer、GuideState、活动标记、统计上下文、短期 Worker 回复、文书二进制 |
| Milvus | 法条、案例、法律术语、专业文档、权威依据和长期记忆向量 |
| Neo4j | 法律、领域、概念和渠道关系图 |
| MinIO | 启动时检查知识文档桶；当前聊天附件默认不持久化 |

## 11. 配置和启动

关键环境变量见 `.env.example` 和 `src/core/config.py`：

- `DASHSCOPE_API_KEY` / `DEEPSEEK_API_KEY` / `DEEPSEEK_MODEL`
- `DB_*`、`REDIS_*`
- `MILVUS_*`、`NEO4J_*`、`MINIO_*`
- `ENABLE_MULTIMODAL`、`VL_API_KEY`
- `FRONTEND_ORIGINS`
- `GUIDE_*` 超时、轮次、案件保留策略和文书 TTL

推荐使用：

```bat
start_dev.bat
```

它会启动：

- FastAPI：`http://127.0.0.1:8085`
- Gradio：`http://127.0.0.1:7864`
- React 桌面前端：`http://127.0.0.1:5173`

也可以分开启动：

```bat
D:\develop\Miniconda\envs\legal\python.exe -m uvicorn src.main:app --host 127.0.0.1 --port 8085 --reload
set LEGAL_AGENT_API_BASE=http://127.0.0.1:8085
D:\develop\Miniconda\envs\legal\python.exe scripts/gradio_chat_demo.py
cd frontend
npm install
npm run dev -- --host 127.0.0.1 --port 5173
```

## 12. 测试和边界

测试覆盖：

- GuideGraph 路由、澄清、动态追问、证据和收敛；
- 高危、安全恢复、时效提示和跨案件边界；
- 原子事实来源、冲突和文书事实守卫；
- 法条、类案、统计 ChatBI 和 SQL 安全；
- 图片校验、多模态和附件解析；
- SSE `UserContext`、Worker 公开回复过滤和文书路由；
- 官方模板文件、哈希和 DOCX 可编辑性。

前端只消费公开 API，不读取 Redis、数据库或内部 Agent 状态。Gradio 不被新前端替换，作为同一后端的对照客户端和回归入口。

## 13. 隐私与安全边界

- 前端生成匿名 `user_id` 和 `session_id`，不展示内部 ID。
- 上传前提示遮挡与案件无关的身份证号、银行卡号和详细住址。
- 后端对文档不留存，对图片默认删除临时副本。
- 视觉识别和文档提取结果必须与原文件核对，不能直接视为已确认事实。
- 任何建议不构成责任、违法、犯罪、胜诉或赔偿结果的确定认定。
- 当前危险优先提示 110 等紧急渠道；重要期限、刑事和重大纠纷应转专业人员。
