# 法律智能体项目说明书

> 文档版本：2026-08-02（依据当前仓库工作区代码、测试与数据状态重写）
> 项目目录：`D:\learn\legal-agent`
> 前端产品名：法护通（桌面网页端）

## 1. 项目定位

本项目是面向普通市民和企业用户的法律信息与维权辅助系统。用户用自然语言描述纠纷或提问，系统将其分为两类任务分别处理：

- **具体纠纷与维权指引**：围绕一个案件收集事实、证据、时间、地区和诉求，给出有边界的行动方案与参考文书。
- **法律知识问答**：查询法条、类案、知识图谱、办事渠道、专业文档和《中国法律年鉴》统计资料。

系统输出是信息和行动参考，不替代律师、法院、仲裁机构、行政机关或公安机关的正式判断。涉及人身安全或紧迫时效风险时，安全分流优先于普通法律分析。

### 1.1 建设目标

- 把法律信息检索、事实梳理、证据整理、渠道推荐和参考文书生成组织为可复用的工程流程；
- 通过结构化案件状态（`GuideState`）保证多轮对话的事实、证据、追问和方案连续性；
- 通过显式服务模式（问答/案件）和 Supervisor 首轮识别实现准确的意图路由；
- 提供可核验的检索来源，不编造法条、类案和统计数字。

### 1.2 非目标与责任边界

- 不提供个案裁判预测、代理服务或最终处理结论；
- 不替代律师、仲裁、法院、行政机关和公安机关；
- 图片/文档识别结果属于待核验线索，不自动认定为已确认事实；
- 证据评估只做材料准备度和证明目标覆盖的保守检查，不认定真实性、合法性、可采性或最终证明力；
- 当前危险情形优先提示 110、12348 等紧急渠道。

## 2. 系统总体架构

```text
前端（法护通 React/Vite）  +  Gradio 验收台
            |                        |
            +-----------> FastAPI Chat API (8085)
                                |
                                +-- mode=case / 已进行案件 --> GuideGraph（维权 Agent）
                                |
                                +-- mode=qa / 已进行问答 ----> 法律问答 Agent
                                |
                                `-- mode=auto 的未分类首轮 --> Supervisor（意图识别与路由）
                                                          |
                                                          +-- 具体纠纷 --> Guide Worker
                                                          +-- 法律知识 --> Legal QA Worker
                                                          +-- 专业服务 --> Professional Worker（建设中）
                                                          +-- 法考助手 --> Exam Worker（建设中）
                                                          `-- 运营数据 --> Operation Worker（内部）

检索与存储层：PostgreSQL | Redis | Milvus | Neo4j | MinIO
```

核心设计：

- **显式模式路由**：前端明确选择“法律问答”或“案件维权”后，请求携带 `mode=qa|case` 直接进入对应 Worker；未选择时 `mode=auto` 才由 Supervisor 在首轮识别。类型确定后写入服务端会话模式，同一 `session_id` 不允许在问答历史与案件事实库之间静默切换。
- **公开回复透传**：Worker 的公开回复写入 Redis 短 TTL 键，路由层优先读取并返回，避免泄露工具轨迹、记忆保存结果或内部 JSON。
- **状态持久化**：`GuideState` 存 Redis；咨询记录存 PostgreSQL；检查点存 Redis；长期记忆存 Milvus。
- **多会话并发**：网页端支持跨会话并发，进度和结果按 `session_id` 写回原记录；同一会话串行执行，避免结构化案件状态被覆盖。

## 3. 后端服务与多智能体

### 3.1 FastAPI 入口（`src/main.py`）

- `LoggingMiddleware`：记录请求路径、状态码与耗时；
- `CORSMiddleware`：允许 `FRONTEND_ORIGINS` 配置的来源；
- `GET /health`：进程健康检查；
- `GET /health/deps`：逐项检查 PostgreSQL、Redis、MinIO、Milvus、Neo4j，异常时返回 `degraded`；
- 生命周期：启动时初始化 MinIO 桶、检查 Milvus/Neo4j，关闭时释放连接池。

### 3.2 Supervisor（`src/agents/supervisor_agent.py`）

使用 LangChain `create_agent`，以 Redis `AsyncRedisSaver` 作为短期记忆检查点、`MilvusStore` 作为长期记忆：

| 工具 | 用途 |
| --- | --- |
| `call_guide_agent` | 具体纠纷、事件或维权诉求，进入 GuideGraph |
| `call_legal_qa_agent` | 法律概念、法条、流程、渠道和法律统计 |
| `call_professional_agent` | 专业法律服务（建设中） |
| `call_exam_agent` | 法考助手（建设中） |
| `call_operation_agent` | 平台运营数据查询（仅限内部） |
| `save_memory` / `search_memory` | 长期记忆写入与检索 |

路由层在每次请求中传入 `UserContext(user_id, session_id)`。会话总结使用 `SummarizationMiddleware`，token 达 4000 或消息数达 6 条时触发压缩。Supervisor 只负责 `mode=auto` 的首轮识别，明确模式或已锁定会话不再经过 Supervisor 重写。

## 4. 会话模式路由

`src/api/routers/chat.py` 实现三种模式：

- `case`：始终进入同一案件状态机，不创建普通问答上下文；
- `qa`：只运行法律问答 Worker，不创建 `GuideState`、事实库或证据清单；
- `auto`：仅首轮由 Supervisor 识别，识别成功后返回 `resolved_mode` 并锁定。

服务端以 `conversation_mode:{user_id}:{session_id}` 保存已确定的模式。后续请求即使传入冲突模式，也以服务端保存的类型为准，防止事实库与问答上下文互相污染。`ChatRequest` 同时支持结构化操作：

| 字段 | 说明 |
| --- | --- |
| `mode` | `auto` / `qa` / `case`，默认 `auto` |
| `action` | `message`（默认）/ `submit_evidence` / `regenerate_solution` |
| `target_case_id` | 结构化操作绑定的案件 ID，不一致时返回 409 |
| `regenerate_solution` | 请求按最新事实与证据重新生成方案 |
| `evidence_requirement_ids` | 证据提交时关联的证据清单项 ID |

`submit_evidence` 会校验消息包含系统生成的文件指纹（SHA-256）且清单项未过期；`regenerate_solution` 以 `CONCLUDE_NOW` 控制意图直接收束并按新证据版本更新方案。

## 5. 维权 GuideGraph（当前实现）

`src/agents/legal_guide/graph.py` 是案件中心的 LangGraph 状态机，状态模型为 `GuideState`，阶段为：

```text
CLARIFY -> ISSUE_SEARCH -> DETAIL_GATHER -> CONCLUDE -> END
```

### 5.1 九节点工作流

```text
prepare_turn -> check_urgency -> extract_issues
                                      |        |
                                      |        +-> clarify -> END
                                      v
                              assess_retrieve
                                  |        |
                                  |        +-> ask_followup -> END
                                  v
                              conclude -> save_record -> END

用户回答追问后：prepare_turn -> check_urgency -> parse_details
                                                   |          |
                                                   +----------+-> extract_issues / assess_retrieve
```

| 节点 | 职责 |
| --- | --- |
| `prepare_turn` | 加载历史上下文、推进轮次、识别“现在生成方案/继续补充/生成文书”等控制意图 |
| `check_urgency` | 每轮检查现实危险和时效风险；高危直接输出 110、12338、12348 等安全提示并暂停普通流程；识别高确定性诈骗表述并输出一次性止付/冻结/报警提示 |
| `extract_issues` | 三层标准化提取法律问题、原子事实、证据属性、地区和时间 |
| `clarify` | 信息不足时一次只询问一个澄清问题（上限 2 轮） |
| `assess_retrieve` | 证据覆盖评估、置信度打分、决策充分性检查；调用 `node_retrieve_followup_basis` 做追问阶段轻量检索，仅在收敛时执行完整检索 |
| `ask_followup` | 每轮展示 2–5 个高信息增益问题的动态表单（可混短文本/长文本/单选/多选）；确只剩一个关键缺口时才问一个 |
| `parse_details` | 解析表单答案或自然语言回答、反问、否定、修正和附件观察，保留待答问题 |
| `conclude` | 完整检索（法条、类案、图谱、渠道）后生成理解、法律依据、路径、胜算、行动清单和证据缺口，并按证据评估版本输出“证据作用与缺口” |
| `save_record` | 向 PostgreSQL 保存可关联的咨询记录 |

内部支撑节点：`node_score`（置信度打分）、`node_retrieve_followup_basis`（追问依据轻量检索，不含类案）、`node_retrieve`（收敛时完整检索）。检索快照按案情指纹复用，避免无实质变化时重复检索。

### 5.2 动态追问

追问不是固定问卷：

- 首轮及后续回答先归入原子事实细节库，再针对未解决的责任、请求、时效、管辖和程序问题生成 2–5 个高信息增益问题；
- 追问阶段执行轻量法条 + 知识图谱检索（明确不含类案），依据写入 `followup_basis_refs` / `followup_basis_graph`，前端作为“为什么要问”的来源；
- 问题可混合 `short_text`、`long_text`、`single_choice`、`multi_choice` 类型，每项可回答“不清楚/无法确认”；
- 已确认、已否认、明确不知道或已回答的决策点不会重复追问；预设目录只作为模型或检索失败时的安全兜底；
- 表单只提交实际填写字段并附稳定问题 ID；用户也可直接聊天框自然回答。

### 5.3 证据中心与版本化

证据需求随事实变化增量维护：

- 证据准备清单 `evidence_requirements`：稳定 ID、标签、证明目标、状态、替代材料、质量缺口、触发事实和依据引用；
- 清单版本 `evidence_requirement_version`：每次事实或依据变化后递增；
- 评估版本 `evidence_evaluation_version`：每次收到附件后递增，重复提交也形成新评估版本；
- 方案版本 `solution_version` 与 `solution_evidence_version`：记录方案更新次数及其已纳入的证据评估版本；
- 用户可统一上传多份 PDF、DOCX、TXT 或图片，也可把文件绑定到某个清单项（`evidence_requirement_ids`）；
- 上传后完成单份材料与整体证明目标覆盖评估；此后才执行包含类案在内的完整检索并生成方案；
- 方案生成后证据中心继续保留；证据评估版本高于方案版本时提示用户按最新证据更新方案。

### 5.4 对话输出格式契约

- 法律知识问答使用标准 Markdown，先给 `## 核心结论`，再按需组织适用规则、办理步骤、注意事项和依据来源；
- 案件追问以“已记录 / 请补充目前尚未明确的信息 / 为什么要问”结构展示，核心问题用引用块突出；
- 最终方案保留稳定章节；并列条件、证据、路径、利弊和步骤使用列表；
- 标题单独成行且与正文保留空行；单个自然段原则上不超过四句话；
- 前端与 Gradio 透传相同的 Markdown 文本，不在浏览器端重写法律结论。

### 5.5 案件边界

已有案件状态存在时，后端用语义分类判断新消息归属：

- `continue`：继续、补充、纠正当前案件；
- `new`：另一组主体/事件/法律关系，创建隔离案件并归档旧状态（`guide_case_archive:*`）；
- `uncertain`：先要求用户确认归属（`GUIDE_CASE_BOUNDARY_CONFIDENCE=0.72` 以下不继承旧案）。

边界审计写入 `GuideState.case_boundary_audit`。案件状态默认长期保留（`GUIDE_SESSION_TTL=0`），由用户手动删除；生成文书使用独立的短期下载 TTL。

### 5.6 事实与证据边界

- 原子事实使用 `CaseFactUpdate`，必须带当前消息 `source_text`；支持 `add`、`replace`、`deny`，冲突不会被静默覆盖；
- 证据分为：用户称持有、系统实际收到的副本、明确缺失、尚未核验的视觉/转述线索；
- 证据评估只描述可能证明目标、来源形式、完整性、主体/时间可见性和补强方向，不认定真实性、合法性、可采性或证明力；
- 只有 `draftable_facts`（用户清晰陈述、无冲突、已核验）可进入文书生成。

### 5.7 置信度与收敛

置信度为确定性规则评分（零额外 LLM 调用）：

- 证据准备度 0.40；
- 事实清晰度 0.30；
- 权责清晰度 0.30。

分档阈值 `HIGH ≥ 0.65`、`MID ≥ 0.50`，输出 `HIGH` / `MEDIUM` / `LOW`，档位越低结论越保守。轮次边界：澄清 2 轮、普通追问软上限 3 轮、硬上限 6 轮、用户明确继续后 8 轮、事实/证据各 3 轮、连续低信息回答 3 次、连续反问 3 次、总轮次保险 20 轮。用户可随时用“现在生成方案”按当前信息收敛。

## 6. 法律知识问答与检索

`src/agents/legal_knowledge/` 提供六类工具：

| 工具 | 数据源 | 说明 |
| --- | --- | --- |
| `search_statute` | Milvus `statute_index` + PostgreSQL | 查询改写 + Dense/BM25/RRF/HyDE/Reranker，PostgreSQL 字面兜底 |
| `search_similar_cases` | Milvus `case_index` + PostgreSQL | 按领域、案由、主题过滤；无命中时给中国裁判文书网检索建议 |
| `search_legal_graph` | Neo4j | 实体提取、NL2Cypher、失败重试和结果整合 |
| `search_channels` | PostgreSQL | 确定性查询，按专属渠道、本地公共法律服务和政务兜底排序去重 |
| `search_legal_docs` | Milvus `knowledge_docs` | 专业文书/文档检索、HyDE 和精排 |
| `search_legal_statistics` | 独立统计库 | 法律年鉴 ChatBI（见下节） |

查询进入工具前可进行法律术语改写；检索调用记录用户、查询、工具、索引、耗时和结果摘要，用于审计和运营分析。法条引用由白名单校验限制在本轮检索结果；类案只能来自结构化检索结果，不能无命中编造。

## 7. 法律统计 ChatBI（NL2SQL）

统计查询独立使用 `LEGAL_STATISTICS_DATABASE_URL`（`legal_statistics_db`）：

```text
自然语言问题
 -> 目录候选检索
 -> LLM 生成 SQL
 -> SELECT / schema / 表名 / 单语句 / 行数白名单校验
 -> 只读事务 + 10 秒超时执行
 -> 确定性答案
 -> line/bar/pie/scatter/heatmap/table 图表推荐
 -> 来源、质量标记、SQL 和原始行返回
```

返回模型 `LegalStatisticsChatBIResult` 包含：`question`、`answer`、`summary`、`sql`、`columns/rows`、`chart` 和 `sources`。连续统计追问通过 Redis `legal_statistics_context:{user_id}:{session_id}` 复用上一条已通过校验的 SQL 条件；用户要求“再加指标/同图对比”时，系统将新旧指标一并重新查询。

## 8. 附件与多模态

### 8.1 文档

`attachment_parser.extract_document_bytes` 支持 PDF、DOCX、TXT：

- 单文件最多 10 MB；PDF 最多读取前 30 页；单次提取最多 8,000 字；
- 返回 SHA-256、预览、来源形式、扫描件提示和带指纹的证据块；
- 后端不保留上传文档二进制。

### 8.2 图片

`POST /api/v1/chat/upload-image` 依赖 `ENABLE_MULTIMODAL=true` 与 `VL_API_KEY`：

- 校验真实图片格式（JPG/PNG/WEBP/GIF/BMP）、大小和像素；
- 使用当前案件领域、事实、证据和最近助手消息生成上下文提示；
- 视觉提示要求只提取客观可见信息，隐藏身份证号、银行卡号、手机号和住址的非必要部分；
- 返回识别文本、图片元数据、SHA-256、是否结合上下文、是否自动注入；
- 默认处理后删除临时图片，除非 `MULTIMODAL_RETAIN_UPLOADS=true`；
- 单独图片无案件上下文时只返回识别结果，不强行创建案件。

## 9. 文书与官方模板

对话中发送“生成文书”等控制语句时，若当前案件已有可用事实，路由层直接进入独立文书服务，不把控制语句写入案情消息：

- 文书类型按领域默认，也支持明确指定劳动仲裁申请书、民事起诉状、投诉信、催告函等；
- 只使用 `draftable_facts`，冲突、推测和未核验视觉结果使用占位符；
- 经过 LLM 审校和确定性事实/法律兜底；
- 生成可编辑 DOCX（python-docx），下载对象在 Redis 保存 24 小时（`GUIDE_DOCUMENT_TTL`）；
- 可同时匹配精确或相关的官方空白 PDF 模板，返回 `official_template_match`（exact/related/none）；
- 官方模板元数据（`resources/legal_document_templates/manifest.json`）包含发布机关、文号、日期、来源页面、页码和 SHA-256；
- 文书指令与生成正文不写入案情消息池，防止被事实抽取器误当成本案陈述。

## 10. 前端（法护通桌面网页端）

`frontend/` 为 React 18 + TypeScript + Vite 桌面网页端，设计说明书见 `frontend/README.md`：

- 双工作模式：**法律问答**与**案件维权**，首次进入显示两张服务能力卡，可显式选择或直接描述让 Supervisor 首轮识别；
- 三栏布局：左侧案件导航、中央对话工作区、右侧资料检查器（案情台账 / 检索依据 / 文书模板）；
- 使用 `/api/v1/chat/stream` 处理全部对话，按 SSE 事件解析；`progress` 事件展示风险检查、事实整理、法条检索、证据评估和方案生成等可公开阶段，不展示思维链；
- 动态追问表单（`FollowupFormCard`）与证据中心（`EvidenceCenterCard`）支持收起、缩小、放大和拖动，提交后旧面板自动隐藏；
- 会话状态按 `session_id` 独立维护，支持多会话并发；处理中的会话显示“后台处理中”，SSE 回调写回发起时捕获的会话；
- `react-markdown` + `remark-gfm` 渲染后端 Markdown；Recharts 渲染统计图表，热力图用 CSS 网格呈现；
- 界面不显示 API 地址、数据库名、内部 ID；附件失败保留明确错误，不静默丢弃。

## 11. Gradio 验收台

`scripts/gradio_chat_demo.py` 是功能完整的 Gradio 测试台（端口 7864），作为人工验收与接口行为参考，展示对话、图片证据、统计图表、检索详情和文书下载入口。Gradio 不被新前端替换，作为同一后端的对照客户端和回归入口。

## 12. 数据与知识库

### 12.1 存储层

| 存储 | 当前用途 |
| --- | --- |
| PostgreSQL（5433） | 法律、法条、案例、渠道、权威来源、追问引用、用户与咨询记录 |
| Redis Stack（6380） | Supervisor Checkpointer、GuideState、活动标记、会话模式、统计上下文、Worker 短回复、文书二进制 |
| Milvus（19531） | `statute_index`、`case_index`、`legal_term_index`、`authority_basis_index`、`knowledge_docs`、长期记忆 |
| Neo4j（7688/7475） | 法律、领域、概念和渠道关系图（Law / Article / LegalConcept / Channel） |
| MinIO（9010/9011） | 知识文档桶 `knowledge-docs`；聊天附件默认不持久化 |

### 12.2 主要数据表

`src/modules/legal/model.py`（Alembic 迁移见 `alembic/versions/`）：

- `laws` / `articles`：法律法规与法条；
- `legal_cases`：类案库；
- `channels`：维权渠道（热线/网站/APP，含地区代码与服务层级）；
- `authority_sources` / `authority_versions` / `followup_rule_citations`：权威来源、版本和追问依据引用；
- `users` / `consultations`：用户与咨询记录。

当前运行实例规模（2026-07-31 盘点，详见 `data/sources/README.md`）：75 部法律、7,105 条法条、983 条案例、18 个权威来源、13 个领域追问目录；Milvus `statute_index` 7,077、`case_index` 752、`legal_term_index` 13,268、`authority_basis_index` 115。

### 12.3 数据与资源

- `data/sources/`：统一来源目录（`cail2019_scm` 案例语料、`case_pilot` 试点案例、`formal` 官方材料、`legal_statistics` 年鉴统计、`practical` 实务材料）及来源索引说明书；
- `data/legal_guide/`：`followup_catalog.json` 追问规则目录、`authority_registry_export.json` 权威依据注册表；
- `data/channels_pilot.json`、`data/channels_supplement.json`：渠道数据；
- `resources/legal_document_templates/`：官方空白文书 PDF、来源页面快照与 manifest；
- `models/`：BM25 稀疏检索模型文件；
- `database/`：案例库与法律统计数据库结构（`.sql`）。

## 13. API 契约

API 基础地址默认 `http://127.0.0.1:8085`。

### 13.1 非流式聊天

`POST /api/v1/chat`

```json
{
  user_id: web-user-uuid,
  session_id: session-uuid,
  message: 用户原始消息或首次案件材料包,
  mode: case,
  action: message
}
```

```json
{
  reply: 仅供用户阅读的公开回复,
  session_id: session-uuid,
  resolved_mode: case,
  mode_locked: true,
  debug: {
    case_id: ",
    domain: consumer_market,
    confidence_tier: MEDIUM,
    followup_basis_refs: [],
    followup_form: null,
    evidence_checklist: [],
    evidence_requirement_version: 0,
    evidence_evaluation_version: 0,
    solution_version: 0,
    convergence: null
  },
  statistics: null,
  document: null
}
```

### 13.2 SSE 流式聊天

`POST /api/v1/chat/stream`，响应 `text/event-stream`：

```text
data: {type:progress,stage:routing,label:正在建立案件档案,detail:...,status:active}

data: {type:progress,stage:followup_retrieval,label:正在检索追问依据,detail:...,status:completed}

data: {type:token,content:...}

data: {type:done,session_id:...,resolved_mode:case,mode_locked:true,debug:{},statistics:{},document:{}}

data: {type:error,message:...}
```

`debug` 字段（`guide_debug_payload`，见 `src/agents/legal_guide/debug_view.py`）包含：`case_id`、`case_boundary_status`、`domain`、`confidence_tier`、`statute_hits`、`case_hits`、`graph_laws`、`graph_channels`、`followup_basis_refs`、`followup_basis_error`、`fallback_guide`、`detail_store`、`followup_form`（kind/questions/planner_mode）、`evidence_checklist`、各版本号与 `convergence`。

### 13.3 其他接口

| 方法 | 路径 | 功能 |
| --- | --- | --- |
| `GET` | `/health` | 进程健康检查 |
| `GET` | `/health/deps` | 依赖健康检查 |
| `GET` | `/api/v1/legal/health` | 法律模块占位检查 |
| `GET` | `/api/v1/chat/document-templates` | 官方空白模板目录 |
| `GET` | `/api/v1/chat/document-templates/{template_id}/official` | 下载官方空白模板 PDF |
| `GET` | `/api/v1/chat/documents/{document_id}` | 下载生成的 DOCX |
| `DELETE` | `/api/v1/chat/conversations/{session_id}` | 删除案件状态、检查点、会话模式、统计上下文、归档、临时文书与案件记忆 |
| `POST` | `/api/v1/chat/upload-document` | multipart 文档上传解析 |
| `POST` | `/api/v1/chat/upload-image` | multipart 图片上传分析（可选自动注入） |

## 14. 配置与启动

关键环境变量见 `.env.example` 与 `src/core/config.py`：

- `DASHSCOPE_API_KEY` / `DEEPSEEK_API_KEY` / `CHAT_MODEL` / `DEEPSEEK_MODEL`；
- `DB_*`、`REDIS_*`、`MILVUS_*`、`NEO4J_*`、`MINIO_*`；
- `EMBEDDING_PROVIDER`（ollama / dashscope / volcengine）与 `EMBEDDING_MODEL`；
- `ENABLE_MULTIMODAL`、`VL_API_KEY`、`VL_MODEL`、`MULTIMODAL_*`；
- `MINERU_API_URL` 等文档解析配置；
- `GUIDE_*`：置信度阈值、轮次上限、动态表单批量（`GUIDE_FOLLOWUP_BATCH_MIN=2` / `MAX=5`）、案件边界置信度、各阶段超时、案件保留策略和文书 TTL；
- `FRONTEND_ORIGINS`。

推荐启动方式：

```bat
start_dev.bat
```

它分别启动：

- FastAPI：`http://127.0.0.1:8085`
- Gradio 验收台：`http://127.0.0.1:7864`
- React 桌面前端：`http://127.0.0.1:5173`

也可分开启动：

```bat
D:\develop\Miniconda\envs\legal\python.exe -m uvicorn src.main:app --host 127.0.0.1 --port 8085 --reload
set LEGAL_AGENT_API_BASE=http://127.0.0.1:8085
D:\develop\Miniconda\envs\legal\python.exe scripts/gradio_chat_demo.py
cd frontend
npm install
npm run dev -- --host 127.0.0.1 --port 5173
```

基础设施与初始化：

```powershell
docker compose up -d
alembic upgrade head
python scripts/init_legal_postgres.py
python scripts/init_legal_neo4j.py
python scripts/init_milvus_indexes.py
python scripts/load_legal_statistics_postgres.py
```

## 15. 测试与验收

测试统一放在 `test/`（31 个 `test_*.py` 文件），运行：

```powershell
python -m pytest test -q
```

覆盖范围：

- GuideGraph 路由、澄清、动态追问表单、证据和收敛；
- 会话模式路由（qa/case/auto）、模式锁定与隔离；
- 高危、安全恢复、时效提示、欺诈止损和跨案件边界；
- 原子事实来源、冲突、文书事实守卫和动态追问批次；
- 法条接地、类案检索守卫、统计 ChatBI 和 SQL 安全；
- 图片校验、多模态、附件解析和 LLM 运行时超时；
- SSE `UserContext`、Worker 公开回复过滤、证据并发和文书路由；
- 官方模板文件、哈希和 DOCX 可编辑性；
- 对话保留、删除会话与记忆上下文。

另有验收执行器：`test/evaluate_guide_scenarios.py`（真实环境多场景验收）和 `test/run_evidence_concurrency.py`（五案件并发证据评估），测试物料见 `test/materials/`（脱敏重构，非真实证据）。

前端验收：`npm run build`（tsc + vite build）通过；双模式会话、服务选择、动态表单、证据中心、SSE 进度、统计图表、文书下载、刷新恢复与多会话并发等场景按 `frontend/README.md` 验收。

## 16. 安全、隐私与合规边界

- 前端生成匿名 `user_id` 与 `session_id`，不展示内部 ID；
- 上传前提示遮挡与案件无关的身份证号、银行卡号和详细住址；
- 后端对文档不留存，对图片默认删除临时副本；
- 视觉识别和文档提取结果必须与原文件核对，不能直接视为已确认事实；
- 法律统计仅用于数据分析，不作为法律依据或个案裁判结论；
- 任何建议不构成责任、违法、犯罪、胜诉或赔偿结果的确定认定；
- 当前危险优先提示 110 等紧急渠道；高确定性诈骗场景输出止付、冻结、报警提示；
- 重要期限、刑事和重大纠纷应转专业人员；
- 运营数据查询仅限内部运营人员，法律年鉴统计不属于平台运营数据。

## 17. 目录总览

```text
legal-agent/
├─ src/
│  ├─ main.py                        # FastAPI 应用、生命周期、健康检查、CORS
│  ├─ api/routers/chat.py            # 对话、SSE 进度、模式路由、附件、文书、模板 API
│  ├─ api/routers/legal.py           # 法律模块健康占位 API
│  ├─ agents/supervisor_agent.py     # 首轮意图路由、短期/长期记忆
│  ├─ agents/workers/                # guide / legal_qa / professional / exam / operation
│  ├─ agents/legal_guide/            # GuideGraph、状态、追问表单、证据清单、文书、进度与调试视图
│  ├─ agents/legal_knowledge/        # 法条、类案、图谱、渠道、文档、统计 RAG
│  ├─ agents/tools/                  # Supervisor 工具、记忆、图片分析
│  ├─ infra/                         # PostgreSQL、Redis、Milvus、Neo4j、MinIO
│  ├─ core/                          # 配置、日志、异常、基础模型
│  └─ modules/legal/model.py         # 法律领域 SQLAlchemy 模型
├─ frontend/                         # 法护通桌面 React/Vite 前端（README 为设计说明书）
├─ scripts/                          # Gradio 验收台、数据初始化、导入、评估
├─ test/                             # 31 个测试文件、验收执行器与 materials 物料
├─ data/                             # 追问题库、来源与统计数据（sources/ 入库管理）
├─ database/                         # 案例库、法律统计数据库结构
├─ docs/                             # 历史优化记录
├─ resources/legal_document_templates/  # 官方空白文书及 manifest
├─ docker/                           # Docker Compose 数据与日志
├─ README.md                         # 项目概览与快速启动
└─ 法律智能体项目说明书.md             # 本文档
```
