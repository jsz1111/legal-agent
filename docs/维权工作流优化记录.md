# 维权工作流优化记录

## 优化目标

本轮优化聚焦维权工作流的三个核心问题：

1. 建立清晰的案件边界，避免同一会话中的新纠纷继承旧案件事实、证据和法律领域。
2. 将追问选择从“模型自评分”升级为“模型提供语义判断、程序执行确定性评分与约束”的可解释决策机制。
3. 将流程收敛依据从事实数量、证据数量和笼统置信度，升级为面向责任、请求、时效、管辖、程序、证据和安全等法律决策维度的充分性判断。

## 设计约束

- 不针对单个词语、单个案例或单次失败增加硬编码分支。
- 不通过堆叠关键词 `if/else` 修复语义理解问题。
- 优先使用结构化状态、明确接口、可配置策略和通用决策模型。
- LLM 负责语义提取和自然语言生成，程序负责状态边界、策略约束、评分、审计与降级。
- 每项关键优化必须有独立测试，三项完成后运行相关测试与全量测试。

## 当前进度

| 优化项 | 状态 | 验证状态 |
|---|---|---|
| 1. 案件边界与生命周期隔离 | 已完成 | 已编写测试，待统一执行 |
| 2. 可解释追问评分与决策轨迹 | 已完成 | 已编写测试，待统一执行 |
| 3. 基于决策充分性的收敛 | 已完成 | 已编写测试，待统一执行 |
| 全量回归与测试后优化 | 已完成 | 232 项通过、5 个子测试通过 |

## 优化记录

### 2026-07-30：建立优化基线

- 已确认现有工作流采用九节点 LangGraph：轮次准备、风险检查、问题提取、澄清、评估与检索、追问、详情解析、结论生成、记录保存。
- 已确认现有状态具备结构化事实、证据状态、冲突、追问历史、置信度和检索结果等基础能力。
- 已确认三项主要风险：
  - 已结束案件收到非文书消息后会直接重开旧状态，缺少案件边界判断。
  - 追问信息增益与用户负担主要由模型自行评分，应用层只做阈值过滤。
  - 收敛仍部分依赖置信度档位和证据数量比例，不能直接表示法律决策是否已具备最低信息。

### 2026-07-30：完成优化 1——案件边界与生命周期隔离

#### 实现内容

- 新增独立的案件生命周期模块 `src/agents/legal_guide/case_lifecycle.py`。
- 为每个维权案件分配独立 `case_id`，并记录同一会话内的 `case_generation`。
- 每条进入已有维权状态的消息会先经过语义级案件边界判断，比较：
  - 当事主体；
  - 争议事件；
  - 法律关系；
  - 争议标的；
  - 时间线；
  - 用户目标。
- 案件关系统一输出为：
  - `continue`：继续原案件；
  - `new`：建立新案件；
  - `uncertain`：暂停状态变更并向用户确认。
- 应用层设置独立置信度门槛。即使模型给出继续或新建结论，低于门槛时也不会直接继承或清空状态。
- 新案件创建时：
  - 生成新的 `case_id`；
  - 清空法律领域、事实、证据、问题、追问历史和检索结果；
  - 只保留用户标识及长期记忆等非案件级上下文；
  - 将旧案件完整状态归档到独立 Redis 键。
- 继续原案件时保持原 `case_id` 和既有案情；已经结束的案件会重新进入问题分析阶段。
- 无法确认时保存用户原始消息，确认后再恢复处理，避免澄清过程丢失案情。
- 新增边界决策审计记录，保存关系、置信度、理由、消息摘要和发生轮次。

#### 通用性说明

本项优化没有使用业务关键词判断新旧案件。程序只消费结构化语义判断结果并执行状态隔离、置信度门控和安全降级；模型无法可靠判断时统一进入确认流程，不为任何单个失败案例添加特判。

#### 测试准备

新增 `test/test_case_lifecycle.py`，覆盖：

- 低置信度结果必须转为待确认；
- 新案件必须获得全新且干净的状态；
- 旧案件必须被独立归档；
- 继续原案件不得改变案件身份；
- 待确认消息不能提前污染案件；
- 用户确认后必须恢复上一条待归属消息。

### 2026-07-30：完成优化 2——可解释追问评分与决策轨迹

#### 实现内容

- 新增独立策略模块 `src/agents/legal_guide/followup_policy.py`。
- 将法律追问统一映射到七类稳定决策效果：
  - `responsibility`：责任主体和责任范围；
  - `claim_scope`：请求类型、范围或金额；
  - `limitation`：期限与关键时间节点；
  - `jurisdiction`：受理机构与管辖；
  - `procedure`：下一步程序路径；
  - `evidence_gap`：关键证明缺口；
  - `safety`：现实安全措施。
- 应用程序根据决策效果权重、题库优先级、回答负担、连续低信息回答和当前追问轮次计算候选得分。
- 安全问题保持强制门控，不参与普通候选竞争。
- 达到体验软上限后自动提高继续追问的最低价值门槛。
- 程序先选择最高价值候选，再把单个已选维度交给 LLM 做自然语言改写；LLM 不再负责候选排序。
- LLM 返回的 `information_gain` 和 `user_burden` 数字不再进入应用决策，最终数值完全由策略模块计算。
- 如果模型试图切换到另一个候选维度，计划会被拒绝，而不是接受模型越权选择。
- 对于题库耗尽后的动态问题，只接受白名单内的法律决策效果标签，再由程序重新计算价值。
- 每次规划均形成 `decision_trace`，包含：
  - 所有候选的决策效果；
  - 应用层信息增益；
  - 用户负担；
  - 题库优先级加成；
  - 最终净得分；
  - 是否可选及淘汰原因；
  - 最终选中的候选编号。
- `GuideState` 新增跨轮次 `followup_decision_trace`，最多保留最近 50 次规划轨迹。

#### 通用性说明

评分依据是稳定的法律决策维度和结构化候选元数据，不依赖“拉黑”“退款”“欠薪”等具体词语。新增领域时只需声明追问槽位及其决策效果，不需要修改节点函数或增加案例分支。

### 2026-07-30：完成优化 3——基于决策充分性的收敛

#### 实现内容

- 新增 `src/agents/legal_guide/decision_sufficiency.py`，与追问策略共用同一套法律决策维度。
- 对每个适用领域逐项计算：
  - 哪些规则已经被结构化事实或明确证据状态解决；
  - 哪些规则虽然已经问过，但用户并未回答；
  - 每个缺口会影响哪个法律决策；
  - 缺口属于阻断性还是建议性。
- 已问过但未回答的问题不再视为“已经具备信息”，从根本上区分“系统问过”和“案件说清楚”。
- 明确方案的最低条件改为：
  - 已形成具体法律问题；
  - 已形成可用的案件事实；
  - 所有当前适用的事实、程序和证据决策维度均已解决。
- 原有以下收敛条件已从新版 `should_conclude` 中移除：
  - HIGH 置信度且存在一项证据；
  - MEDIUM 置信度且证据数量比例达到阈值。
- 用户主动要求停止、连续低信息回答、追问上限和总轮次上限仍然可以结束流程，但只代表“必须按现有信息给方案”，不代表信息已经充分。
- `node_assess_retrieve` 在每轮检索后生成并持久化 `decision_sufficiency` 报告，再据此决定追问或结束。
- 当最低决策维度全部覆盖时，系统标记为 `decision_sufficient` 并停止继续追问。
- 当信息不足但必须结束时，系统输出条件式行动方案。
- 结论生成后由确定性程序附加“决策边界与条件”，逐项列出尚未覆盖的责任、请求、期限、管辖、程序、证据或安全信息，并明确：
  - 可以先做证据保全、记录沟通等低风险行动；
  - 责任、金额、期限和受理机构判断必须按条件理解；
  - 缺口核实前不得将方案理解为责任已经成立或结果已经确定。
- 面向用户的方案可信度标签优先服从决策充分性，避免出现“置信度高”但关键决策事实仍缺失的矛盾文案。

#### 通用性说明

收敛判断只读取题库规则、语义槽位、结构化事实和证据状态，不读取具体案件关键词。任何领域都通过相同的“规则是否解决 → 决策维度是否覆盖 → 明确或条件式方案”链路完成收敛。

#### 测试准备

新增：

- `test/test_followup_policy.py`：验证程序先于模型排序、模型自评分无效、模型不能更换候选、审计轨迹完整。
- `test/test_decision_sufficiency.py`：验证高置信度不再提前结束、已问未答不算解决、完整决策维度可以结束、条件式方案强制披露缺口。

## 测试记录

### 第一轮：新增机制测试

命令范围：

- `test/test_case_lifecycle.py`
- `test/test_followup_policy.py`
- `test/test_decision_sufficiency.py`

结果：`14 passed`。

### 第二轮：原有维权工作流回归

覆盖动态追问、路由、人物场景、情景契约、补充体验、风险多轮、置信度与权威来源。

首次结果：`134 passed, 13 failed`。

失败分析：

- 旧测试仍要求模型通过自报的 `information_gain` 和 `user_burden` 控制是否追问；
- 旧测试允许模型跳过程序选中的高价值事实，直接选择较低优先级证据问题；
- 旧测试要求返回结构中不存在新增的决策审计轨迹；
- 题库耗尽后的动态测试没有声明法律决策效果，因此无法通过新策略门控。

处理方式：

- 将旧契约更新为“程序评分优先、模型数字无效、模型不得更换候选”；
- 为证据安全测试补齐更高优先级事实，使测试真正进入证据阶段；
- 保留并验证新增审计轨迹；
- 为合法的题库外动态问题提供白名单决策效果标签。

复测结果：`147 passed`。

### 第三轮：全项目测试

首次结果：

- `228 passed`
- `2 failed`
- `2 errors`
- `5 subtests passed`

未通过原因：

- 两项多模态测试无法访问 Windows 系统临时目录；
- 两项 Supervisor 测试无法连接 `localhost:19531`；
- Docker 检查发现 etcd 因持久卷瞬时 `input/output error` 退出，Milvus 随之退出。

处理方式：

- 将 pytest 临时目录切换到项目工作区；
- 未删除、重建或覆盖任何 Docker 数据卷；
- 非破坏性重启 etcd 和 Milvus；
- 确认 Milvus 健康检查恢复后重跑相关测试。

最终结果：

```text
232 passed, 4 warnings, 5 subtests passed
```

耗时：48.06 秒。

剩余警告：

- 4 条既有 Pydantic Supervisor 上下文序列化警告；
- 测试进程退出时出现一次异步连接回收警告。

以上警告不影响本轮三项维权工作流优化，也未造成测试失败。

## 测试后优化

- 删除了未被任何模块调用的旧版 `check_convergence`，避免代码库同时保留“按法条数量收敛”和“按法律决策充分性收敛”两套互相冲突的逻辑。
- 保留唯一有效收敛入口 `should_conclude`，确保生产流程与测试契约一致。

### 2026-07-30：补充会话控制与结论后续办

- 案件边界语义判断新增独立的 `control_intent`：
  - `conclude_now`：停止追问，按当前信息生成方案；
  - `continue_gathering`：用户愿意继续回答；
  - `case_detail`：本轮包含实质案情；
  - `other`：其他会话行为。
- “是否继续原案件”与“是否继续追问”由同一次语义判断分别输出，避免增加额外模型调用。
- 对“就按目前情况生成吧”等未出现在固定短语中的自然表达，程序读取结构化 `conclude_now`，不把某句话加入关键词列表。
- 如果用户一边补充事实一边要求生成，系统先提取本轮事实，再按当前信息生成条件式或明确方案。
- 如果用户只要求生成，流程控制语句不会作为案件事实、证据或待回答法律问题入库。
- 方案生成后，用户继续补充原案件时：
  - 案件边界判断为 `continue`；
  - 保持原 `case_id`；
  - 将阶段从 `END` 恢复为 `ISSUE_SEARCH`；
  - 新事实进入结构化解析；
  - 重新计算追问价值和决策充分性；
  - 生成更新后的方案，而不是建立重复案件。

#### 补充验证

- 定向回归：`71 passed`；
- 全项目回归：`234 passed, 4 warnings, 5 subtests passed`；
- 覆盖“只要求立即生成”“补充事实并立即生成”“结论后继续补充原案件”和“新事项建立独立案件”等状态转换；
- 剩余 4 条 Pydantic 序列化警告及进程退出时的异步连接回收警告均为既有警告，未造成测试失败。

### 2026-07-31：补齐行动方案后的继续补充入口

- 行动方案末尾新增确定性的后续操作提示：
  - 用户可以直接发送新的事实或证据；
  - 用户可以回复“继续补充”，由系统继续询问当前价值最高的一个问题；
  - 后续内容归入同一案件，并触发决策充分性重算与方案更新。
- “继续补充”被识别为 `continue_gathering` 时，无论此前是否处于补充选择菜单，都直接进入追问规划。
- 纯会话控制语句不进入法律问题或案情事实抽取，因此不会再显示“已把‘继续补充’记为案情”的错误确认。
- 如果用户在表达继续意愿的同时提供了新事实，仍会正常抽取新事实，再继续工作流。
- 新增体验回归覆盖方案尾部入口、纯继续控制路由、同一案件续办及防重复提示。
- 定向回归结果：`27 passed`；全项目回归结果：`236 passed, 4 warnings, 5 subtests passed`。

#### 继续补充后的软收敛修正

- 根据真实对话“被他人殴打 → 未报警 → 生成方案 → 继续补充”复盘发现：
  - `continue_gathering` 已正确识别；
  - 状态也已恢复到原案件；
  - 但三轮追问后的价值门槛仍从 `0.45` 提高到 `0.62`；
  - 当时仍未覆盖的“原始线索”和“伤情材料”候选得分约为 `0.547`，被软门槛淘汰后再次生成方案。
- 用户明确选择继续时，将追问价值门槛恢复为普通门槛；该规则作用于所有法律领域和决策维度，不依赖“被打”“报警”等案情关键词。
- 用户明确选择继续时，可以越过决策充分性自动收敛和用户耐心软门槛；以下硬边界仍然保留：
  - 用户明确要求生成方案；
  - 追问总数达到 `GUIDE_MAX_ASK_ROUNDS`；
  - 总对话轮次达到 `GUIDE_MAX_TOTAL_ROUNDS`；
  - 其他必须立即停止的安全或系统边界。
- 结论后恢复案件时清零连续低信息计数，避免用户已经重新表达配合意愿后仍被旧耐心状态立即收敛。
- 新增回归验证软门槛前后候选净得分不变，只改变用户重新授权后的可选资格，并验证显式继续可以进入追问规划。
- 定向回归结果：`45 passed`。
- 全项目回归结果：`238 passed, 4 warnings, 5 subtests passed`。

### 2026-07-31：以用户帮助和实际进展为中心的收敛优化

#### 一轮一个法律决策目标

- 将“一次追问只能问一个字段”调整为“一轮只服务一个法律决策目标”。
- 同一个目标下允许组合 2-3 个通常可以一起回答的紧密要素，例如经营者、商品和支付金额。
- 责任、时效、程序、证据、诉求等不同目标仍不得拼成表单式连问。
- 应用程序继续只选择一个最高价值候选；LLM 只负责把该候选自然表达，不能自行增加其他决策目标。

#### 有效否定与真正无进展分离

- “没有报警”“没有发票”“不认识对方”“暂时不知道”等回答会改变程序、证据替代和后续行动，视为有效回答。
- 证据明确不存在会写入 `unavailable`，事实无法获知会写入 `unknown`，二者不再增加连续低信息计数。
- 只有含义不清、前后冲突、没有回答当前问题且没有新增事实或材料时，才计为无进展。
- 连续 2 次无进展时切换为低负担选择，允许用户简短回答、记录为未知或按现有信息生成方案。
- 连续 3 次真正无进展才强制生成条件式方案。

#### 动态追问额度与异常保险

- 默认主动追问上限保留为 6 次。
- 用户明确选择继续且仍有高价值问题时，可以逐次开放到绝对上限 8 次。
- 达到绝对上限后不再提示用户点击“继续补充”，但仍允许用户直接发送新的事实或证据并更新同一案件。
- 总用户消息上限由 12 轮提高到 20 轮，仅作为异常循环保险，不参与普通案件的日常收敛。
- 普通收敛继续以决策充分性、候选价值和实际进展为主。

#### 用户反问即时处理

- 用户在追问过程中提出问题时，系统先基于当前已检索法条和结构化案情给出简短回答，再恢复原来的一个关键追问。
- 依据不足时必须明确不能确定，不得为了即时回答而编造法条、期限、金额或机构信息。
- 已经即时回答的问题不再放入最终方案重复作答；达到反问硬上限时仍会安全收敛。

#### 可恢复的安全中断

- 现实危险不再被建模为“案件永久结束”，而是 `safety_pause_active` 安全中断。
- 中断期间保留 `case_id`、原始危险陈述和完整状态；即使首轮还没有完成法律问题抽取，也会持久化该状态。
- 用户后续状态仍不明确时，只确认是否已经脱离危险，不进入普通维权追问。
- 用户确认安全后，恢复同一案件，将暂停前的原始事件与最新安全状态一起进入结构化解析。
- 安全恢复消息如果同时要求立即生成方案，仍保留 `conclude_now` 控制意图。

#### 阶段性验证

- 单一决策目标、有效否定、动态额度、反问即时回答、安全暂停及恢复等相关回归：`101 passed`。
- 扩展工作流回归：`157 passed`。
- 全项目回归：`246 passed, 4 warnings, 5 subtests passed`。
- 剩余警告仍为既有 Pydantic Supervisor 上下文序列化警告及测试进程退出时的异步连接回收警告，未造成测试失败。

### 2026-07-31：证据效用评估子系统（第一阶段）

#### 从“是否持有”升级为“证明目标覆盖”

- 保留现有 9 个顶层工作流节点，不新增独立 Agent。
- 新增通用证据评估模块，将当前案件拆成：
  - 结构化证据项；
  - 领域证明目标；
  - 证据与证明目标之间的关联；
  - 证明目标覆盖状态和下一步补强建议。
- 证明目标直接来自领域追问题库，不在工作流节点中为发票、聊天记录、病历等单独增加关键词分支。
- 每项证明目标区分：
  - `preliminarily_covered`：基础来源和完整性信息已覆盖，可用于形成较明确的行动计划；
  - `partially_covered`：已有相关材料，但原始载体、完整性、主体、时间或取得方式仍需核验；
  - `known_missing`：用户明确表示材料不存在；
  - `conflicted`：关于材料是否持有的陈述前后冲突；
  - `unresolved`：尚未确认是否存在相关材料。

#### 来源约束与能力边界

- LLM 只负责从用户陈述中结构化材料属性，不能判断真实性、合法性、可采性或最终证明力。
- 原始载体、内容完整性、主体、时间和取得方式等属性必须有用户原文锚点；没有原文支持的模型字段会被丢弃。
- 系统公开展示“可能证明什么、当前缺口、下一步补强”，不输出单一的“证据有效分”或胜诉概率。
- “用户说有材料”和“该证明目标已经覆盖”被彻底分离。

#### 接入追问、置信度和收敛

- 部分覆盖的关键材料可以生成一次低负担的质量追问，询问同一证明目标下的原始载体、完整性、主体和时间。
- 证据准备度不再只按材料数量计算；部分覆盖只获得有限准备度，避免一张未经核验的截图明显抬高置信度。
- 用户明确表示“没有某项材料”仍属于有效回答，不再重复追问存在性；但它会被记录为已知证明缺口，不能被误判成证据充分。
- 决策充分性开始读取证明目标覆盖结果：部分覆盖、明确缺失和冲突会进入条件式方案边界。
- 最终方案新增应用程序确定性生成的“证据作用与缺口”，说明材料用途、待核验点、补强方向和能力边界。

#### 阶段性验证

- 新增证据评估契约测试，覆盖部分覆盖、初步覆盖、明确缺失、原文锚点约束、质量追问、条件式收敛和用户侧覆盖矩阵。
- 证据评估、决策充分性及追问策略首轮定向回归：`17 passed`。
- 扩展相关工作流回归：`78 passed`。
- 第一轮全项目回归：`253 passed, 4 warnings, 5 subtests passed`。
- 新增解析接入和最终覆盖矩阵测试后，证据子系统自身回归：`9 passed`。
- 最终全项目回归：`255 passed, 4 warnings, 5 subtests passed`。
- 剩余警告仍为既有 Pydantic Supervisor 上下文序列化警告及异步连接回收警告，未造成测试失败。

#### 真实接口复盘后的二次优化

- 使用“完整平台订单 + 裁剪的问题截图”走真实消费维权接口后发现：
  - LLM 可能把订单中的商家名称、金额、日期等内容误拆成多个证据项；
  - 同一目标已有一份基础属性完整的材料时，其他较弱副本仍会把整个目标降为部分覆盖；
  - 仅靠题库材料名称匹配，无法稳定识别名称不同但证明内容相同的材料。
- 采用通用结构修正，没有向节点函数增加“截图”“裁剪”等个案关键词：
  - 以带原文锚点的 `evidence_details` 作为当前轮材料级清单，材料内部字段不再进入证据库存；
  - 覆盖判断改为“是否至少存在一份基础属性完整的支持材料”，较弱副本不再错误降级更强材料；
  - 新增跨领域证明角色本体，包括交易、付款、关系、事件、问题、身份、时间、沟通、程序、损害、责任、权属和侵权等；
  - 材料可以通过题库关系或证明角色关联目标，避免依赖某个案件措辞。
- 持久化状态复核确认：
  - 错误拆分的金额、日期等内容已从证据清单移除；
  - 完整平台订单对应的交易付款目标已变为 `preliminarily_covered`；
  - 商品问题截图可以通过 `problem` 证明角色关联到问题材料目标，并保持 `partially_covered`。
- 二次优化定向回归：`64 passed`。
- 二次优化最终全项目回归：`258 passed, 4 warnings, 5 subtests passed`。

### 2026-07-31：动态追问与知识检索性能优化

#### 先决策追问，再按需检索

- `assess_retrieve` 调整为先计算证据覆盖、置信度、决策充分性和追问候选，再判断是否需要知识检索。
- 当本轮仍需追问时，完全跳过法条向量检索、案例检索、知识图谱、渠道查询、PG 补充和重排序。
- 只有本轮确定生成行动方案时，才执行完整知识检索；追问逻辑只依赖结构化案情、证据覆盖和应用程序持有的候选评分。
- 最终检索结果新增案情指纹。事实、问题、证据、时间或地区未变化时可以复用快照；关键状态变化后会重新检索，避免复用过期依据。

#### 动态追问的确定性降级

- 应用程序继续负责候选资格、信息增益、用户负担和优先级排序；LLM 只负责把已选中的一个候选表达得自然。
- 追问措辞调用超过 8 秒、返回非法结构、拒绝提问、擅自更换候选或生成不合格问句时，直接回退到同一政策候选的题库表达。
- 降级不会改问另一个未经策略选择的问题，也不会因为措辞模型失败而提前生成方案。
- 回退结果保留候选编号、决策维度、评分和选择轨迹，便于解释“为什么先问这一点”。

#### 分阶段超时与安全降级

- 紧急识别、问题抽取、回答解析、追问措辞、案件边界、最终生成和审校均配置独立超时，不再允许单个模型调用无限占用整轮。
- 追问答案解析失败时，一般陈述会作为待核实事实保留，疑问句仍按用户反问处理，不再直接丢弃本轮输入。
- 明确的现实危险信号在模型调用前触发高精度安全熔断，避免紧急识别模型超时导致安全保护失效。
- 最终方案生成超时后，基于已检索原文、结构化案情和渠道数据生成确定性五段式方案，并继续执行法条白名单、证据边界和追问清理。
- PG 补充、法条标题、渠道查询和重排序也增加独立超时；局部失败只降级对应部分，不拖垮整轮。

#### 阶段性验证

- 新增“追问时零全量检索”“收敛时才检索”“追问模型失败仍询问策略首选问题”“解析超时保留用户陈述”“最终生成超时仍返回完整方案”等回归测试。
- 本轮性能与工作流定向回归：`78 passed`。
- 本轮最终全项目回归：`262 passed, 4 warnings, 5 subtests passed`。
- 剩余警告仍为既有 Pydantic Supervisor 上下文序列化警告，不影响维权工作流执行。

### 2026-07-31：短回答上下文与重复追问回归修复

#### 根因复盘

- 真实会话日志显示，`deepseek-v4-flash` 在问题抽取阶段把大量输出额度用于隐藏推理，连续四轮在 15 秒超时。
- 超时后虽然保留了用户原话，但旧降级路径只能把它存成无类型事件，法律领域一直停在 `other`，主体、关系、金额等语义维度没有进入追问覆盖判断。
- 旧澄清节点只向模型提供最后一句用户回复，也没有把澄清问题登记为待回答问题，因此“投资理财”“亏了钱”“是个人”等短回答容易被当成脱离上下文的新描述。
- 固定题库问题包含多个维度；即使其中部分已经被回答，模型超时后的固定降级仍会把整句题目重新问一遍。

#### 上下文接续修复

- 澄清节点现在读取最近用户与助手对话以及已保存的结构化案情，明确禁止再次询问已经回答的内容。
- 每一道澄清问题都会写入 `pending_ask_details`；下一轮先按“问题 + 回答 + 案情”解析，不再直接把短回答重新分类。
- 生成式问题抽取失败时，最近三轮原始用户对话会作为语义术语映射种子保留，不再返回空问题和空事实。
- 即使标准法律问题尚未形成，只要已有口语问题或案情事实，Redis 仍保留维权状态，避免下一轮从空白案件重新开始。

#### 模型阶段配置修复

- 法律问题抽取提示词删除大段示例和重复格式说明，保留领域、原子事实、证据属性及原文锚点契约。
- 短结构化任务统一使用低推理强度和独立输出上限；最终方案仍保留单独的较大输出额度。
- 同一真实案情的抽取从连续 15 秒超时变为约 6.6 秒内返回完整 JSON，并识别为 `consumer_market`。
- 超时路径仍保留确定性语义种子，模型配置失效时不会再次丢失用户陈述。

#### 已回答维度去重

- 通用 `legal_relationship` 决策现在同时建模“发生了什么”和“对方/双方关系”两个维度。
- 固定降级问题不再机械展示整条题库问句；当同一决策目标已有部分维度覆盖时，只询问第一个未覆盖维度。
- 即使 LLM 返回的自然问句重新夹带了已覆盖维度，应用程序也会按覆盖矩阵收窄为唯一缺失维度，并同步收窄回答提示。
- 例如事件经过已经明确但对方身份未知时，只问对方身份；主体和事件都明确后，该候选直接退出，不再换一种措辞重复追问。
- 早期低信息领域标签允许被后来带原文锚点的更具体事实修正；完整检索完成后仍保持领域稳定，避免无依据漂移。
- 用户没有回答当前问题、而是主动补充了另一项实质案情时，会先保存该信息并重新识别法律问题和领域，而不是强行按原问题解释。

#### 阶段性验证

- 新增短回答上下文、澄清待回答登记、抽取超时语义保留、未覆盖维度聚焦、已回答候选退出、早期领域修正和降级状态持久化测试。
- 相关工作流回归：`140 passed`。
- 本轮最终全项目回归：`269 passed, 4 warnings, 5 subtests passed`。

### 2026-07-31：首轮案件材料包与多格式材料接入

#### 首轮尽量一次说清

- 对话区新增“首次提交案件信息”入口，引导用户一次填写事情经过、对方及双方关系、时间地点金额、期望结果和既有处理情况。
- 只把用户实际填写的字段写入案件材料包；空项明确表示本轮未提供，禁止系统自行推测。
- 该入口复用原有维权状态机，不另建一套简化流程；因此结构化内容会正常进入事实抽取、领域识别、证据评估、充分性判断和动态追问。
- 用户仍可直接使用普通对话框，不强迫填写固定表单；表单的作用是降低首轮信息遗漏和后续重复追问。

#### 已有材料一次提交

- 对话附件现支持图片、PDF、DOCX 和 TXT；旧界面中“可以选择文档但实际不解析”的断链已消除。
- PDF 最多提取前 30 页，单文件和单轮均设置文本边界；附件总数限制为 8 个，单文件限制为 10MB，防止异常材料拖垮一轮请求。
- 每个已处理附件都生成 SHA-256 摘要，并在送入案件状态前保留文件名、来源形式、提取文字和核对提示。
- 扫描型 PDF 未提取到文字时，不假装已读取内容，而是提示改传清晰图片或手动说明关键内容。
- 不再接收旧版 `.doc`，避免界面宣称支持而后端无法可靠解析；可先转为 DOCX 或 PDF。

#### 证据来源状态接入

- 证据库存新增附件来源识别：只有原文锚点确实位于带摘要指纹的图片或文档块中，才标记为 `uploaded_copy`。
- 用户只说“我有合同/发票”时仍标记为 `user_claimed_present`；上传了无关附件也不会把口述材料错误升级为已上传副本。
- `uploaded_copy` 只代表系统收到并读取了一个副本，不代表原件真实性、内容完整性、关联性或法律可采性已经确认。
- 后续追问可以据此优先检查材料质量缺口，而不是重复询问“有没有这份材料”。

#### 阶段性验证

- 新增首轮材料包构造、TXT 提取与摘要、文档单次注入、表单提交清空、附件来源升级和无关附件不误升级测试。
- 本轮针对性回归：`26 passed`。
- 本轮最终全项目回归：`275 passed, 4 warnings, 5 subtests passed`。

### 2026-07-31：测试物料驱动的证据入口与内容一致性优化

#### 附件内容与用户陈述分层

- 带文件摘要指纹的附件块不再直接进入法律问题抽取；问题识别只读取用户案情和紧凑附件清单，避免长文档把法律领域挤成 `other`。
- 无论证据质量模型是否超时，应用程序都先确定性保存文件名、来源形式、摘要指纹和提取片段，单个模型失败不会让附件从案件状态中消失。
- 首次案件材料包的五个表单字段由应用程序直接解析为稳定语义键；小模型只负责法律领域、问题、地区和时间分类，防止分类超时导致已经填写的事实丢失或被重复追问。
- 后续补交附件使用同一分层逻辑：附件文字不会被误当成用户对上一道问题的否定回答，也不会因用户只说“继续补充”而绕过材料入库。

#### 材料质量与参考资料区分

- 证据项新增 `case_specificity`，区分本案具体记录、空白模板/法规/票样、无法判断和未知状态。
- 空白合同示范文本、票样、法规或办事指南即使成功上传，也不能覆盖本案劳动关系、交易、就医或投诉处理等证明目标。
- 质量检查限制为“可见基础属性”，只判断材料是否完整、能否看清主体和时间、取得方式及可能证明的事实类型，不判断真实性、责任、可采性或胜诉。
- 模型引用必须能够回到附件可见原文；允许将多个逐字字段用分号紧凑连接，但任一片段无法在原文中找到时整条结果都会被丢弃。

#### 多材料并行检查与冲突识别

- 每份附件独立执行短结构化质量判断并并行运行；一份材料超时或格式错误不会拖累同批其他附件，失败项只进行一次独立重试。
- 订单、支付、聊天和平台工单不再要求一次生成一个很长的多文件 JSON，降低输出截断和整批降级概率。
- 对可跨材料比较的稳定字段建立通用声明层，目前覆盖交易金额、支付日期和订单编号；同一字段出现不同值时，相关材料统一标记内容冲突。
- 冲突状态不会直接判断哪份材料真实，而是要求优先核对平台、银行或机构可回查的原始记录。

#### 真实物料验证

- 消费纠纷四文件链路正确识别为 `consumer_market`；完整订单和支付记录使交易付款目标达到初步覆盖，裁剪聊天仍保持部分覆盖，且未重复追问表单中已有的时间。
- 补交冲突摘要后，系统识别出 `800/1200 元`及`2026-07-18/2026-07-19`两组跨材料矛盾。
- 官方劳动合同示范文本被识别为 `blank_or_reference`，不会被当作已经签署的劳动合同。
- 劳动场景识别为 `labor_social_security`，合同摘要、工资流水和考勤转录均保持副本/摘要边界；人身伤害场景识别为 `criminal_public_security`，报警、病历和费用材料分别进入程序、损害和损失证明链。
- 当前定向回归：`83 passed`。
- 最终全项目回归：`282 passed, 4 warnings, 5 subtests passed`。
- 剩余警告仍为既有 Supervisor 测试中的 Pydantic 上下文序列化提示，未造成测试失败。

### 2026-07-31：用户控制按钮与文书路由隔离

#### 快捷操作卡片

- 对话区新增“现在生成方案”“继续补充”“生成参考文书”三个快捷按钮。
- 按钮仍复用已有文字控制协议，因此用户既可以点击，也可以继续输入原口令。
- “现在生成方案”用于停止当前追问并按现有信息收敛。
- “继续补充”用于在同一案件中恢复信息收集，并继续只问一个最关键问题。
- “生成参考文书”用于直接进入独立文书服务，不再要求普通用户记住固定口令。

#### 控制意图与案情事实隔离

- 文书请求不再依赖案件状态必须恰好处于 `END`；已有实质案件状态时，无论是条件式方案、恢复会话还是补充阶段，都可以进入文书服务。
- “生成文书”不再进入法律问题抽取、动态追问、知识检索或行动方案生成流程。
- 文书指令和生成的文书正文不写入案情消息池，避免后续继续补充时被误当成用户事实。
- 文书只读取已有 `draftable_facts`、证据清单和检索依据；不会把“生成文书”写进“理解您的情况”。
- 文书生成后保持原案件阶段，用户仍可改文书类型、重新生成或继续补充。

#### 阶段性验证

- 新增非结束阶段文书请求的路由隔离测试，确认不会进入 GuideGraph，且控制指令不会污染案件事实。
- 新增三个快捷按钮的精确指令映射测试。
- 文书路由、按钮、附件和官方模板定向回归：`24 passed`。
- 真实接口验证：第二轮“生成文书”直接返回`消费者投诉信`和文书附件，不重复行动方案，不写入控制指令。
- 最终全项目回归：`284 passed, 4 warnings, 5 subtests passed`。

## 最终结论

三项优化已经形成统一闭环：

1. 案件生命周期决定一条消息应进入哪个案件状态。
2. 追问策略决定当前案件最值得询问哪个法律决策点。
3. 决策充分性决定何时停止追问，以及应输出明确方案还是条件式方案。

整个闭环建立在结构化事实、语义槽位、证据状态和稳定法律决策维度之上，没有为单个词语、单个对话或单个失败案例增加节点内硬编码分支。
